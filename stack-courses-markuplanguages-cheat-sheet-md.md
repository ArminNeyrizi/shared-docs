در اینجا خلاصه‌ای از مهم‌ترین و پرکاربردترین دستورات مارک‌داون (Markdown) آورده شده است. این دستورات به شما کمک می‌کنند تا متن خود را به سرعت و با فرمت‌بندی مناسب بنویسید.

### ۱. عنوان‌ها (Headings)
برای ایجاد عنوان از تعدادی علامت `#` استفاده می‌کنیم. هر چه تعداد `#` بیشتر باشد، عنوان کوچک‌تر می‌شود.

```markdown
# عنوان سطح ۱ (بزرگ‌ترین)
## عنوان سطح ۲
### عنوان سطح ۳
#### عنوان سطح ۴
```

### ۲. ضخیم و کج کردن متن (Bold & Italic)
- **ضخیم (Bold):** متن را بین دو ستاره `**` یا دو آندرلاین `__` قرار دهید.
- **کج (Italic):** متن را بین یک ستاره `*` یا یک آندرلاین `_` قرار دهید.
- **ضخیم و کج:** متن را بین سه ستاره `***` یا سه آندرلاین `___` قرار دهید.

```markdown
**این متن ضخیم است**
*این متن کج است*
***این متن ضخیم و کج است***
```

### ۳. لیست‌ها (Lists)
- **لیست مرتب‌نشده (Bullet Points):** از `-`، `*` یا `+` استفاده کنید.
- **لیست مرتب‌شده (Numbered):** از اعداد به همراه نقطه `1.` استفاده کنید.

```markdown
- آیتم اول
- آیتم دوم
  - زیرمجموعه آیتم دوم

1. مرحله اول
2. مرحله دوم
3. مرحله سوم
```

### ۴. لیست انجام‌دادنی (To-Do List)
همان‌طور که قبلاً توضیح داده شد:

```markdown
- [ ] انجام نشده
- [x] انجام شده
```

### ۵. لینک و تصویر (Links & Images)
- **لینک:** از `[]` برای متن و `()` برای آدرس استفاده کنید.
- **تصویر:** مانند لینک عمل می‌کند، اما قبل از `[]` علامت تعجب `!` قرار می‌گیرد.

```markdown
[متن لینک](https://example.com)
![متن جایگزین تصویر](https://example.com/image.jpg)
```

### ۶. نقل‌قول (Blockquote)
برای ایجاد نقل‌قول از علامت `>` استفاده می‌کنیم.

```markdown
> این یک نقل‌قول است.
> می‌تواند چند خطی هم باشد.
```

### ۷. کد (Code)
- **کد درون‌خطی (Inline):** متن کد را بین دو بک‌تیک `` ` `` قرار دهید.
- **بلوک کد (Code Block):** برای کدهای طولانی، از سه بک‌تیک `` ``` `` در بالا و پایین کد استفاده کنید. می‌توانید نام زبان برنامه‌نویسی را بعد از سه بک‌تیک اول بنویسید تا رنگ‌بندی (Syntax Highlighting) فعال شود.

```markdown
این یک متغیر است: `var x = 10;`

```javascript
function sayHello() {
  console.log("Hello!");
}
```
```

### ۸. خط جداکننده (Horizontal Rule)
برای ایجاد خط افقی از سه یا بیشتر `-`، `*` یا `_` استفاده کنید.

```markdown
---
```

### ۹. جدول (Tables)
جدول‌ها با استفاده از خط‌چین `-` و خط عمودی `|` ساخته می‌شوند.

```markdown
| عنوان ۱ | عنوان ۲ |
| ------- | ------- |
| سلول ۱  | سلول ۲  |
| سلول ۳  | سلول ۴  |
```

### ۱۰. پانویس و ارجاع (Footnotes)
برای ایجاد پانویس از `[^1]` استفاده می‌کنید و تعریف آن را در پایین صفحه می‌نویسید.

```markdown
این یک متن با ارجاع است[^1].

[^1]: این توضیح پانویس است.
```

---

### جدول سریع مرجع (Cheat Sheet)

| کار | سینتکس | مثال |
| :--- | :--- | :--- |
| **ضخیم** | `**متن**` | `**Bold**` |
| *کج* | `*متن*` | `*Italic*` |
| ~~خط‌خورده~~ | `~~متن~~` | `~~Strikethrough~~` |
| لینک | `[متن](آدرس)` | `[Google](https://google.com)` |
| تصویر | `![alt](آدرس)` | `![Logo](img.png)` |
| کد درون‌خطی | `` `کد` `` | `` `code` `` |
| بلوک کد | ``` ``` | ```` ```python\nprint("Hi")\n``` ```` |
| لیست مرتب‌نشده | `- متن` | `- Item 1` |
| لیست مرتب‌شده | `1. متن` | `1. First` |
| نقل‌قول | `> متن` | `> Quote` |
| خط جداکننده | `---` | `---` |
| چک‌باکس | `- [ ]` یا `- [x]` | `- [ ] Task` |

این دستورات پایه‌ای‌ترین و کاربردی‌ترین عناصر مارک‌داون هستند که در بیشتر پلتفرم‌ها (مثل GitHub، Notion، Obsidian و...) کار می‌کنند.