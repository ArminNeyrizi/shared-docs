
# 🎮 بازی Latency Master

## 🎯 هدف پروژه
ساخت یک وب‌اپلیکیشن بازی‌گونه که در آن کاربر باید عبارات لاتک (LaTeX) را با سرعت و دقت بالا تایپ کند. این بازی باید مشابه بازی‌های تایپ سرعتی (مثل Monkeytype یا TypeRacer) عمل کند، اما با چالش اضافه‌ی سینتکس لاتک.

**هدف اصلی:** پیاده‌سازی منطق تشخیص کلمه به کلمه (Word-by-Word) یا کاراکتر به کاراکتر (Character-by-Character) برای عبارات لاتک، با استفاده از منطق مشابه `obsidian-latex-suite` برای تبدیل تگ‌ها.

## 🛠 تکنولوژی‌ها
*   **فریم‌ورک:** آزاد (React, Vue, Svelte یا Vanilla JS).
*   **رندرینگ لاتک:** **KaTeX** (برای نمایش سریع پیش‌نمایش عبارت هدف).
*   **ذخیره‌سازی:** LocalStorage (برای ثبت رکوردها).
*   **منبع الهام:** منطق تبدیل تگ‌ها از `obsidian-latex-suite`.

---

## 📋 ویژگی‌های کلیدی بازی (Game Mechanics)

### ۱. حالت‌های بازی (Game Modes)
کاربر باید بتواند بین دو حالت انتخاب کند:
*   **حالت تمرین (Practice Mode):** عبارات به ترتیب یا تصادفی نمایش داده می‌شوند. بدون محدودیت زمانی. فقط برای یادگیری سینتکس.
*   **حالت سرعتی (Speed Challenge):**
    *   زمان شروع از لحظه‌ای است که کاربر اولین کلید را می‌زند.
    *   تعداد عبارات محدود است (مثلاً ۱۰ یا ۲۰ عبارت).
    *   در پایان، **WPM** (Words Per Minute) و **Accuracy** (دقت) محاسبه می‌شود.

### ۲. رابط کاربری بازی (UI Layout)
صفحه بازی باید شامل بخش‌های زیر باشد:
1.  **بخش هدف (Target Display):**
    *   عبارت لاتک هدف با فونت بزرگ و خوانا نمایش داده شود.
    *   *نکته:* می‌توانید از KaTeX استفاده کنید تا عبارت هدف را به صورت فرمول ریاضی زیبا نمایش دهید، یا فقط کد خام لاتک را نشان دهید تا کاربر کد را یاد بگیرد. (پیشنهاد: نمایش کد خام + پیش‌نمایش کوچک فرمول در کنار).
2.  **بخش ورودی (Input Area):**
    *   یک فیلد ورودی (Input/Textarea) که کاربر تایپ می‌کند.
    *   کاراکترهای تایپ شده صحیح باید **سبز**، کاراکترهای غلط **قرمز** و کاراکترهای باقی‌مانده **خاکستری** شوند (مانند بازی‌های تایپ معروف).
3.  **پنل آمار (Stats Panel):**
    *   نمایش زمان سپری شده.
    *   نمایش WPM لحظه‌ای.
    *   نمایش دقت (Accuracy %).
4.  **پنل رکوردها (Leaderboard):**
    *   نمایش ۵ رکورد برتر کاربر در LocalStorage.

### ۳. منطق بازی و هوش مصنوعی ساده (The "Brain")
این بخش مهم‌ترین قسمت فنی پروژه است:

*   **سیستم تشخیص خطا (Error Detection):**
    *   اگر کاربر اشتباه تایپ کند، بازی نباید متوقف شود (مثل بازی‌های مدرن)، اما خطا باید شمارش شود و روی دقت اثر بگذارد.
    *   کلید `Backspace` باید کار کند اما فقط تا آخرین کاراکتر صحیح.
*   **تبدیل هوشمند تگ‌ها (Smart Tag Conversion):**
    *   مشابه افزونه Obsidian، اگر کاربر `\a` را تایپ کرد و سپس کلید `Space` یا `,` یا `(` را زد، باید به صورت خودکار به `\alpha` تبدیل شود.
    *   این تبدیل باید در حین تایپ (Live) انجام شود و بلافاصله در UI بازتاب یابد.
*   **تشخیص پایان عبارت:**
    *   وقتی کاربر کل عبارت را درست تایپ کرد، به طور خودکار به عبارت بعدی برود (با یک انیمیشن نرم).

### ۴. سیستم رکورد و ذخیره‌سازی (Persistence)
*   پس از اتمام بازی، اطلاعات زیر در `localStorage` ذخیره شود:
    *   تاریخ و ساعت.
    *   WPM (Words Per Minute).
    *   Accuracy (دقت).
    *   تعداد خطاها.
*   یک صفحه "تاریخچه" (History) ایجاد کنید که کاربر بتواند روند پیشرفت خود را ببیند.

---

## 📐 معیارهای ارزیابی (Rubric)

### ۱. منطق بازی و مدیریت وضعیت (Game Logic & State) - ۴۰٪
*   **دقت در مقایسه:** آیا سیستم به درستی کاراکتر به کاراکتر ورودی کاربر را با متن هدف مقایسه می‌کند؟
*   **مدیریت خطا:** آیا بازخورد بصری (قرمز/سبز) بلافاصله و بدون تاخیر (Lag) اعمال می‌شود؟
*   **پیاده‌سازی تگ‌ها:** آیا تبدیل خودکار تگ‌ها (مثل `\b` به `\begin`) به درستی و بدون تداخل با تایپ عادی کار می‌کند؟

### ۲. رابط کاربری و تجربه کاربری (UI/UX) - ۳۰٪
*   **طراحی جذاب:** استفاده از رنگ‌های مناسب، انیمیشن‌های نرم برای تغییر کلمات، و فونت‌های خوانا.
*   **واکنش‌گرایی:** بازی در موبایل و دسکتاپ به خوبی کار کند.
*   **تمرکز:** رابط کاربری نباید حواس کاربر را پرت کند.

### ۳. کیفیت کد و معماری (Code Quality) - ۲۰٪
*   **جداسازی منطق:** منطق بازی (محاسبه WPM، مقایسه رشته‌ها) نباید داخل کامپوننت‌های نمایشی (View) پخش شود.
*   **کارایی:** استفاده از `requestAnimationFrame` یا بهینه‌سازی رندرها برای جلوگیری از لگ در تایپ سریع.
*   **تمیزی کد:** نام‌گذاری متغیرها و توابع به زبان انگلیسی و معنی‌دار.

### ۴. مستندات (Documentation) - ۱۰٪
*   فایل `README.md` شامل:
    *   نحوه بازی.
    *   توضیح نحوه پیاده‌سازی تبدیل تگ‌ها (با ارجاع به کد اصلی Obsidian).
    *   اسکرین‌شات از صفحه بازی و جدول رکوردها.

---

## 💡 راهنمایی فنی برای دانشجوها (Hints)

### ۱. چالش اصلی: مدیریت Cursor و Selection
در بازی‌های تایپ، شما نباید اجازه دهید کاربر با ماوس یا کلیدهای جهت‌نما (Arrow Keys) جایش را در متن تغییر دهد.
*   **راهکار:** رویدادهای `keydown` را بررسی کنید. اگر کلیدی غیر از حروف، اعداد، `Backspace`، `Space` یا `Enter` بود، اجازه تغییر وضعیت را ندهید (مگر اینکه در حال انتخاب کل کلمه برای پاک کردن باشد).

### ۲. پیاده‌سازی تبدیل تگ‌ها (The Abbreviation Logic)
شما نیاز به یک تابع دارید که قبل از نمایش کاراکتر جدید، چک کند آیا آخرین کلمات تایپ شده یک تگ مخفف هستند یا خیر.
*   *مثال:*
    ```javascript
    const abbreviations = {
      '\\a': '\\alpha',
      '\\b': '\\begin{equation}',
      // ...
    };

    function processAbbreviation(currentInput) {
       // چک کن آیا currentInput با یکی از کلیدهای abbreviations تمام می‌شود؟
       // اگر بله، آن را جایگزین کن.
    }
    ```

### ۳. محاسبه WPM
فرمول استاندارد:
`WPM = (تعداد کاراکترهای صحیح / 5) / زمان به دقیقه`

### ۴. استفاده از KaTeX برای نمایش هدف
برای اینکه کاربر بداند فرمولی که دارد تایپ می‌کند چه شکلی است، می‌توانید عبارت هدف را با KaTeX رندر کنید و در کنار کد خام نمایش دهید.

---

## 🚀 مراحل پیشنهادی (Milestones)

1.  **هفته اول (هسته بازی):**
    *   ایجاد لیست عبارات لاتک (آرایه‌ای از رشته‌ها).
    *   ساخت بخش نمایش هدف و ورودی.
    *   پیاده‌سازی مقایسه کاراکتر به کاراکتر و رنگ‌آمیزی (سبز/قرمز).

2.  **هفته دوم (منطق تبدیل تگ‌ها):**
    *   ایجاد دیکشنری تگ‌ها (بر اساس کد Obsidian).
    *   پیاده‌سازی تابع تشخیص و جایگزینی تگ‌ها هنگام تایپ.

3.  **هفته سوم (آمار و رکورد):**
    *   محاسبه WPM و Accuracy.
    *   اتصال به LocalStorage برای ذخیره و نمایش رکوردها.

4.  **هفته چهارم (بهبود UI و باگ‌گیری):**
    *   اضافه کردن انیمیشن‌ها.
    *   تست در موبایل.
    *   بهینه‌سازی کد.

---

## ❓ سوال نهایی
آیا مایلید یک **لیست نمونه عبارات لاتک** (از ساده تا پیشرفته) برای استفاده در بازی به دانشجوها ارائه دهم؟ یا خودشان باید لیست را از منابع لاتک پیدا کنند؟ (پیشنهاد من ارائه یک لیست پایه توسط شماست تا روی منطق تمرکز کنند).