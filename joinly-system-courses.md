در کل مهندسی نرم‌افزار، وقتی می‌خواهیم یک ساختار جدید (مثل وصل کردن User به Student) را روی داده‌های از قبل موجود پیاده کنیم، همیشه از یک **قانون ۳ مرحله‌ای (Three-Step Migration)** استفاده می‌کنیم:

#### مرحله ۱: اضافه کردن فیلد به صورت «اختیاری» (Safe Change)

ابتدا تغییرات را طوری در `schema.prisma` اعمال می‌کنیم که دیتابیس به هم نریزد. یعنی فیلد جدید را با `?` اختیاری می‌کنیم.

**اقدام شما در فایل:** در مدل `Student`، فیلد `userId` را موقتاً اختیاری کنید:

Code snippet

```
model Student {
  // ...
  userId      String?            @unique  // موقتاً اختیاری شد
  user        User?              @relation(fields: [userId], references: [id])
  // ...
}
```

حالا به جای `push`، از سیستم مایگریشن استفاده کنید:

Bash

```
npx prisma migrate dev --name add_user_and_todo
```

_این کار یک پوشه به نام `prisma/migrations` می‌سازد و فایل SQL این تغییر را آنجا ذخیره می‌کند و بدون هیچ خطایی روی دیتابیس اعمال می‌شود._

#### مرحله ۲: همگام‌سازی داده‌های قبلی (Data Migration)

حالا دیتابیس شما آپدیت شده، اما دانشجوهای قبلیِ شما هنوز `User` ندارند (فیلد userId آن‌ها null است). در این مرحله، ما یک اسکریپت کوچکِ یک‌بار-مصرف (یا یک API موقت) می‌نویسیم که تمام `Student`های فعلی را پیدا کند، برای هر کدام یک `User` در دیتابیس بسازد و `userId`آن‌ها را آپدیت کند.

#### مرحله ۳: اجباری کردنِ مجددِ فیلد (Enforcement)

وقتی مطمئن شدیم که تمام دانشجوهای قدیمی حالا یک `User` متصل به خود دارند، برمی‌گردیم به `schema.prisma`، علامت‌های `?` را پاک می‌کنیم تا فیلد دوباره اجباری شود. سپس دوباره مایگریشن می‌سازیم:

Bash

```
npx prisma migrate dev --name make_userid_required
```