

در برنامه‌نویسی روش‌های مختلفی برای طراحی و نوشتن نرم‌افزار وجود دارد که به آن‌ها **پارادایم برنامه‌نویسی (Programming Paradigm)** گفته می‌شود.  
دو پارادایم بسیار رایج در جاوااسکریپت و بسیاری از زبان‌های دیگر عبارت‌اند از:

- برنامه‌نویسی تابعی (Functional Programming)
- برنامه‌نویسی شی‌گرا (Object-Oriented Programming)

درک تفاوت این دو دیدگاه کمک می‌کند بفهمیم **چرا و چه زمانی از شی‌گرایی استفاده می‌کنیم.**

---

## شی‌گرایی (Object-Oriented Programming)

در شی‌گرایی برنامه به صورت مجموعه‌ای از **اشیا (Objects)** طراحی می‌شود.

هر **شیء** ترکیبی از دو چیز است:

- **داده‌ها (Properties)**
- **رفتارها (Methods)**

به بیان ساده:

- داده‌ها وضعیت شیء را نگه می‌دارند
- متدها عملیاتی هستند که شیء می‌تواند انجام دهد

مثال دنیای واقعی:

فرض کنید می‌خواهیم یک **کاربر (User)** را در برنامه مدل کنیم.

یک کاربر ممکن است ویژگی‌های زیر را داشته باشد:

- نام
- ایمیل
- رمز عبور

و رفتارهای زیر:

- ورود به سیستم
- خروج از سیستم
- تغییر رمز

در شی‌گرایی این اطلاعات و رفتارها در قالب یک **Object** یا **Class** کنار هم قرار می‌گیرند.

مثال ساده در JavaScript:

```javascript
const user = {
  name: "Ali",
  email: "ali@example.com",

  login() {
    console.log("User logged in");
  }
};
```

در این مثال:

- `name` و `email` → داده هستند  
- `login()` → رفتار است

---

# تفاوت Functional و OOP

### 1. نگاه به داده‌ها

**Functional Programming**

در برنامه‌نویسی تابعی تمرکز روی **توابع خالص (Pure Functions)** است.  
داده‌ها معمولاً جدا از توابع قرار دارند.

```javascript
function calculateTotal(price, tax) {
  return price + tax;
}
```

در اینجا تابع فقط ورودی می‌گیرد و خروجی تولید می‌کند.

---

**OOP**

در شی‌گرایی داده و رفتار **کنار هم در یک شیء** قرار می‌گیرند.

```javascript
const cart = {
  price: 100,
  tax: 10,

  total() {
    return this.price + this.tax;
  }
};
```

---

### 2. ساختار برنامه

**Functional**

- برنامه از توابع کوچک ساخته می‌شود
- تمرکز روی تبدیل داده‌ها است
- state کمتر تغییر می‌کند

**OOP**

- برنامه از کلاس‌ها و اشیا ساخته می‌شود
- هر بخش از سیستم یک شیء است
- اشیا با هم تعامل دارند

---

### 3. مدیریت پیچیدگی

Functional:

- برای پردازش داده‌ها بسیار مناسب است
- در سیستم‌های data-heavy خوب عمل می‌کند

OOP:

- برای مدل‌سازی سیستم‌های بزرگ مناسب‌تر است
- زمانی که موجودیت‌های مختلف داریم بهتر جواب می‌دهد

مثال:

در یک سیستم فروشگاهی ممکن است این اشیا را داشته باشیم:

- User
- Product
- Order
- Payment

در OOP هرکدام می‌توانند یک **کلاس** باشند.

---

# چرا OOP مفید است؟

شی‌گرایی چند مزیت مهم دارد:

### 1. مدل‌سازی دنیای واقعی

بسیاری از سیستم‌ها دارای موجودیت هستند:

- کاربر
- سفارش
- محصول
- پیام

شی‌گرایی این موجودیت‌ها را به صورت **Object** مدل می‌کند.

---

### 2. سازماندهی بهتر کد

در پروژه‌های بزرگ اگر کدها پراکنده باشند نگهداری آن سخت می‌شود.

OOP کمک می‌کند:

- کدها در کلاس‌ها سازماندهی شوند
- هر کلاس یک مسئولیت داشته باشد

---

### 3. قابلیت استفاده مجدد

با استفاده از **کلاس‌ها** می‌توان چندین نمونه ساخت.

```javascript
class User {
  constructor(name) {
    this.name = name;
  }
}

const u1 = new User("Ali");
const u2 = new User("Sara");
```

یک کلاس → چندین **instance**

---

### 4. نگهداری و توسعه آسان‌تر

در پروژه‌های بزرگ:

- اضافه کردن ویژگی جدید
- تغییر رفتار یک بخش
- رفع باگ

در ساختار OOP ساده‌تر انجام می‌شود.

---

# چه زمانی OOP استفاده کنیم؟

شی‌گرایی زمانی بسیار مفید است که:

- سیستم **موجودیت‌های واضح** دارد (User, Order, Product)
- پروژه **بزرگ یا در حال رشد** است
- چند برنامه‌نویس روی پروژه کار می‌کنند
- نیاز به **ساختار معماری مشخص** داریم

در پروژه‌های Node.js معمولاً از OOP در این لایه‌ها استفاده می‌شود:

- Controller
- Service
- Repository
- Model

---

# چه زمانی OOP مناسب نیست؟

برای کدهای کوچک یا پردازش ساده داده‌ها، استفاده از OOP ممکن است **بیش از حد پیچیده** باشد.

در این موارد معمولاً **Functional Programming** ساده‌تر است.

مثال:

- تبدیل داده‌ها
- پردازش آرایه‌ها
- اسکریپت‌های کوچک

---

✅ نتیجه مهم:

در JavaScript می‌توان **هر دو سبک Functional و OOP** را با هم استفاده کرد.  
بیشتر پروژه‌های واقعی ترکیبی از این دو هستند.

---
### سوال‌ها
