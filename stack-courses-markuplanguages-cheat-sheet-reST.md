حتماً! در اینجا یک **چیت‌شیت (Cheat Sheet)** کاربردی و سریع برای زبان نشانه‌گذاری **reStructuredText (reST)** آورده شده است. این زبان معمولاً در مستندات پروژه‌های پایتون (مثل Sphinx) استفاده می‌شود.

### 📝 ساختار کلی و پاراگراف‌ها

| مفهوم | سینتکس | توضیح |
| :--- | :--- | :--- |
| **پاراگراف** | متن معمولی | فقط با یک خط خالی از بخش قبلی جدا شود. |
| **عنوان اصلی** | `==================` | خطی با کاراکترهای تکراری زیر عنوان (بزرگترین سطح). |
| **عنوان فرعی** | `------------------` | خطی با کاراکترهای تکراری زیر عنوان (سطح دوم). |
| **عنوان کوچکتر** | `````````````````````` | برای سطوح پایین‌تر (سطح سوم و...). |

> **نکته:** کاراکترهای زیر عنوان باید دقیقاً به اندازه طول عنوان یا بیشتر باشند.

---

### 🔠 استایل متن

| سبک | سینتکس | خروجی |
| :--- | :--- | :--- |
| **بولد (پررنگ)** | ``**متن**`` | **متن** |
| *ایتالیک (کج)* | ``*متن*`` | *متن* |
| ``مونو-اسپیس`` | ````متن```` | `متن` (فونت ثابت) |
| ``خط‌خوردگی`` | ``==متن==`` | ~~متن~~ |
| <sup>بالانویس</sup> | ``^متن^`` | متن^sup^ |
| <sub>زیرنویس</sub> | ``~متن~`` | متن~sub~ |

---

### 📋 لیست‌ها

#### لیست مرتب (شماره‌دار)
```rst
1. آیتم اول
2. آیتم دوم
   1. زیرمجموعه (با تورفتگی ۴ فاصله)
   2. زیرمجموعه دیگر
```

#### لیست نامرتب (نقطه‌ای)
```rst
- آیتم اول
- آیتم دوم
  * زیرمجموعه (با تورفتگی)
  * زیرمجموعه دیگر
```

#### لیست تعریفی (Term-Definition)
```rst
اصطلاح ۱
   تعریف این اصطلاح.

اصطلاح ۲
   تعریف این اصطلاح.
```

---

### 🔗 لینک‌ها و ارجاعات

| نوع | سینتکس | توضیح |
| :--- | :--- | :--- |
| **لینک URL** | ``http://example.com`` | به صورت خودکار تبدیل به لینک می‌شود. |
| **لینک با متن** | ``متن لینک <http://example.com>__`` | متن "متن لینک" قابل کلیک می‌شود. |
| **ارجاع داخلی** | ``متن <#internal-ref>__``<br><br>.. _internal-ref: نام ارجاع | ایجاد لینک به بخش‌های دیگر مستندات. |
| **ارجاع به صفحه** | ``Page Title <filename.rst>_`` | لینک به فایل rst دیگر. |

> **نکته:** دو آندرلاین `__` در انتهای لینک یا ارجاع، آن را به یک ارجاع "ناشناس" (anonymous reference) تبدیل می‌کند که می‌توان چندین بار از آن استفاده کرد.

---

### 🖼️ تصاویر

```rst
.. image:: /path/to/image.png
   :width: 200
   :height: 100
   :alt: متن جایگزین
```

---

### 💻 کدها و بلوک‌های کد

#### کد درون‌خطی (Inline Code)
```rst
تابع ``print()`` را صدا بزنید.
```

#### بلوک کد (Code Block)
```rst
.. code-block:: python

   def hello():
       print("Hello, World!")
```
> **نکته:** پس از `.. code-block::` زبان برنامه‌نویسی را مشخص کنید (مثلاً `python`, `bash`, `html`). خطوط کد باید با ۴ فاصله تورفتگی داشته باشند.

---

### 📦 جدول‌ها (Grid Tables)

```rst
+------------+------------+-----------+
| سرستون ۱   | سرستون ۲   | سرستون ۳   |
+============+============+===========+
| ردیف ۱، ستون ۱ | ردیف ۱، ستون ۲ | ردیف ۱، ستون ۳ |
+------------+------------+-----------+
| ردیف ۲، ستون ۱ | ردیف ۲، ستون ۲ | ردیف ۲، ستون ۳ |
+------------+------------+-----------+
```

---

### 📦 بلوک‌های نقل قول (Block Quotes)

```rst
این یک نقل قول است:

   متن نقل قول باید با ۴ فاصله تورفتگی داشته باشد.
   می‌تواند چند خطی باشد.
```

---

### ⚠️ نکات مهم و عیب‌یابی

1.  **فضای خالی:** reST به شدت به تورفتگی (Indentation) حساس است. همیشه از **فاصله (Space)** استفاده کنید، نه Tab.
2.  **خطوط خالی:** برای شروع یک بلوک جدید (مثل لیست، جدول، یا پاراگراف جدید)، حتماً یک خط خالی بگذارید.
3.  **کاراکترهای خاص:** اگر می‌خواهید کاراکترهای خاصی مثل `*` یا `` ` `` به صورت متنی نمایش داده شوند، از کاراکتر پس‌زمینه (backslash) استفاده کنید: ``\*`` یا ``\` ``.
4.  **تبدیل به HTML:** برای دیدن خروجی HTML، می‌توانید از ابزار `docutils` استفاده کنید:
    ```bash
    python -m docutils rst_example.rst --output-file=output.html
    ```

این چیت‌شیت اکثر نیازهای روزمره شما در نوشتن مستندات با reST را پوشش می‌دهد.