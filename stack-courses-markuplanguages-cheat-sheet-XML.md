# 📄 XML Cheat Sheet (راهنمای سریع XML)

### ۱. ساختار کلی (Basic Structure)
هر فایل XML باید با **Declaration** شروع شود و دارای یک **ریشه (Root)** واحد باشد.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<rootElement>
    <childElement attribute="value">
        Text content
    </childElement>
</rootElement>
```

---

### ۲. قوانین اساسی (Core Rules)
1.  **تگ‌ها باید بسته شوند:** `<br>` غلط است، `<br></br>` یا `<br />` درست است.
2.  **تگ‌ها تودرتو باشند:** `<a><b></a></b>` غلط است. باید `<a><b></b></a>` باشد.
3.  **نام‌گذاری تگ‌ها:**
    *   نباید با عدد یا حروف `xml` (با هر حالتی) شروع شوند.
    *   نباید فاصله (Space) داشته باشند (از `_` یا `-` استفاده کنید).
    *   حساس به حروف بزرگ و کوچک هستند (`<Name>` با `<name>` فرق دارد).
4.  **ویژگی‌ها (Attributes):** مقادیر ویژگی‌ها حتماً باید داخل کوتیشن `""` یا `''` باشند.
    *   درست: `<person id="123">`
    *   غلط: `<person id=123>`

---

### ۳. سینتکس‌های مهم

#### الف) تگ‌های ساده و پیچیده
```xml
<!-- تگ خالی (Self-closing) -->
<emptyTag />

<!-- تگ با محتوا -->
<message>Hello World</message>

<!-- تگ با ویژگی -->
<book id="101" type="novel">Title</book>
```

#### ب) متن‌های خاص (Special Characters)
در XML کاراکترهای `<` و `&` معنا دارند. برای نمایش آن‌ها باید از **Entity References** استفاده کنید:

| کاراکتر | معنی | کد Entity |
| :--- | :--- | :--- |
| `<` | کمتر از | `&lt;` |
| `>` | بیشتر از | `&gt;` |
| `&` | و | `&amp;` |
| `"` | کوتیشن دابل | `&quot;` |
| `'` | کوتیشن تکی | `&apos;` |

> **نکته:** برای متون طولانی یا حاوی کاراکترهای خاص، از **CDATA** استفاده کنید:
> ```xml
> <description>
>   <![CDATA[ این متن شامل < و & و " است و تغییر نمی‌کند. ]]>
> </description>
> ```

---

### ۴. کامنت‌ها (Comments)
```xml
<!-- این یک کامنت است -->
<!-- متن طولانی
     در چند خط -->
```
*توجه: کامنت‌ها نمی‌توانند داخل تگ‌ها باشند.*

---

### ۵. عناصر تکرار شونده (Repeating Elements)
معمولاً برای لیست‌ها از یک تگ والد و تگ‌های فرزند استفاده می‌شود:

```xml
<students>
    <student>
        <name>Ali</name>
        <grade>10</grade>
    </student>
    <student>
        <name>Sara</name>
        <grade>12</grade>
    </student>
</students>
```

---

### ۶. تفاوت XML با HTML
| ویژگی | XML | HTML |
| :--- | :--- | :--- |
| **هدف** | ذخیره و انتقال داده | نمایش داده |
| **تگ‌ها** | کاربر تعریف می‌کند (`<name>`) | از پیش تعریف شده (`<p>`, `<h1>`) |
| **حساسیت** | حساس به حروف بزرگ/کوچک | معمولاً حساس نیست |
| **بستن تگ‌ها** | اجباری | اختیاری در برخی مرورگرها |

---

### ۷. اعتبارسنجی (Validation)
برای اطمینان از ساختار صحیح فایل XML، از دو روش استفاده می‌شود:

1.  **DTD (Document Type Definition):** قدیمی‌تر و ساده‌تر.
    ```xml
    <!DOCTYPE note [
      <!ELEMENT note (to, from, heading, body)>
      <!ELEMENT to (#PCDATA)>
    ]>
    ```
2.  **XSD (XML Schema Definition):** مدرن‌تر، قدرتمندتر و پیشنهاد شده برای پروژه‌های جدید.

---

### ۸. نکات پایانی
*   همیشه از **UTF-8** برای انکدینگ استفاده کنید تا از نمایش صحیح حروف فارسی و دیگر زبان‌ها اطمینان حاصل شود.
*   فایل‌های XML نباید دارای **BOM** (Byte Order Mark) اضافی باشند (مگر اینکه اکسپلورر خاصی نیاز داشته باشد)، زیرا باعث خطا در پارس شدن می‌شود.
*   برای خوانایی بهتر، از **Indentation** (تورفتگی) مناسب استفاده کنید.

---
اگر نیاز به مثال خاصی یا توضیح درباره بخش‌های پیشرفته‌تر (مثل XSLT یا XPath) دارید، بفرمایید!