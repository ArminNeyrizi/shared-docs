حتماً ✅ در بخش **۰.۶ کپسوله‌سازی (Encapsulation)**، هدف این است که بفهمیم چرا و چطور اطلاعات داخل یک کلاس یا شیء باید محافظت شوند. بگذار شفاف توضیحش دهم:

---

### 💡 تعریف کلی
**کپسوله‌سازی (Encapsulation)** یکی از اصول چهارگانه برنامه‌نویسی شی‌ءگرا (OOP) است و به معنی **پنهان‌سازی جزئیات داخلی یک کلاس و کنترل دسترسی به آن‌ها از بیرون** است.

---

### 🔓 Public در مقابل 🔐 Private

| سطح دسترسی | توضیح | کاربرد |
|-------------|--------|----------|
| `public` | اعضای (متد یا متغیر) کلاس از همه‌جا قابل دسترسی‌اند | برای رفتارهایی که باید در دسترس همه باشند (مثل متدهای API کلاس) |
| `private` | فقط درون همان کلاس قابل دسترسی‌اند | برای داده‌ها و متدهای کمکی که نباید از بیرون تغییر یا دیده شوند |

در جاوااسکریپت مدرن (ES2022 به بعد)، فیلدهای خصوصی با علامت `#` تعریف می‌شوند:

```javascript
class User {
  #password; // فیلد خصوصی

  constructor(username, password) {
    this.username = username; // عمومی
    this.#password = password; // خصوصی
  }

  checkPassword(input) {
    return this.#password === input;
  }
}

const u = new User("Ali", "1234");
console.log(u.username);     // ✅ قابل دسترسی
console.log(u.#password);    // ❌ خطا - دسترسی غیرمجاز
```

---

### 🎯 چرا کپسوله‌سازی مهم است

1. **کنترل دسترسی (Access Control):**  
   فقط متدهای خاص بتوانند داده‌های حساس را بخوانند یا تغییر دهند.

2. **API سطح شیء (Object-Level API):**  
   کد بیرون نیاز ندارد بداند درون شیء چه اتفاقی می‌افتد؛ فقط از متدهای عمومی استفاده می‌کند. این همان چیزی است که رابط‌های (interface) تمیز می‌سازد.

3. **کاهش کوپلینگ (Low Coupling):**  
   وابستگی بین بخش‌های مختلف برنامه کاهش می‌یابد، چون تغییر در پیاده‌سازی داخلی یک کلاس نیازی به تغییر در سایر بخش‌ها ندارد.

---

### 🧩 مثال از اهمیت آن

فرض کن کلاس `BankAccount` داری. اگر موجودی (`balance`) عمومی باشد، هر کسی می‌تواند مستقیماً آن را تغییر دهد و پول اضافه کند!

```javascript
class BankAccount {
  #balance = 0;

  deposit(amount) {
    if (amount > 0) this.#balance += amount;
  }

  getBalance() {
    return this.#balance;
  }
}
```

در این حالت فقط از طریق **متدهای کنترل‌شده** می‌شود موجودی را تغییر داد.
