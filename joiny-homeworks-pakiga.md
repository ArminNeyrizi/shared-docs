
# 🌍  پلتفرم گزارش آلودگی مردمی

## توضیح خلاصه

این یک **اپلیکیشن وب** است که به مردم اجازه می‌دهد آلودگی های محیطشان را **گزارش** دهند.

---

## 🎯 هدف اصلی

```
👤 شهروند عادی          →        🏭 محیط زیست/سازمان مربوطه
     ↓                         ↓
  ثبت گزارش          ←→       مشاهده و پیگیری
  (عکس + موقعیت)              (بررسی و اقدام)
```

---

## ⭐ قابلیت‌های کلیدی

| قابلیت | توضیح |
|--------|--------|
| 📍 ثبت گزارش | کاربر مکان آلودگی را روی نقشه مشخص می‌کند |
| 📷 آپلود عکس | همراه با توضیحات ارسال می‌شود |
| 📊 مشاهده آمار | تعداد گزارش‌ها، وضعیت حل شده و... |
| 🗺️ نقشه تعاملی | نمایش آلودگی‌ها روی نقشه |
| ✅ پیگیری وضعیت | هر گزارش وضعیت دارد (در انتظار/حل شده/...) |

---

## 🏗️ ساختار پروژه 

```
📦 پروژه فعلی شما:

┌─────────────────────────────────────────────┐
│     Frontend (Vite + React + TypeScript)    │
├─────────────────────────────────────────────┤
│     src/components/                         │
│     ├──  ui/             ← ۵۰+ کامپوننت UI  │
│     │                     (shadcn/ui)       │
│     ├── Header.tsx       ← هدر سایت         │
│     ├── HeroSection.tsx   ← بخش معرفی       │
│     ├── ReportForm.tsx    ← فرم گزارش‌دهی    │
│     ├── StatsSection.tsx  ← آمار و ارقام    │
│     └── ...                                 │
│                                             │
│   src/pages/                                │
│     ├── Index.tsx       ← صفحه اصلی         │
│     └── NotFound.tsx    ← صفحه ۴۰۴          │
│                                             │
│    src/integrations/                        │
│     └──    supabase/   ← اتصال به دیتابیس   │
│         ├──  client.ts   ← تنظیم کلاینت      │
│         └──  types.ts   ← تایپ‌های دیتابیس   │
└─────────────────────────────────────────────┘
```

---

## 🛠️ تکنولوژی‌های استفاده شده

```
🔹 Core:         Vite + React 18 + TypeScript
🔹 UI:           Tailwind CSS + shadcn/ui (Radix UI)
🔹 Backend:      Supabase (PostgreSQL + Auth)
🔹 State:        TanStack Query
🔹 Icons:        Lucide React
🔹 Build:        Bun (بجای npm)
```

---

## 📋 صفحات اصلی پروژه

```
/ (Index)          ← صفحه اصلی
  ├── Header       ← هدر با لوگو و ناوبری
  ├── HeroSection  ← بنر معرفی اصلی
  ├── StatsSection ← آمار کلی (گزارش‌ها)
  ├── PollutionCategories ← دسته‌بندی آلودگی‌ها
  ├── RecentReports ← جدیدترین گزارش‌ها
  ├── ReportForm   ← فرم ثبت گزارش جدید
  ├── SuccessStories ← نمونه موفقیت‌ها
  └── Footer       ← فوتر

/* (NotFound)      ← صفحه ۴۰۴
```

---
