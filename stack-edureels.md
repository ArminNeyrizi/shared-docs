## ۱. معرفی و اهداف پروژه

پلتفرم **EduReels** یک شبکه اجتماعی-آموزشی وب‌محور است که مدل تعاملی و جذاب فید ویدیوهای کوتاه (ریلز اینستاگرام) را با ساختار ساختاریافته‌ی یادگیری (پلی‌لیست‌ها و دوره‌های جامع) ترکیب می‌کند.

- **هدف اصلی:** کاهش اصطکاک یادگیری، تبدیل مفاهیم پیچیده به میکرو-درس‌های زیر ۶۰ ثانیه به عنوان قلاب (Hook)، و هدایت مخاطب به سمت دوره‌های عمیق‌تر، افزایش جذابیت درس خواندن.
- **مخاطبین هدف:** مدرسین (تولیدکنندگان محتوا و اساتید) و یادگیرندگان (دانش‌آموزان و جویندگان مهارت).

## ۲. معماری کلی سیستم (System Architecture)

سیستم به صورت **Monorepo** یا سرویس‌های مجزا با معماری سه‌لایه طراحی می‌شود: (بعدا قرار است برای هر پلتفرم با زبان همان پلتفرم فرانت اپلیکیشن به صورت Native نوشته شود، اولویت شروع با Web است.)

- **فرانت‌اند (Client):** Next.js 14+ (App Router) + Tailwind CSS + Zustand (مدیریت استیت ویدیو و پلیر).
- **بک‌اند (API Server):** **NestJS** (TypeScript) با معماری ماژولار (Modular Architecture).
- **دیتابیس (Database):** PostgreSQL همراه با ORM قدرتمند **Prisma**.
- **ذخیره‌سازی فایل (Storage):** AWS S3 یا فضای ابری سازگار با S3 (برای نگهداری ویدیوها و تصاویر پروفایل).

## ۳. مدل داده و ساختار دیتابیس (Database Schema)

برای پیاده‌سازی این سیستم در Prisma، مدل‌های اصلی به شرح زیر خواهند بود:


1. **User (کاربران / اساتید):**
    - `id`, `email`, `passwordHash`, `fullName`, `bio`, `avatarUrl`, `role` (STUDENT, INSTRUCTOR, ADMIN), `createdAt`

2. **Course (دوره‌ها):**
    - `id`, `title`, `description`, `instructorId` (FK), `thumbnailUrl`, `price`, `createdAt`

3. **Playlist (پلی‌لیست‌ها / سری‌های آموزشی):**
    - `id`, `title`, `description`, `courseId` (FK - اختیاری، هر پلی‌لیست می‌تواند مستقل یا داخل دوره باشد), `instructorId` (FK), `createdAt`
    
4. **Reel (ریلزها / میکرو‌درس‌ها):**
    - `id`, `title`, `videoUrl`, `duration`, `orderIndex` (ترتیب در پلی‌لیست), `playlistId` (FK), `instructorId` (FK), `viewsCount`, `likesCount`, `createdAt`
    
5. **Interaction (تعاملات - لایک و کامنت):**
    - `Like` (userId, reelId), `Comment` (id, userId, reelId, content, createdAt)
        
          
## ۴. معماری بک‌اند با NestJS (ماژول‌ها و ساختار)

پروژه NestJS بر اساس ماژول‌های مجزا (Domain-driven / Feature-based) به صورت زیر ساختاردهی می‌شود:

  

Plaintext

```
src/
├── main.ts
├── app.module.ts
├── common/             # Interceptors, Guards, Decorators (JWT Auth)
├── prisma/             # Prisma Service & Client
├── auth/               # احراز هویت (JWT, Login/Register)
├── users/              # مدیریت پروفایل کاربران و اساتید
├── courses/            # مدیریت دوره‌های آموزشی
├── playlists/          # مدیریت پلی‌لیست‌ها و زنجیره‌های ویدیویی
├── reels/              # مدیریت ریلزها، آپلود ویدیو، فید عمودی
└── interactions/       # لایک، کامنت و آمار بازدید
```

### شرح وظایف ماژول‌های کلیدی NestJS:
- **`ReelsModule`:** مسئول دریافت ویدیوها، اتصال به سرویس Storage برای آپلود امن، مدیریت ترتیب ویدیوها در پلی‌لیست، و ارائه فید عمودی (Pagination بر اساس اسکرول).
- **`PlaylistsModule`:** مدیریت ارتباط بین ریلزها و پلی‌لیست‌ها (چند ویدیو به عنوان اجزای یک زنجیره آموزشی).
- **`CoursesModule`:** تجمیع پلی‌لیست‌ها در قالب یک دوره جامع.

## ۵. جریان تجربه کاربری (User Flow & Features) در نسخه وب

1. **فید ریلز (Reels Feed):**  
    - کاربر وارد صفحه اصلی وب‌اپلیکیشن می‌شود.
    - ویدیوها به صورت تمام‌صفحه یا کارت‌های متمرکز با قابلیت اسکرول عمودی (`Scroll Snap`) پخش می‌شوند.
    - در سمت راست ویدیو: دکمه لایک، تعداد کامنت، دکمه ذخیره و **یک دکمه ویژه هدایت به پلی‌لیست/دوره مربوطه**.
2. **پنل مدرس (Instructor Dashboard):** 
    - مدرس ویدیو (ریلز) را آپلود می‌کند، عنوان وتگ می‌زند.
    - ریلزهای آپلود شده را کشیده و رها می‌کند (Drag & Drop) تا یک **پلی‌لیست** بسازد.
    - پلی‌لیست‌ها را درون یک **دوره** سازمان‌دهی می‌کند.
