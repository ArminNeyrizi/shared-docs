### ۱. تعریف
**JSON** مخفف **J**ava**S**cript **O**bject **N**otation است.
- یک فرمت متنی سبک برای ذخیره و تبادل داده‌هاست.
- مستقل از زبان است (توسط اکثر زبان‌های برنامه‌نویسی پشتیبانی می‌شود).
- بر پایه سینتکس اشیاء جاوااسکریپت است، اما در سمت سرور (Backend) هم استفاده می‌شود.

---

### ۲. ساختار اصلی
JSON دو ساختار اصلی دارد:

#### الف) مجموعه‌ای از جفت‌های «نام/مقدار» (Key/Value Pairs)
شبیه به **Object** در جاوااسکریپت یا **Dictionary** در پایتون.
```json
{
  "نام": "علی",
  "سن": ۲۵,
  "شهر": "تهران"
}
```

#### ب) آرایه‌ای از مقادیر (Ordered List)
شبیه به **Array** در جاوااسکریپت یا **List** در پایتون.
```json
[
  "سیب",
  "موز",
  "پرتقال"
]
```

---

### ۳. انواع داده‌های پشتیبانی شده
JSON فقط ۶ نوع داده را پشتیبانی می‌کند:

| نوع داده | مثال | توضیح |
| :--- | :--- | :--- |
| **String** | `"Hello"` | حتماً باید داخل **دابل کوتیشن** `""` باشد. |
| **Number** | `100`, `3.14` | اعداد صحیح یا اعشاری (بدون کوتیشن). |
| **Boolean** | `true`, `false` | مقادیر منطقی (حرف کوچک). |
| **Null** | `null` | نشان‌دهنده خالی بودن مقدار. |
| **Object** | `{...}` | یک بلوک داده‌ای ساختاریافته. |
| **Array** | `[...]` | لیستی از مقادیر. |

> ⚠️ **نکته مهم:** `undefined`، `function` و `Symbol` در JSON **مجاز نیستند**.

---

### ۴. قوانین طلایی (Syntax Rules)
1.  داده‌ها در جفت‌های `نام: مقدار` هستند.
2.  نام‌ها (Keys) باید حتماً داخل **دابل کوتیشن** `""` باشند.
    *   ✅ درست: `"age": "20"`
    *   ❌ غلط: `age: 20`
3.  مقادیر (Values) می‌توانند هر نوع داده‌ای از ۶ نوع بالا باشند.
4.  داده‌ها با **ویرگول** `,` از هم جدا می‌شوند (به جز آخرین آیتم).
5.  از تک‌کوتیشن `'` برای دور مقادیر استفاده **نکنید**.
6.  از کامنت (`//` یا `/* */`) در JSON استاندارد **اجتناب کنید** (چون معتبر نیست).

---

### ۵. مثال ترکیبی (Nested JSON)
ترکیب Object و Array برای نمایش داده‌های پیچیده:

```json
{
  "شرکت": "فناوری نوین",
  "تعداد_کارمندان": ۵۰,
  "مدیران": [
    {
      "id": 1,
      "نام": "سارا",
      "سمت": "مدیر فنی"
    },
    {
      "id": 2,
      "نام": "رضا",
      "سمت": "مدیر محصول"
    }
  ],
  "فعال": true,
  "گواهی_امنیت": null
}
```

---

### ۶. تبدیل JSON در زبان‌های مختلف

| زبان | تبدیل به JSON | تبدیل از JSON |
| :--- | :--- | :--- |
| **JavaScript** | `JSON.stringify(obj)` | `JSON.parse(string)` |
| **Python** | `json.dumps(obj)` | `json.loads(string)` |
| **C#** | `JsonConvert.SerializeObject()` | `JsonConvert.DeserializeObject()` |
| **Java** | `new JSONObject(obj)` | `new JSONObject(string)` |
| **PHP** | `json_encode($data)` | `json_decode($data, true)` |

---

### ۷. اعتبارسنجی (Validation)
همیشه داده‌های JSON خود را اعتبارسنجی کنید تا از خطاهای پارسینگ جلوگیری شود.
- سایت‌های آنلاین: [jsonlint.com](https://jsonlint.com)
- فرمت‌دهی خودکار (Pretty Print): اکثر ویرایشگرهای کد (VS Code, Sublime) با زدن `Ctrl+Shift+P` و تایپ `Format Document` این کار را انجام می‌دهند.

---

### ۸. تفاوت JSON و JavaScript Object
| ویژگی | JSON | JS Object |
| :--- | :--- | :--- |
| **کوتیشن‌ها** | keys و string values حتماً باید `""` باشند. | keys اختیاری هستند (اگر نام معتبر باشند). |
| **توابع** | پشتیبانی نمی‌کند. | پشتیبانی می‌کند. |
| **کامنت** | ندارد. | دارد. |
| **نوع داده** | فقط داده خام. | می‌تواند کد و منطق داشته باشد. |

---

✅ **نکته پایانی:** اگر داده‌ای را از سرور دریافت کردید و خطای `Unexpected token` دیدید، احتمالاً مشکل از کوتیشن‌ها، ویرگول اضافه/کم، یا کاراکترهای غیرمجاز است.