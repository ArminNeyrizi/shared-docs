در اینجا یک **چیت‌شیت (Cheat Sheet)** کاربردی و خلاصه از تگ‌های اصلی HTML آورده شده است. این راهنما برای یادگیری سریع یا مرور کدها بسیار مفید است.

### ۱. ساختار پایه یک فایل HTML
```html
<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عنوان صفحه</title>
</head>
<body>
    <!-- محتوای صفحه در اینجا قرار می‌گیرد -->
</body>
</html>
```

### ۲. متن‌ها و تیترها
| تگ | توضیح |
| :--- | :--- |
| `<h1>` تا `<h6>` | تیترها (h1 بزرگ‌ترین و h6 کوچک‌ترین) |
| `<p>` | پاراگراف متن |
| `<br>` | شکستن خط (Line Break) |
| `<hr>` | خط افقی جداکننده |
| `<strong>` | متن پررنگ (Bold) - با اهمیت سمنتیک |
| `<em>` | متن کج (Italic) - با تاکید |
| `<span>` | کانتینر خطی (Inline) برای استایل‌دهی |

### ۳. لینک‌ها و تصاویر
```html
<!-- لینک -->
<a href="https://example.com" target="_blank">متن لینک</a>

<!-- تصویر -->
<img src="image.jpg" alt="توضیح تصویر" width="300" height="200">
```

### ۴. لیست‌ها
```html
<!-- لیست مرتب (شماره‌دار) -->
<ol>
  <li>آیتم اول</li>
  <li>آیتم دوم</li>
</ol>

<!-- لیست نامرتب (بولت‌دار) -->
<ul>
  <li>آیتم اول</li>
  <li>آیتم دوم</li>
</ul>
```

### ۵. جداول (Tables)
```html
<table border="1">
  <thead>
    <tr>
      <th>سرستون ۱</th>
      <th>سرستون ۲</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>سلول ۱</td>
      <td>سلول ۲</td>
    </tr>
  </tbody>
</table>
```

### ۶. فرم‌ها (Forms)
```html
<form action="/submit-data" method="POST">
  <!-- ورودی متن -->
  <label for="fname">نام:</label>
  <input type="text" id="fname" name="fname" placeholder="نام خود را وارد کنید">
  
  <!-- دکمه ارسال -->
  <button type="submit">ارسال</button>
  
  <!-- چک‌باکس -->
  <input type="checkbox" id="agree" name="agree">
  <label for="agree">موافقت با قوانین</label>
</form>
```

### ۷. کانتینرهای معنایی (Semantic Tags)
این تگ‌ها برای ساختاردهی بهتر صفحه و سئو (SEO) استفاده می‌شوند:
*   `<header>`: سربرگ سایت یا بخش
*   `<nav>`: منوی ناوبری
*   `<main>`: محتوای اصلی صفحه
*   `<section>`: بخش‌بندی محتوا
*   `<article>`: محتوای مستقل (مثل یک پست بلاگ)
*   `<aside>`: محتوای جانبی (سایدبار)
*   `<footer>`: پابرگ سایت یا بخش

### ۸. اتریبیوت‌های مهم (Attributes)
*   `id`: شناسه منحصر‌به‌فرد برای یک المان (مثلاً برای CSS یا JS).
*   `class`: کلاس برای استایل‌دهی گروهی المان‌ها.
*   `style`: استایل‌دهی داخلی (Inline).
*   `title`: متن راهنما که با هاور موس نمایش داده می‌شود.
*   `disabled`: غیرفعال کردن یک ورودی.

### ۹. کاراکترهای خاص (Entities)
گاهی اوقات باید کاراکترهایی را نمایش دهید که در HTML رزرو شده‌اند:
*   `&nbsp;` : فاصله غیرقابل شکستن
*   `&lt;` : علامت کمتر از (<)
*   `&gt;` : علامت بیشتر از (>)
*   `&amp;` : علامت آندرلاین (&)
*   `&quot;` : علامت نقل قول (")

---
**نکته:** همیشه سعی کنید از تگ‌های معنایی (Semantic) استفاده کنید تا کد شما خوانا‌تر و برای موتورهای جستجو بهینه‌تر باشد.