زبان‌های نشانه‌گذاری (Markup Languages) ابزارهایی هستند که برای افزودن ساختار، فرمت یا معنا به داده‌های خام استفاده می‌شوند. بر اساس هدف و کاربردشان، می‌توان آن‌ها را به چند دسته اصلی تقسیم کرد. در ادامه مهم‌ترین انواع آن‌ها را بررسی می‌کنیم:

### ۱. زبان‌های نشانه‌گذاری عمومی (General-Purpose Markup Languages)
این زبان‌ها برای نمایش محتوا در صفحات وب و اینترنت استفاده می‌شوند.

*   **HTML (HyperText Markup Language):**
    *   **کاربرد:** استاندارد اصلی برای ایجاد صفحات وب.
    *   **ویژگی:** ساختار صفحات را تعریف می‌کند (مانند تیترها، پاراگراف‌ها، تصاویر و لینک‌ها) اما برای استایل‌دهی یا منطق برنامه‌نویسی مناسب نیست.
*   **XHTML (eXtensible HyperText Markup Language):**
    *   **کاربرد:** نسخه‌ی سخت‌گیرانه‌تر و تمیزتر HTML که بر اساس XML نوشته شده است.
    *   **ویژگی:** تگ‌ها باید بسته شوند و حروف کوچک باشند. امروزه کمتر استفاده می‌شود و HTML5 جایگزین آن شده است.

### ۲. زبان‌های نشانه‌گذاری ساختاریافته (Structured Data Markup)
این زبان‌ها برای ذخیره و انتقال داده‌های ساختاریافته بین سیستم‌ها استفاده می‌شوند.

*   **XML (eXtensible Markup Language):**
    *   **کاربرد:** ذخیره و انتقال داده‌ها در برنامه‌های مختلف، پیکربندی سیستم‌ها، و وب‌سرویس‌ها (مانند SOAP).
    *   **ویژگی:** تگ‌ها توسط کاربر تعریف می‌شوند (برخلاف HTML که تگ‌های از پیش تعیین شده دارد). بسیار انعطاف‌پذیر و قدرتمند است.
*   **JSON (JavaScript Object Notation):**
    *   **کاربرد:** اگرچه بیشتر یک فرمت داده است تا یک زبان نشانه‌گذاری کلاسیک، اما اغلب به عنوان جایگزین سبک‌تر XML برای تبادل داده در وب (APIها) استفاده می‌شود.
    *   **ویژگی:** خوانایی بالا، سبک و سریع برای پردازش توسط جاوااسکریپت.

### ۳. زبان‌های نشانه‌گذاری ساده (Simple Markup Languages)
این زبان‌ها برای فرمت‌دهی سریع متن بدون نیاز به پیچیدگی HTML یا XML طراحی شده‌اند.

*   **Markdown:**
    *   **کاربرد:** نوشتن اسناد، مستندات فنی، پست‌های بلاگ و README در گیت‌هاب.
    *   **ویژگی:** سینتکس بسیار ساده (مثلاً `#` برای تیتر، `**` برای بولد). متن‌های Markdown به HTML تبدیل می‌شوند.
*   **reStructuredText (reST):**
    *   **کاربرد:** بیشتر در مستندات پروژه‌های پایتون و Sphinx استفاده می‌شود.
    *   **ویژگی:** قدرتمندتر از Markdown در موارد پیچیده‌تر مستندسازی.
*   **AsciiDoc:**
    *   **کاربرد:** مستندسازی حرفه‌ای و تولید خروجی‌های مختلف (PDF، HTML).
    *   **ویژگی:** قابلیت‌های پیشرفته‌تر نسبت به Markdown.

### ۴. زبان‌های نشانه‌گذاری برای فرمت‌دهی خاص
*   **LaTeX:**
    *   **کاربرد:** نوشتن مقالات علمی، پایان‌نامه‌ها و اسناد ریاضی.
    *   **ویژگی:** کنترل بسیار دقیق بر چیدمان، فرمول‌های ریاضی و رفرنس‌دهی. خروجی نهایی معمولاً PDF است.
*   **YAML (YAML Ain't Markup Language):**
    *   **کاربرد:** پیکربندی فایل‌ها (مثل Docker، Kubernetes، Ansible) و ذخیره داده‌های ساختاریافته.
    *   **ویژگی:** خوانایی بالا برای انسان، استفاده از تورفتگی (Indentation) برای ساختار.

### ۵. زبان‌های نشانه‌گذاری گرافیکی و چندرسانه‌ای
*   **SVG (Scalable Vector Graphics):**
    *   **کاربرد:** نمایش تصاویر برداری در وب.
    *   **ویژگی:** یک فایل XML است که شکل‌های گرافیکی را تعریف می‌کند. کیفیت تصویر با زوم کردن کاهش نمی‌یابد.

---

### جدول مقایسه سریع

| زبان | کاربرد اصلی | ساختار | پیچیدگی |
|------|-------------|--------|---------|
| **HTML** | نمایش صفحات وب | تگ‌های از پیش تعریف شده | متوسط |
| **XML** | تبادل داده و پیکربندی | تگ‌های سفارشی | بالا |
| **JSON** | تبادل داده در وب (API) | ساختار کلید-مقدار | کم |
| **Markdown** | مستندسازی ساده | سینتکس سبک | کم |
| **YAML** | پیکربندی سیستم‌ها | تورفتگی | کم |
| **LaTeX** | اسناد علمی و ریاضی | دستورات خاص | بالا |
