### Computer & IT Encyclopedia — با آموزش کاربردی هر اصطلاح

> یک مرجع آموزشی کامل برای حدود ۲۰۰ اصطلاح پایه‌ای دنیای کامپیوتر، شبکه، برنامه‌نویسی، امنیت، هوش مصنوعی و... — به زبان ساده، با مثال‌های عملی.

---

## 📖 راهنمای استفاده از دانشنامه

هر اصطلاح با سه بخش ثابت توضیح داده شده است:

| بخش | کاربرد |
|---|---|
| **تعریف** | توضیح کامل و دقیق مفهوم |
| **آموزش و کاربرد** | نحوه استفاده عملی، مثال واقعی و نکته حفظی |
| **تعریف کوتاه** | یک جمله کوتاه برای مرور و حفظ کردن |

💡 **پیشنهاد مطالعه:** روزانه یک فصل را بخوانید، «تعریف کوتاه» هر اصطلاح را برای خودتان با صدای بلند تکرار کنید و در پایان کتاب، به بخش «جداول مقایسه‌ای» و «خودآزمایی» برگردید.

---

## 📚 فهرست فصل‌ها

1. [فصل ۱ — مبانی کامپیوتر](#chapter1)
2. [فصل ۲ — حافظه و ذخیره‌سازی](#chapter2)
3. [فصل ۳ — سیستم‌عامل](#chapter3)
4. [فصل ۴ — فایل و پوشه](#chapter4)
5. [فصل ۵ — شبکه](#chapter5)
6. [فصل ۶ — وب و اینترنت](#chapter6)
7. [فصل ۷ — برنامه‌نویسی](#chapter7)
8. [فصل ۸ — پایگاه داده](#chapter8)
9. [فصل ۹ — امنیت](#chapter9)
10. [فصل ۱۰ — رایانش ابری](#chapter10)
11. [فصل ۱۱ — هوش مصنوعی](#chapter11)
12. [فصل ۱۲ — گرافیک و چندرسانه‌ای](#chapter12)
13. [فصل ۱۳ — اصطلاحات عمومی نرم‌افزار](#chapter13)
14. [پیوست الف — جداول مقایسه‌ای](#appendix)
15. [پیوست ب — خودآزمایی](#quiz)

---

<a id="chapter1"></a>
# فصل ۱ | مبانی کامپیوتر 🖥️

در این فصل با اجزای اصلی تشکیل‌دهنده‌ی یک کامپیوتر آشنا می‌شویم: از قطعات فیزیکی (سخت‌افزار) تا برنامه‌ها (نرم‌افزار) و مفاهیمی مثل بیت، بایت و پردازنده. این مفاهیم، الفبای دنیای دیجیتال هستند.

---

### ۱.۱) Computer — کامپیوتر / رایانه
- **تعریف:** دستگاهی الکترونیکی که داده‌ها را دریافت (Input)، پردازش (Processing)، ذخیره (Storage) و در نهایت نتیجه را به صورت خروجی (Output) ارائه می‌دهد. از گوشی موبایل و لپ‌تاپ تا سرورها و ابرکامپیوترها، همه کامپیوتر محسوب می‌شوند.
- **آموزش و کاربرد:** مدل کلاسیک کار هر کامپیوتر چرخه‌ی **ورودی ← پردازش ← خروجی (IPO)** است. مثلاً وقتی با کیبورد تایپ می‌کنید، حروف «ورودی» هستند، پردازنده آن‌ها را «پردازش» می‌کند و متن روی مانیتور «خروجی» است.
- **تعریف کوتاه:** ماشینی که داده می‌گیرد، پردازش می‌کند و نتیجه می‌دهد.

### ۱.۲) Hardware — سخت‌افزار
- **تعریف:** تمام اجزای فیزیکی و قابل لمس کامپیوتر؛ مانند مانیتور، کیبورد، پردازنده، رم و هارددیسک.
- **آموزش و کاربرد:** روش تشخیص سخت‌افزار ساده است: هر چیزی از کامپیوتر که بتوانید با دست لمسش کنید، سخت‌افزار است. سخت‌افزار به‌تنهایی کاربردی ندارد و بدون نرم‌افزار فقط یک مشت قطعه بی‌جان است.
- **تعریف کوتاه:** قطعات فیزیکی و لمسی کامپیوتر.

### ۱.۳) Software — نرم‌افزار
- **تعریف:** مجموعه برنامه‌ها، دستورها و داده‌هایی که به سخت‌افزار می‌گویند چه کاری انجام دهد؛ مانند سیستم‌عامل، مرورگر، بازی‌ها و اپلیکیشن‌ها.
- **آموزش و کاربرد:** نرم‌افزارها دو دسته‌ی اصلی دارند: **نرم‌افزارهای سیستمی** (مثل ویندوز) که خودِ کامپیوتر را مدیریت می‌کنند و **نرم‌افزارهای کاربردی** (مثل Word و Photoshop) که برای انجام کارهای کاربر ساخته شده‌اند.
- **تعریف کوتاه:** برنامه‌ها و دستورالعمل‌های غیرلمسی کامپیوتر.

### ۱.۴) Firmware — میان‌افزار / سفت‌افزار
- **تعریف:** نرم‌افزاری که به صورت دائمی روی حافظه‌ی یک قطعه‌ی سخت‌افزاری ذخیره شده و همان قطعه را کنترل می‌کند؛ در واقع حلقه‌ی اتصال بین سخت‌افزار و نرم‌افزار است.
- **آموزش و کاربرد:** Firmware در روتر، مادربرد (BIOS/UEFI)، پرینتر، تلویزیون و حتی ماوس وجود دارد. وقتی شرکت سازنده «آپدیت Firmware» منتشر می‌کند، یعنی برنامه‌ی داخلی خودِ دستگاه به‌روز می‌شود، نه برنامه‌ای روی ویندوز.
- **تعریف کوتاه:** نرم‌افزاری که داخل سخت‌افزار تعبیه شده و آن را کنترل می‌کند.

### ۱.۵) Data — داده
- **تعریف:** واقعیت‌ها، ارقام و نشانه‌های خام و پردازش‌نشده؛ مانند عددها، متن‌ها، صداها و تصویرهایی که به‌خودی‌خود معنای مشخصی ندارند.
- **آموزش و کاربرد:** «۱۷، ۲۵، ۳۲» به‌تنهایی فقط داده است؛ اما اگر بگوییم این‌ها دمای هوای سه شهر است، به اطلاعات تبدیل می‌شود. داده سوخت کامپیوتر است.
- **تعریف کوتاه:** اطلاعات خام و پردازش‌نشده.

### ۱.۶) Processing — پردازش
- **تعریف:** انجام محاسبات و عملیات روی داده‌ها برای تولید نتیجه؛ این کار عمدتاً بر عهده‌ی پردازنده (CPU) است.
- **آموزش و کاربرد:** هر کاری که کامپیوتر انجام می‌دهد — از جمع دو عدد تا رندر ویدیو — نوعی پردازش است. سرعت «پردازش» است که مشخص می‌کند یک کامپیوتر چقدر سریع به نظر می‌رسد.
- **تعریف کوتاه:** انجام عملیات و محاسبات روی داده‌ها.

### ۱.۷) Information — اطلاعات
- **تعریف:** داده‌هایی که پردازش، دسته‌بندی و معنادار شده‌اند و برای تصمیم‌گیری قابل استفاده‌اند.
- **آموزش و کاربرد:** زنجیره‌ی مهم را حفظ کنید: **Data (داده) ← Processing (پردازش) ← Information (اطلاعات)**. میانگین نمرات کلاس «اطلاعات» است، چون از پردازش داده‌های خام (نمره‌ی هر دانش‌آموز) به دست آمده.
- **تعریف کوتاه:** داده‌ی پردازش‌شده و معنادار.

### ۱.۸) Binary — دودویی / باینری
- **تعریف:** سیستم عددی مبنای دو که فقط از رقم‌های **۰ و ۱** استفاده می‌کند؛ زبان مادری تمام کامپیوترها، چون قطعات الکترونیکی فقط دو حالت «روشن/خاموش» دارند.
- **آموزش و کاربرد:** هر نوشته، عکس و آهنگ در کامپیوتر در نهایت به رشته‌ای از صفر و یک تبدیل می‌شود. مثال: عدد ۵ در باینری برابر ۱۰۱ است (۱×۴ + ۰×۲ + ۱×۱). فرمت فایل‌های اجرایی را نیز «Binary» می‌نامند.
- **تعریف کوتاه:** زبان صفر و یکِ کامپیوتر.

### ۱.۹) Bit — بیت
- **تعریف:** کوچک‌ترین واحد داده در کامپیوتر که فقط یکی از دو مقدار ۰ یا ۱ را دارد.
- **آموزش و کاربرد:** دقت کنید: سرعت اینترنت معمولاً با **بیت بر ثانیه (Mbps)** و حجم فایل‌ها با **بایت (MB)** بیان می‌شود. پس اینترنت «۱۰۰ مگابیتی» حدود ۱۲٫۵ مگابایت در ثانیه دانلود می‌کند (تقسیم بر ۸).
- **تعریف کوتاه:** کوچک‌ترین واحد اطلاعات (۰ یا ۱).

### ۱.۱۰) Byte — بایت
- **تعریف:** واحد اصلی اندازه‌گیری حافظه و داده که از **۸ بیت** تشکیل شده است.
- **آموزش و کاربرد:** یک کاراکتر انگلیسی تقریباً ۱ بایت جا می‌گیرد. واحدهای بزرگ‌تر را حفظ کنید: کیلوبایت (KB)، مگابایت (MB)، گیگابایت (GB) و ترابایت (TB) که هر کدام ۱۰۲۴ برابر قبلی هستند. یک آهنگ معمولی ≈ ۵ مگابایت و یک فیلم ≈ چند گیگابایت است.
- **تعریف کوتاه:** ۸ بیت = ۱ بایت؛ واحد اصلی اندازه‌گیری حافظه.

### ۱.۱۱) CPU — پردازنده‌ی مرکزی
- **تعریف:** مغز کامپیوتر؛ قطعه‌ای که دستورهای برنامه‌ها را اجرا و محاسبات را انجام می‌دهد و سایر قطعات را کنترل می‌کند.
- **آموزش و کاربرد:** پردازنده‌های معروف: Intel (Core i3/i5/i7/i9) و AMD (Ryzen). مشخصات مهم هنگام خرید: **تعداد هسته (Core)** برای کارهای هم‌زمان و **فرکانس (GHz)** برای سرعت هر هسته. برای کارهای روزمره i5/Ryzen 5 کافی است.
- **تعریف کوتاه:** مغز و موتور اصلی کامپیوتر.

### ۱.۱۲) GPU — پردازنده‌ی گرافیکی
- **تعریف:** پردازنده‌ای تخصصی برای پردازش تصویر، ویدیو و محاسبات موازی؛ برخلاف CPU که چند هسته‌ی قدرتمند دارد، GPU هزاران هسته‌ی کوچک برای انجام هم‌زمان عملیات مشابه دارد.
- **آموزش و کاربرد:** برای بازی‌های سنگین، تدوین ویدیو، طراحی سه‌بعدی و آموزش مدل‌های هوش مصنوعی، GPU (کارت گرافیک) از CPU مهم‌تر است. NVIDIA، AMD و Intel سازندگان اصلی‌اند. GPU دو نوع است: **یکپارچه (Integrated)** داخل پردازنده و **مجزا (Dedicated)**.
- **تعریف کوتاه:** پردازنده‌ی متخصص گرافیک و محاسبات موازی.

### ۱.۱۳) Hard (Hard Disk) — هارد / هارددیسک
- **تعریف:** در اصطلاح رایج، منظور حافظه‌ی دائمی کامپیوتر (HDD یا SSD) است؛ جایی که سیستم‌عامل، برنامه‌ها و فایل‌های شما حتی بعد از خاموشی روی آن می‌مانند.
- **آموزش و کاربرد:** «هارد» در گفتگوی روزمره معمولاً به هر نوع دیسک داخلی گفته می‌شود، اما از نظر فنی HDD (دیسک مکانیکی) با SSD (حافظه فلش) تفاوت دارند — در فصل ۲ کامل توضیح داده‌ایم. ظرفیت‌های رایج امروزی از ۲۵۶ گیگابایت تا چند ترابایت است.
- **تعریف کوتاه:** حافظه‌ی دائمی کامپیوتر برای نگهداری فایل‌ها.

### ۱.۱۴) RAM — حافظه‌ی موقت / رم
- **تعریف:** حافظه‌ی دسترسی تصادفی (Random Access Memory)؛ فضای کاری سریع و **موقت** که برنامه‌های در حال اجرا و داده‌های لازم CPU در آن قرار می‌گیرند و با خاموش شدن کامپیوتر پاک می‌شود.
- **آموزش و کاربرد:** RAM را مثل «میز کار» تصور کنید و هارد را مثل «کشو». هرچه میز بزرگ‌تر، هم‌زمان کارهای بیشتری روی آن باز می‌شود. وقتی کامپیوتر کند می‌شود و برنامه‌ها هنگ می‌کنند، یکی از دلایل اصلی پر شدن رم است. برای کارهای معمولی ۸ تا ۱۶ گیگابایت مناسب است.
- **تعریف کوتاه:** حافظه‌ی موقت و سریعی که با خاموشی پاک می‌شود.

### ۱.۱۵) Boot — بوت / راه‌اندازی
- **تعریف:** فرایند روشن شدن و آماده‌سازی کامپیوتر؛ از لحظه‌ی زدن دکمه‌ی پاور تا بارگذاری کامل سیستم‌عامل.
- **آموزش و کاربرد:** اصطلاحات مرتبط: **Boot up** = روشن شدن، **Reboot** = راه‌اندازی مجدد (ری‌استارت)، **Boot Menu** = منویی (با کلیدهایی مثل F12 یا Esc هنگام روشن شدن) برای انتخاب این‌که سیستم از کدام دیسک یا فلش بالا بیاید — مثلاً برای نصب ویندوز با فلش.
- **تعریف کوتاه:** فرایند راه‌اندازی و بالا آمدن سیستم.

### ۱.۱۶) ROM — حافظه‌ی فقط‌خواندنی
- **تعریف:** حافظه‌ای دائمی و غیرفرار (Read-Only Memory) که محتوای آن هنگام خاموشی پاک نمی‌شود و دستورهای اولیه‌ی راه‌اندازی سیستم (Firmware) را در خود نگه می‌دارد.
- **آموزش و کاربرد:** برخلاف RAM که موقتی است، ROM همیشگی است و معمولاً کاربر نمی‌تواند آن را تغییر دهد. نام‌های DVD-ROM و CD-ROM هم از همین مفهوم «فقط خواندنی» می‌آیند: می‌توانید بخوانید، نه بنویسید.
- **تعریف کوتاه:** حافظه‌ی دائمی فقط‌خواندنی برای دستورهای پایه.

### ۱.۱۷) Motherboard — مادربرد / برد اصلی
- **تعریف:** برد مدار اصلی کامپیوتر که تمام قطعات — پردازنده، رم، کارت گرافیک، هارد و... — مستقیماً یا با کابل به آن متصل می‌شوند و از طریق آن با هم ارتباط دارند.
- **آموزش و کاربرد:** هنگام خرید مادربرد به **سوکت پردازنده** (مثلاً AM5 برای AMD)، نوع رم پشتیبانی‌شده (DDR4/DDR5)، تعداد اسلات‌های M.2 و اندازه‌ی برد (ATX, mATX) توجه کنید. مادربرد «ستون فقرات» سیستم است.
- **تعریف کوتاه:** بردی که همه‌ی قطعات به آن وصل می‌شوند.

### ۱.۱۸) Chipset — چیپ‌ست
- **تعریف:** مجموعه تراشه‌هایی روی مادربرد که جریان داده بین CPU، رم، کارت گرافیک و دستگاه‌های جانبی را مدیریت و هماهنگ می‌کنند.
- **آموزش و کاربرد:** چیپ‌ست تعیین می‌کند مادربرد چه امکاناتی دارد: چند پورت USB، چند اسلات PCIe، پشتیبانی از اورکلاک و... . در گذشته به پل‌های شمالی و جنوبی تقسیم می‌شد. اگر CPU «مغز» باشد، چیپ‌ست «دفتر مدیریت» آن است.
- **تعریف کوتاه:** مدیر ارتباطات داخلی روی مادربرد.

### ۱.۱۹) Cache — حافظه‌ی نهان / کش
- **تعریف:** حافظه‌ای بسیار سریع و کم‌حجم (معمولاً داخل پردازنده) که داده‌ها و دستورهای پرتکرار را نزدیک CPU نگه می‌دارد تا دوباره به رم کندتر مراجعه نکند.
- **آموزش و کاربرد:** کش CPU سه سطح دارد: **L1** کوچک و فوق‌سریع، **L2** متوسط و **L3** بزرگ‌تر و مشترک بین هسته‌ها. وقتی در مشخصات پردازنده «Cache 24MB» می‌بینید، منظور همین است. (توجه: کش مرورگر در فصل ۶ مفهومی مشابه اما جداگانه است.)
- **تعریف کوتاه:** حافظه‌ی فوق‌سریع برای دسترسی فوری به داده‌های پرکاربرد.

### ۱.۲۰) Clock Speed — سرعت ساعت / فرکانس کلاک
- **تعریف:** تعداد چرخه‌های پردازشی که CPU در هر ثانیه انجام می‌دهد؛ با واحد گیگاهرتز (GHz) اندازه گرفته می‌شود.
- **آموزش و کاربرد:** فرکانس ۳٫۵ گیگاهرتز یعنی ۳٫۵ میلیارد چرخه در ثانیه. نکته‌ی مهم: فرکانس بالاتر همیشه به معنی کامپیوتر سریع‌تر نیست؛ معماری پردازنده و تعداد هسته‌ها هم مهم‌اند. اصطلاح **اورکلاک (Overclock)** یعنی اجرای CPU با فرکانسی بیشتر از مقدار کارخانه.
- **تعریف کوتاه:** تعداد ضربان‌های کاری پردازنده در هر ثانیه.

### ۱.۲۱) Bus — گذرگاه / باس
- **تعریف:** مسیرهای ارتباطی (سیم‌کشی داخلی) که داده، آدرس و فرمان‌ها را بین پردازنده، حافظه و سایر قطعات جابه‌جا می‌کنند.
- **آموزش و کاربرد:** گذرگاه را مثل بزرگراه درون کامپیوتر تصور کنید؛ هرچه عرض (Width) آن بیشتر باشد، داده‌ی بیشتری هم‌زمان عبور می‌کند. انواع اصلی: گذرگاه داده (Data Bus)، گذرگاه آدرس (Address Bus) و گذرگاه کنترل (Control Bus).
- **تعریف کوتاه:** بزرگراه انتقال داده درون کامپیوتر.

### ۱.۲۲) Power Supply (PSU) — منبع تغذیه / پاور
- **تعریف:** قطعه‌ای که برق شهر (۲۲۰ ولت متناوب) را به ولتاژهای پایین و پایدارِ مورد نیاز قطعات (۱۲، ۵ و ۳٫۳ ولت مستقیم) تبدیل می‌کند.
- **آموزش و کاربرد:** هنگام خرید به **توان (Watt)** توجه کنید — برای سیستمی با کارت گرافیک قدرتمند معمولاً ۶۵۰ وات به بالا لازم است — و به گواهی کیفیت **80 Plus** (Bronze, Gold...) که راندمان را نشان می‌دهد. پاور بی‌کیفیت می‌تواند کل سیستم را بسوزاند؛ روی آن صرفه‌جویی نکنید.
- **تعریف کوتاه:** تأمین‌کننده‌ی برق مناسب قطعات کامپیوتر.

### ۱.۲۳) BIOS — بایوس
- **تعریف:** میان‌افزار قدیمی روی مادربرد که بلافاصله بعد از روشن شدن، سخت‌افزار را تست (POST) می‌کند و سیستم‌عامل را پیدا و اجرا می‌کند.
- **آموزش و کاربرد:** برای ورود به تنظیمات BIOS هنگام روشن شدن، معمولاً کلید **Del** یا **F2** را فشار می‌دهند. در محیط آن می‌توانید ترتیب بوت (از کدام دیسک بالا بیاید)، فرکانس رم و تنظیمات فن را تغییر دهید. امروزه BIOS جای خود را به UEFI داده است.
- **تعریف کوتاه:** برنامه‌ی اولیه‌ی راه‌اندازی سخت‌افزار (نسخه‌ی قدیمی).

### ۱.۲۴) UEFI — یوئی‌اف‌آی
- **تعریف:** جایگزین مدرن BIOS؛ میان‌افزاری با رابط گرافیکی، بوت سریع‌تر، امنیت بیشتر و پشتیبانی از دیسک‌های بزرگ‌تر از ۲ ترابایت.
- **آموزش و کاربرد:** قابلیت مهم UEFI قابلیت **Secure Boot** است که جلوی بارگذاری سیستم‌عامل‌ها و بدافزارهای سطح‌پایینِ غیرمجاز را هنگام بوت می‌گیرد. برای نصب ویندوز ۱۱ فعال بودن Secure Boot و TPM الزامی است.
- **تعریف کوتاه:** نسخه‌ی مدرن، سریع و امن BIOS.

### ۱.۲۵) User — کاربر
- **تعریف:** شخصی که از کامپیوتر یا سرویس استفاده می‌کند؛ در سیستم‌ها معمولاً هر فرد یک **حساب کاربری (User Account)** با نام کاربری و رمز عبور دارد.
- **آموزش و کاربرد:** انواع کاربر در سیستم‌عامل: **Administrator** (مدیر با دسترسی کامل)، **Standard** (کاربر عادی) و **Guest** (مهمان). نکته‌ی امنیتی: برای کارهای روزمره از حساب ادمین استفاده نکنید تا در صورت نفوذ بدافزار، خسارت محدود شود.
- **تعریف کوتاه:** استفاده‌کننده از سیستم یا سرویس.

### ۱.۲۶) Analog — آنالوگ
- **تعریف:** داده یا سیگنالی **پیوسته و ممتد** که می‌تواند بی‌نهایت مقدار بگیرد؛ در مقابل سیگنال **دیجیتال** که گسسته و از صفر و یک ساخته شده است.
- **آموزش و کاربرد:** ساعت عقربه‌ای آنالوگ و ساعت عددی دیجیتال است. صدای طبیعیِ ضبط‌شده روی نوار کاست آنالوگ است. کامپیوترها دیجیتال‌اند، پس برای پردازش صدا یا دما ابتدا با مبدل **ADC** (آنالوگ به دیجیتال) نمونه‌برداری می‌کنند و برای پخش با **DAC** برعکس.
- **تعریف کوتاه:** سیگنال پیوسته، نقطه‌ی مقابل دیجیتال.

### ۱.۲۷) PCI / PCIe — پی‌سی‌آی
- **تعریف:** استاندارد رابط اتصال کارت‌های توسعه (گرافیک، شبکه، صدا و...) به مادربرد. نسل قدیمی آن PCI و نسل امروزیِ سریع آن **PCI Express (PCIe)** است.
- **آموزش و کاربرد:** کارت گرافیک‌ها معمولاً در اسلات **PCIe x16** نصب می‌شوند. نسل‌های PCIe (۳٫۰، ۴٫۰، ۵٫۰) سرعت انتقال را دوبرابر می‌کنند. SSDهای NVMe امروزی هم از همین مسیر استفاده می‌کنند (در فصل ۲ می‌بینید).
- **تعریف کوتاه:** اسلات و استاندارد اتصال کارت‌ها به مادربرد.

✅ **پایان فصل ۱** — حالا می‌دانید یک کامپیوتر از چه اجزایی تشکیل شده و چطور کار می‌کند.

<a id="chapter2"></a>
# فصل ۲ | حافظه و ذخیره‌سازی 💾

داده‌ها باید جایی نگهداری شوند. در این فصل با انواع دیسک‌ها، رابط‌های اتصال، سیستم‌فایل‌ها و روش‌های محافظت از داده (بکاپ و RAID) آشنا می‌شوید.

---

### ۲.۱) HDD — هارددیسک مکانیکی
- **تعریف:** حافظه‌ی دائمی مغناطیسی که داده‌ها را روی صفحات فلزی چرخان (Platter) و با سوزن خواندن/نوشتن ذخیره می‌کند.
- **آموزش و کاربرد:** سرعت چرخش رایج ۵۴۰۰ یا ۷۲۰۰ دور در دقیقه (RPM) است. هاردها ارزان‌ترین حافظه به ازای هر گیگابایت‌اند و برای آرشیو عکس و فیلم عالی‌اند، اما به ضربه حساس و نسبت به SSD کند هستند. اگر صدای «کلیق‌کلیق» غیرعادی از هارد شنیدید، فوراً بکاپ بگیرید!
- **تعریف کوتاه:** حافظه‌ی مغناطیسی مکانیکی با صفحات چرخان.

### ۲.۲) SSD — حافظه‌ی جامد / اس‌اس‌دی
- **تعریف:** حافظه‌ای مبتنی بر تراشه‌های فلش که هیچ قطعه‌ی متحرکی ندارد؛ به همین دلیل چندین برابر سریع‌تر، بی‌صدا، کم‌مصرف و مقاوم‌تر از HDD است.
- **آموزش و کاربرد:** بزرگ‌ترین ارتقای حس‌شونده برای هر کامپیوتر قدیمی، جایگزینی هارد با SSD است — بوت ویندوز از چند دقیقه به چند ثانیه می‌رسد. نکته: برای سلامت SSD در ویندوز از TRIM استفاده می‌شود؛ میزان عمر آن با **TBW** (مقدار کل نوشتن) سنجیده می‌شود.
- **تعریف کوتاه:** حافظه‌ی سریع فلش بدون قطعه‌ی متحرک.

### ۲.۳) NVMe — ان‌وی‌ام‌ای
- **تعریف:** پروتکل ارتباطی فوق‌سریع که مخصوص SSDها طراحی شده و از مسیر پرسرعت PCIe استفاده می‌کند؛ سرعتش به چندین هزار مگابایت در ثانیه می‌رسد.
- **آموزش و کاربرد:** SSDهای NVMe معمولاً در شکل ظریف **M.2** (شبیه آدامس‌بار) روی اسلات مخصوص مادربرد نصب می‌شوند و حدود ۵ تا ۱۰ برابر از SSDهای SATA سریع‌ترند. برای تدوین ویدیو و بازی‌های نسل جدید انتخاب اول است. NVMe «پروتکل» است و M.2 «شکل ظاهری» — این دو را اشتباه نگیرید.
- **تعریف کوتاه:** استاندارد فوق‌سریع ارتباط SSD از مسیر PCIe.

### ۲.۴) SATA — ساتا
- **تعریف:** رابط رایج اتصال هارد و SSD به مادربرد با کابل مخصوص؛ نسل سوم آن (SATA III) حداکثر به حدود ۶۰۰ مگابایت در ثانیه می‌رسد.
- **آموزش و کاربرد:** اگر SSD معمولیِ ۲٫۵ اینچی می‌خرید، تقریباً حتماً SATA است و به کابل دیتا + کابل برق پاور نیاز دارد. سقف سرعت SATA حدود ۶۰۰ مگابایت است، پس برای سرعت‌های بالاتر باید سراغ NVMe بروید.
- **تعریف کوتاه:** رابط رایج اتصال دیسک‌ها با سقف حدود ۶۰۰ مگابایت.

### ۲.۵) PCI / PCIe در ذخیره‌سازی
- **تعریف:** همان استاندارد اتصال فصل ۱ است که در بحث ذخیره‌سازی نقش حیاتی دارد: SSDهای NVMe از مسیر PCIe به مادربرد وصل می‌شوند تا از گلوگاه SATA رد نشوند.
- **آموزش و کاربرد:** هر نسل PCIe سرعت را دو برابر می‌کند: یک SSD با PCIe 3.0 حدود ۳۵۰۰ و با PCIe 4.0 حدود ۷۰۰۰ مگابایت در ثانیه سرعت خواندن دارد. قبل از خرید SSD سریع، چک کنید مادربردتان از همان نسل پشتیبانی کند.
- **تعریف کوتاه:** بزرگراه پرسرعت اتصال SSDهای مدرن.

### ۲.۶) USB Flash — فلش‌مموری
- **تعریف:** حافظه‌ی قابل‌حمل کوچک که با پورت USB به دستگاه‌ها وصل می‌شود و برای انتقال و نگهداری موقت فایل‌ها استفاده می‌شود.
- **آموزش و کاربرد:** استانداردها (USB 2.0 / 3.0 / 3.2) تعیین‌کننده‌ی سرعت‌اند؛ پورت‌های آبی معمولاً USB 3 هستند. برای جدا کردن امن فلش در ویندوز حتماً از گزینه‌ی **Eject** استفاده کنید تا داده‌ها خراب نشوند. با ابزارهایی مثل Rufus می‌توان فلش را «بوت‌ابل» کرد و ویندوز نصب کرد.
- **تعریف کوتاه:** حافظه‌ی جیبی قابل‌حمل با اتصال USB.

### ۲.۷) Memory Card — کارت حافظه
- **تعریف:** کارت‌های حافظه‌ی کوچک مثل SD و microSD که در دوربین، گوشی، کنسول بازی و وسایل مشابه استفاده می‌شوند.
- **آموزش و کاربرد:** روی کارت حافظه به **کلاس سرعت** (مانند Class 10، UHS-I، V30) توجه کنید: برای فیلم‌برداری 4K حداقل کلاس V30 لازم است. ظرفیت‌های پایین (زیر ۳۲ گیگ) معمولاً FAT32 و بالاتر exFAT فرمت می‌شوند.
- **تعریف کوتاه:** حافظه‌ی جمع‌وجور دستگاه‌های قابل‌حمل.

### ۲.۸) Partition — پارتیشن
- **تعریف:** تقسیم‌بندی منطقی یک دیسک فیزیکی به چند بخش مجزا که هرکدام مثل یک دیسک مستقل (مثلاً درایو C و D) دیده می‌شوند.
- **آموزش و کاربرد:** رایج‌ترین الگو: پارتیشن C برای ویندوز و برنامه‌ها و پارتیشن D برای فایل‌ها — تا اگر ویندوز خراب شد، با نصب مجدد فقط C پاک شود. برای مدیریت پارتیشن در ویندوز از ابزار **Disk Management** و برای کارهای پیشرفته از ابزارهایی مثل MiniTool استفاده می‌شود.
- **تعریف کوتاه:** بخش‌بندی منطقی یک دیسک به چند قسمت.

### ۲.۹) File System — سیستم‌فایل
- **تعریف:** روش و قواعد سازماندهی، نام‌گذاری، ذخیره و بازیابی فایل‌ها روی دیسک؛ مثل NTFS، FAT32 و exFAT.
- **آموزش و کاربرد:** سیستم‌فایل تعیین می‌کند حداکثر حجم فایل چقدر باشد، چه سیستم‌عامل‌هایی دیسک را بخوانند و چه امکانات امنیتی وجود داشته باشد. سیستم‌فایل‌های رایج لینوکس ext4 و مک APFS است.
- **تعریف کوتاه:** نظام مدیریت و سازماندهی فایل روی دیسک.

### ۲.۱۰) NTFS — ان‌تی‌اف‌اس
- **تعریف:** سیستم‌فایل استاندارد ویندوز با پشتیبانی از فایل‌های بزرگ، سطح دسترسی (Permission)، رمزنگاری و قابلیت ژورنالینگ (ثبت تغییرات برای جلوگیری از خرابی).
- **آموزش و کاربرد:** برای نصب ویندوز و استفاده‌ی داخلی در ویندوز، NTFS بهترین انتخاب است. نکته: مک به‌صورت پیش‌فرض NTFS را فقط **می‌خواند** و نمی‌تواند روی آن بنویسد؛ پس برای انتقال فایل بین ویندوز و مک از exFAT استفاده کنید.
- **تعریف کوتاه:** سیستم‌فایل اصلی و کامل‌ترین گزینه‌ی ویندوز.

### ۲.۱۱) FAT32 — فت ۳۲
- **تعریف:** سیستم‌فایل قدیمی اما بسیار سازگار که تقریباً همه‌ی دستگاه‌ها (از مانیتور ماشین تا کنسول بازی) آن را می‌شناسند؛ با محدودیت حداکثر حجم هر فایل = **۴ گیگابایت**.
- **آموزش و کاربرد:** اگر فایل تکی بزرگ‌تر از ۴ گیگ روی فلش FAT32 کپی کنید با خطا مواجه می‌شوید — راه‌حل، فرمت به exFAT است. FAT32 بیشتر برای فلش‌های بوت و دستگاه‌های قدیمی کاربرد دارد.
- **تعریف کوتاه:** سیستم‌فایل قدیمی سازگار با همه، با سقف ۴ گیگ برای هر فایل.

### ۲.۱۲) exFAT — ای‌ایکس‌فت
- **تعریف:** سیستم‌فایل نسبتاً مدرن و سبک مخصوص فلش‌مموری و کارت حافظه که محدودیت ۴ گیگابایتی FAT32 را ندارد و هم در ویندوز هم در مک به‌خوبی کار می‌کند.
- **آموزش و کاربرد:** بهترین انتخاب برای هارد اکسترنال و فلش‌هایی که بین دستگاه‌های مختلف (ویندوز، مک، دوربین) جابه‌جا می‌شوند. تنها نقطه‌ضعف: قابلیت‌های امنیتی و ژورنالینگ NTFS را ندارد.
- **تعریف کوتاه:** سیستم‌فایل سبک برای فلش، مناسب فایل‌های بزرگ و استفاده بین‌پلتفرمی.

### ۲.۱۳) Cluster — کلاستر / خوشه
- **تعریف:** کوچک‌ترین واحدی از فضا که سیستم‌فایل می‌تواند به یک فایل اختصاص دهد؛ معمولاً چند سکتور متوالی است (مثلاً ۴ کیلوبایت).
- **آموزش و کاربرد:** یک فایل ۱ کیلوبایتی روی کلاستر ۴ کیلوبایتی، همان ۴ کیلوبایت را اشغال می‌کند — به همین دلیل در Properties فایل دو مقدار **Size** (اندازه‌ی واقعی) و **Size on disk** (فضای اشغال‌شده) می‌بینید. خوشه‌های خراب (Bad Cluster) نشانه‌ی مشکل دیسک‌اند.
- **تعریف کوتاه:** کوچک‌ترین واحد تخصیص فضا به فایل.

### ۲.۱۴) Sector — سکتور / بخش
- **تعریف:** کوچک‌ترین واحد فیزیکی ذخیره‌سازی روی دیسک (به‌صورت سنتی ۵۱۲ بایت و در دیسک‌های جدید ۴ کیلوبایت).
- **آموزش و کاربرد:** **Bad Sector** بخشی از دیسک است که آسیب دیده و داده‌ی آن گم یا خراب می‌شود. ابزار «chkdsk» در ویندوز می‌تواند سکتورهای خراب را شناسایی و علامت‌گذاری کند تا دیگر استفاده نشوند؛ اما زیاد شدنشان زنگ خطر جدی برای عمر دیسک است.
- **تعریف کوتاه:** کوچک‌ترین واحد فیزیکی ذخیره‌سازی روی دیسک.

### ۲.۱۵) Backup — نسخه‌ی پشتیبان / بکاپ
- **تعریف:** کپی تهیه‌شده از داده‌ها برای بازیابی در صورت حذف، خرابی، سرقت یا حمله‌ی باج‌افزار.
- **آموزش و کاربرد:** قانون طلایی **3-2-1** را رعایت کنید: حداقل **۳** نسخه از داده، روی **۲** رسانه‌ی متفاوت (مثلاً لپ‌تاپ + هارد اکسترنال) و **۱** نسخه بیرون از خانه (مثل فضای ابری). ابزارهای رایج: File History ویندوز، Time Machine مک و سرویس‌های ابری مثل Google Drive.
- **تعریف کوتاه:** کپی امنیتی از اطلاعات برای روز مبادا.

### ۲.۱۶) RAID — رید
- **تعریف:** فناوری ترکیب چند دیسک فیزیکی در قالب یک مجموعه برای افزایش سرعت، افزونگی (تحمل خرابی) یا هر دو.
- **آموزش و کاربرد:** حالت‌های رایج: **RAID 0** = نصف‌کردن داده بین دو دیسک برای سرعت دوبرابر (بدون امنیت)، **RAID 1** = کپی یکسان روی دو دیسک (آینه‌ای؛ اگر یکی بسوزد داده‌ها سالم‌اند)، **RAID 5** = امنیت + سرعت با حداقل ۳ دیسک. توجه: RAID جایگزین بکاپ نیست، چون حذف اشتباه در هر دو دیسک اتفاق می‌افتد.
- **تعریف کوتاه:** ترکیب چند دیسک برای امنیتِ داده یا سرعت بیشتر.

✅ **پایان فصل ۲**

---

<a id="chapter3"></a>
# فصل ۳ | سیستم‌عامل ⚙️

سیستم‌عامل، مدیرعامل کامپیوتر است: سخت‌افزار را مدیریت می‌کند، برنامه‌ها را اجرا می‌کند و رابط بین شما و ماشین است. در این فصل آن را به‌خوبی می‌شناسید.

---

### ۳.۱) Operating System (OS) — سیستم‌عامل
- **تعریف:** مهم‌ترین نرم‌افزار هر کامپیوتر که منابع سخت‌افزاری (پردازنده، حافظه، دیسک‌ها) را مدیریت، برنامه‌ها را اجرا و بستر تعامل کاربر با ماشین را فراهم می‌کند.
- **آموزش و کاربرد:** بدون سیستم‌عامل، کامپیوتر فقط یک صفحه‌ی سیاه است. وظایف اصلی‌اش: مدیریت فایل‌ها، اجرای هم‌زمان برنامه‌ها، مدیریت حافظه، کنترل امنیت و اتصال سخت‌افزارها. نمونه‌ها: Windows، Linux، macOS، Android و iOS.
- **تعریف کوتاه:** نرم‌افزار اصلی که کل کامپیوتر را مدیریت می‌کند.

### ۳.۲) Windows — ویندوز
- **تعریف:** سیستم‌عامل شرکت مایکروسافت؛ رایج‌ترین سیستم‌عامل کامپیوترهای شخصی در جهان با پشتیبانی از گسترده‌ترین مجموعه نرم‌افزار و سخت‌افزار.
- **آموزش و کاربرد:** نسخه‌های فعلی رایج ویندوز ۱۰ و ۱۱ هستند. کلیدهای میانبر ضروری: **Win + E** (باز کردن File Explorer)، **Ctrl + Shift + Esc** (Task Manager)، **Win + I** (تنظیمات) و **Alt + Tab** (جابه‌جایی بین پنجره‌ها).
- **تعریف کوتاه:** سیستم‌عامل محبوب و پرکاربرد مایکروسافت.

### ۳.۳) Linux — لینوکس
- **تعریف:** سیستم‌عامل آزاد و متن‌باز (Open Source) که هسته‌ی آن توسط لینوس توروالدز ساخته شد؛ پایه‌ی بیشتر سرورها، ابرها و ابرکامپیوترهای دنیا.
- **آموزش و کاربرد:** به هر نسخه‌ی آماده‌ی لینوکس یک «توزیع (Distro)» گفته می‌شود: **Ubuntu** و **Mint** برای تازه‌کارها، Debian برای پایداری و Fedora برای کاربران حرفه‌ای. لینوکس رایگان، امن و بسیار قابل سفارشی‌سازی است. اگر می‌خواهید برنامه‌نویس یا متخصص شبکه شوید، یادگیری لینوکس ضروری است.
- **تعریف کوتاه:** سیستم‌عامل آزاد، امن و متن‌باز؛ پادشاه سرورها.

### ۳.۴) macOS — مک‌او‌اس
- **تعریف:** سیستم‌عامل اختصاصی کامپیوترهای مک اپل که بر پایه‌ی یونیکس ساخته شده و با سخت‌افزار اپل یکپارچگی کامل دارد.
- **آموزش و کاربرد:** نقاط قوت: پایداری، بهینگی با سخت‌افزار و اکوسیستم هماهنگ با آیفون و آیپد (مثل قابلیت AirDrop). فقط روی سخت‌افزار اپل به‌صورت رسمی اجرا می‌شود. ابزار ترمینال آن شبیه لینوکس است.
- **تعریف کوتاه:** سیستم‌عامل اختصاصی کامپیوترهای اپل.

### ۳.۵) Android — اندروید
- **تعریف:** سیستم‌عامل موبایل گوگل که بر پایه‌ی هسته‌ی لینوکس ساخته شده و روی اکثر گوشی‌ها و تبلت‌های دنیا (سامسونگ، شیائومی و...) نصب است.
- **آموزش و کاربرد:** اندروید متن‌باز است و به همین دلیل هر سازنده یک نسخه‌ی سفارشی از آن (مثل One UI سامسونگ) ارائه می‌دهد. برنامه‌ها را معمولاً از فروشگاه **Google Play** نصب می‌کنید؛ نصب فایل APK از منابع ناشناس ریسک امنیتی بالایی دارد.
- **تعریف کوتاه:** سیستم‌عامل رایج موبایل مبتنی بر لینوکس.

### ۳.۶) iOS — آی‌او‌اس
- **تعریف:** سیستم‌عامل موبایل اختصاصی اپل برای آیفون که با امنیت بالا، به‌روزرسانی‌های طولانی‌مدت و یکپارچگی با سخت‌افزار شناخته می‌شود.
- **آموزش و کاربرد:** برخلاف اندروید، iOS سیستم بسته‌ای است: نصب برنامه فقط از **App Store** انجام می‌شود که همین امنیت را بالا می‌برد و آزادی کاربر را کمتر. نسخه‌ی تبلت آن با نام iPadOS شناخته می‌شود.
- **تعریف کوتاه:** سیستم‌عامل امن و بسته‌ی آیفون.

### ۳.۷) Kernel — هسته‌ی سیستم‌عامل
- **تعریف:** قلب و مرکزی‌ترین بخش سیستم‌عامل که مستقیماً با سخت‌افزار کار می‌کند: مدیریت CPU، حافظه، دستگاه‌ها و ارتباط برنامه‌ها با سخت‌افزار بر عهده‌ی آن است.
- **آموزش و کاربرد:** وقتی می‌گوییم «لینوکس»، دقیقاً منظور از لینوکس، یک **هسته** است. اگر کرنل دچار خطای جدی شود کل سیستم کرش می‌کند — صفحه‌ی آبی ویندوز (BSOD) معمولاً خطای سطح کرنل است.
- **تعریف کوتاه:** قلب سیستم‌عامل و واسط مستقیم با سخت‌افزار.

### ۳.۸) Driver — درایور
- **تعریف:** نرم‌افزار کوچکی که به سیستم‌عامل یاد می‌دهد چطور با یک سخت‌افزار خاص (کارت گرافیک، پرینتر، اسکنر و...) ارتباط برقرار کند.
- **آموزش و کاربرد:** اگر قطعه‌ای کار نمی‌کند یا درست کار نمی‌کند، اول به درایور آن شک کنید. در ویندوز با **Device Manager** می‌توانید درایورها را به‌روزرسانی، غیرفعال یا حذف کنید. درایور کارت گرافیک را همیشه از سایت NVIDIA/AMD/Intel دانلود کنید، نه منابع ناشناس.
- **تعریف کوتاه:** مترجم بین سیستم‌عامل و سخت‌افزار.

### ۳.۹) Process — فرایند / پروسه
- **تعریف:** برنامه‌ی در حال اجرا در حافظه؛ هر پروسه فضای حافظه‌ی اختصاصی و منابع خودش را دارد و توسط سیستم‌عامل مدیریت می‌شود.
- **آموزش و کاربرد:** در **Task Manager** (با Ctrl+Shift+Esc) همه‌ی پروسه‌های فعال و میزان مصرف CPU و رم هرکدام را می‌بینید. وقتی برنامه‌ای هنگ می‌کند، با **End Task** پروسه‌اش را خاتمه می‌دهید. یک مرورگر کروم معمولاً چندین پروسه جدا دارد.
- **تعریف کوتاه:** برنامه‌ای که در حال اجراست.

### ۳.۱۰) Thread — رشته / نخ
- **تعریف:** کوچک‌ترین واحد اجرای دستورات که درون یک پروسه زندگی می‌کند؛ یک پروسه می‌تواند چند Thread هم‌زمان داشته باشد تا کارهای خود را موازی انجام دهد.
- **آموزش و کاربرد:** مثال: مرورگر با یک Thread صفحه را بارگذاری، با Thread دیگر ویدیو پخش و با سومی به کلیک‌های شما پاسخ می‌دهد. به همین دلیل است که پردازنده‌های «چند هسته‌ای و چند رشته‌ای» (مثل ۸ هسته / ۱۶ رشته) برنامه‌ها را روان‌تر اجرا می‌کنند.
- **تعریف کوتاه:** زیرواحد اجراییِ سبک درون یک فرایند.

### ۳.۱۱) Service — سرویس
- **تعریف:** برنامه‌هایی که بدون رابط کاربری در پس‌زمینه اجرا می‌شوند و معمولاً با روشن شدن سیستم خودکار شروع به کار می‌کنند.
- **آموزش و کاربرد:** در ویندوز با اجرای **services.msc** فهرست سرویس‌ها را می‌بینید (مثل Windows Update یا سرویس چاپ). غیرفعال‌کردن سرویس‌های غیرضروری گاهی سرعت بوت را بیشتر می‌کند، اما خیلی‌ها حیاتی‌اند — با احتیاط تغییر دهید. معادل لینوکسی سرویس، «Daemon» نام دارد.
- **تعریف کوتاه:** برنامه‌ی پس‌زمینه‌ی بدون پنجره برای وظایف سیستمی.

### ۳.۱۲) Task — وظیفه
- **تعریف:** واحدی از کار یا عملیات که باید توسط سیستم انجام شود؛ می‌تواند یک برنامه‌ی باز، یک پروسه‌ی پس‌زمینه یا یک کار زمان‌بندی‌شده باشد.
- **آموزش و کاربرد:** دو ابزار کاربردی ویندوز: **Task Manager** برای دیدن و مدیریت کارهای در حال اجرا، و **Task Scheduler** برای زمان‌بندی اجرای خودکار برنامه‌ها (مثلاً بکاپ هر شب ساعت ۱۲).
- **تعریف کوتاه:** واحد کاری روی سیستم، قابل مشاهده در Task Manager.

### ۳.۱۳) Registry — رجیستری
- **تعریف:** پایگاه‌داده‌ی مرکزی تنظیمات ویندوز که اطلاعات سیستم، سخت‌افزار، کاربران و برنامه‌های نصب‌شده را به صورت ساختار درختی نگه می‌دارد.
- **آموزش و کاربرد:** با اجرای **regedit** (در پنجره‌ی Run با Win+R) وارد رجیستری می‌شوید. برخی تنظیمات و ترفندهای ویندوز فقط از همین‌جا قابل تغییرند. ⚠️ هشدار: تغییر اشتباه می‌تواند ویندوز را نابود کند؛ قبل از هر تغییر از شاخه‌ی موردنظر با Export پشتیبان بگیرید.
- **تعریف کوتاه:** دفترچه‌ی مرکزی تنظیمات ویندوز.

### ۳.۱۴) Terminal — ترمینال
- **تعریف:** پنجره‌ای برای ارتباط متنی با سیستم‌عامل که در آن دستورها را تایپ و نتیجه را به صورت متن دریافت می‌کنید.
- **آموزش و کاربرد:** در بسیاری از کارها (به‌خصوص مدیریت سرور و برنامه‌نویسی) ترمینال از محیط گرافیکی سریع‌تر و قدرتمندتر است. چند دستور اولیه‌ی لینوکس: `ls` (نمایش فایل‌ها)، `cd` (تغییر پوشه)، `pwd` (مسیر فعلی). در ویندوز هم Windows Terminal وجود دارد.
- **تعریف کوتاه:** محیط متنی صحبت کردن با سیستم‌عامل.

### ۳.۱۵) Shell — پوسته / شل
- **تعریف:** برنامه‌ی مفسر دستورات؛ واسطی که دستورهای تایپ‌شده‌ی شما را می‌خواند، تفسیر و به هسته‌ی سیستم‌عامل منتقل می‌کند و خروجی را برمی‌گرداند.
- **آموزش و کاربرد:** ترمینال «پنجره» است و شل «مغز» داخل آن. شل‌های معروف لینوکس: **Bash** و **Zsh**. با شل می‌توان اسکریپت (فایل متنی حاوی دستورها) نوشت و کارهای تکراری را خودکار کرد.
- **تعریف کوتاه:** مفسر و اجراکننده‌ی دستورهای کاربر.

### ۳.۱۶) Command Prompt (CMD) — خط فرمان ویندوز
- **تعریف:** خط فرمان کلاسیک ویندوز برای اجرای دستورهای متنی با نام فایل cmd.exe.
- **آموزش و کاربرد:** باز کردن: تایپ `cmd` در منوی Start. دستورهای پرکاربرد: `dir` (فهرست فایل‌ها)، `ipconfig` (مشاهده‌ی IP شبکه)، `ping google.com` (تست اتصال اینترنت)، `chkdsk` (بررسی دیسک) و `sfc /scannow` (ترمیم فایل‌های ویندوز).
- **تعریف کوتاه:** خط فرمان کلاسیک ویندوز.

### ۳.۱۷) PowerShell — پاورشل
- **تعریف:** خط فرمان پیشرفته و زبان اسکریپت‌نویسی مدرن مایکروسافت که برخلاف CMD با «اشیا» کار می‌کند و برای مدیریت و خودکارسازی حرفه‌ای سیستم طراحی شده.
- **آموزش و کاربرد:** دستورهای PowerShell (به نام **Cmdlet**) الگوی «فعل-اسم» دارند: `Get-Process` (فهرست پروسه‌ها)، `Get-Service` (فهرست سرویس‌ها). برای مدیران سیستم (ادمین‌ها)، پاورشل ابزاری حیاتی است و می‌توان با آن صدها کامپیوتر را هم‌زمان مدیریت کرد.
- **تعریف کوتاه:** خط فرمان قدرتمند و اسکریپتی ویندوز.

✅ **پایان فصل ۳**

<a id="chapter4"></a>
# فصل ۴ | فایل و پوشه 📁

دنیای دیجیتال ما از فایل‌ها ساخته شده است. این فصل یاد می‌گیرد فایل چیست، چطور سازماندهی می‌شود و فرمت‌های مهم فشرده‌سازی و دیسک کدام‌اند.

---

### ۴.۱) File — فایل
- **تعریف:** مجموعه‌ای از داده‌های مرتبط که با یک نام مشخص روی حافظه ذخیره می‌شود؛ مانند سند، عکس، آهنگ یا برنامه.
- **آموزش و کاربرد:** هر فایل سه بخش اصلی دارد: **نام**، **پسوند** و **مسیر ذخیره‌سازی**. عملیات پایه روی فایل: ساخت، کپی (Ctrl+C)، انتقال (Ctrl+X)، تغییر نام (F2) و حذف. فایل‌های پاک‌شده معمولاً اول به Recycle Bin می‌روند و تا خالی‌کردن سطل بازیافت قابل برگشت‌اند.
- **تعریف کوتاه:** واحد ذخیره‌سازی اطلاعات با یک نام مشخص.

### ۴.۲) Folder — پوشه
- **تعریف:** محفظه‌ای برای گروه‌بندی و سازماندهی فایل‌ها و حتی پوشه‌های دیگر (پوشه‌ی تودرتو).
- **آموزش و کاربرد:** عادت حرفه‌ای: ساختار پوشه‌ای منظم بسازید (مثلاً: اسناد ← کار ← پروژه‌ها ← ۱۴۰۵) تا هر فایلی را در چند ثانیه پیدا کنید. ساخت پوشه‌ی جدید در ویندوز با **Ctrl+Shift+N** انجام می‌شود.
- **تعریف کوتاه:** محفظه‌ی نگهداری و دسته‌بندی فایل‌ها.

### ۴.۳) Directory — دایرکتوری / شاخه
- **تعریف:** همان پوشه در ادبیات فنی سیستم‌عامل‌ها (به‌خصوص لینوکس و خط فرمان)؛ ساختاری درختی که از ریشه (Root) شروع می‌شود.
- **آموزش و کاربرد:** در خط فرمان با دستور `cd` (Change Directory) بین شاخه‌ها حرکت می‌کنید و با `..` یک سطح بالا می‌روید. در لینوکس ریشه‌ی درخت `/` است و در ویندوز هر دیسک ریشه‌ی خودش را دارد (مثل C:\).
- **تعریف کوتاه:** معادل فنی و رسمی «پوشه».

### ۴.۴) Path — مسیر
- **تعریف:** آدرس دقیق محل قرارگیری یک فایل یا پوشه در سیستم، از ریشه تا مقصد.
- **آموزش و کاربرد:** مثال ویندوز: `C:\Users\Ali\Documents\report.docx` و مثال لینوکس: `/home/ali/report.docx`. دو نوع مسیر داریم: **مطلق (Absolute)** = آدرس کامل از ریشه، و **نسبی (Relative)** = نسبت به جایی که الان هستید. با راست‌کلیک + Shift روی فایل و انتخاب «Copy as path» مسیر را کپی کنید.
- **تعریف کوتاه:** آدرس یک فایل یا پوشه روی سیستم.

### ۴.۵) Extension — پسوند فایل
- **تعریف:** حروفی که بعد از آخرین نقطه‌ی نام فایل می‌آیند و نوع و فرمت فایل را مشخص می‌کنند: مانند `.txt` (متن)، `.jpg` (عکس)، `.mp4` (ویدیو)، `.exe` (اجرایی).
- **آموزش و کاربرد:** ویندوز به‌صورت پیش‌فرض پسوندها را مخفی می‌کند؛ برای نمایش آن‌ها در File Explorer گزینه‌ی **File name extensions** را فعال کنید — این کار برای امنیت هم مهم است، چون فایل خطرناکی مثل «عکس.jpg.exe» با این ترفند لو می‌رود. پسوند اشتباه، برنامه‌ی بازکننده‌ی اشتباه را به همراه دارد.
- **تعریف کوتاه:** نشانه‌ی نوع فایل بعد از نقطه (.pdf، .exe و...).

### ۴.۶) ZIP — زیپ
- **تعریف:** رایج‌ترین فرمت فشرده‌سازی فایل‌ها برای کم‌کردن حجم و بسته‌بندی چند فایل در یک فایل واحد.
- **آموزش و کاربرد:** ویندوز و مک ZIP را بدون هیچ نرم‌افزاری باز می‌کنند: راست‌کلیک ← **Extract All**. برای ساخت: راست‌کلیک ← Send to ← Compressed folder. فایل‌های jpg و mp4 از قبل فشرده‌اند و زیپ‌شدنشان حجم را زیاد کم نمی‌کند.
- **تعریف کوتاه:** فرمت فشرده‌سازی رایج و هماهنگ با همه‌جا.

### ۴.۷) RAR — رار
- **تعریف:** فرمت فشرده‌سازی اختصاصی نرم‌افزار WinRAR با نسبت فشرده‌سازی بالاتر و امکاناتی مثل رمزگذاری، تقسیم فایل (Split) و ریکاوری در برابر خرابی.
- **آموزش و کاربرد:** برای **ساخت** فایل RAR به WinRAR نیاز دارید (نرم‌افزار پولی)، اما **بازکردن** آن با ابزارهای رایگان مثل 7-Zip هم ممکن است. در ایران بسیاری از فایل‌های دانلودی با پسوندهای .rar و .part1.rar توزیع می‌شوند (نسخه‌ی تقسیم‌شده برای دانلود راحت‌تر).
- **تعریف کوتاه:** فرمت فشرده‌سازی قدرتمند با قابلیت‌های پیشرفته.

### ۴.۸) ISO — آیزو
- **تعریف:** فایل تصویری (Image) کامل و دقیق از یک دیسک نوری (CD/DVD) شامل تمام ساختار و محتوای آن در قالب یک فایل واحد.
- **آموزش و کاربرد:** فایل‌های نصب ویندوز و توزیع‌های لینوکس به صورت ISO منتشر می‌شوند. در ویندوز ۱۰/۱۱ کافی است روی ISO دابل‌کلیک کنید تا مثل یک DVD مجازی **Mount** شود. برای ریختن ISO روی فلش (بوتیبل‌کردن) از ابزار **Rufus** استفاده کنید — کپی ساده‌ی فایل کافی نیست!
- **تعریف کوتاه:** تصویر کامل یک دیسک در قالب یک فایل.

### ۴.۹) Shortcut — میانبر
- **تعریف:** فایل کوچکی که فقط به فایل، پوشه یا برنامه‌ی اصلی اشاره می‌کند تا بدون رفتن به محل اصلی، سریع به آن دسترسی داشته باشید.
- **آموزش و کاربرد:** آیکن‌های دسکتاپ معمولاً Shortcut هستند (فلش کوچک گوشه‌ی آیکن نشانه‌اش است). نکته‌ی مهم: **حذف Shortcut برنامه را پاک نمی‌کند** — فقط لینکش از بین می‌رود. برای حذف واقعی برنامه باید از Control Panel یا Settings ← Apps استفاده کنید.
- **تعریف کوتاه:** لینک دسترسی سریع به فایل یا برنامه، نه خودشان.

### ۴.۱۰) Hidden File — فایل مخفی
- **تعریف:** فایل یا پوشه‌ای که ویژگی «مخفی» (Hidden) آن فعال شده تا در نمایش عادی دیده نشود؛ معمولاً برای محافظت از فایل‌های سیستمی یا حریم خصوصی.
- **آموزش و کاربرد:** برای دیدن فایل‌های مخفی در ویندوز: File Explorer ← تب View ← تیک **Hidden items**. ⚠️ فایل‌های مخفی سیستمی را دست‌کاری یا حذف نکنید، چون برای کارکرد ویندوز لازم‌اند. برخی ویروس‌ها هم خود را مخفی می‌کنند.
- **تعریف کوتاه:** فایلی که عمداً از دید پنهان شده است.

### ۴.۱۱) Read-Only — فقط‌خواندنی
- **تعریف:** ویژگی فایلی که اجازه‌ی مشاهده و خواندن را می‌دهد اما اجازه‌ی تغییر، ذخیره روی همان فایل یا حذف آن را نمی‌دهد.
- **آموزش و کاربرد:** برای فعال/غیرفعال‌کردن: راست‌کلیک روی فایل ← Properties ← تیک **Read-only**. برای تغییر فایل فقط‌خواندنی، باید «Save As» کنید و آن را با نام دیگر ذخیره کنید. فایل‌های روی CD/DVD ذاتاً فقط‌خواندنی‌اند.
- **تعریف کوتاه:** فایلی که فقط می‌توان خواند، نه تغییر داد.

### ۴.۱۲) Permission — مجوز / دسترسی
- **تعریف:** مجموعه قواعدی که مشخص می‌کند هر کاربر چه کاری با یک فایل یا پوشه می‌تواند انجام دهد: خواندن (Read)، نوشتن/ویرایش (Write) یا اجرا (Execute).
- **آموزش و کاربرد:** در ویندوز با راست‌کلیک ← Properties ← تب **Security** مجوزها را می‌بینید. در لینوکس با دستور `chmod` تغییر می‌دهند؛ مثلاً سطح ۷۵۵ یعنی: مالک همه‌کاری، بقیه فقط خواندن و اجرا. خطای معروف «Access Denied» یعنی مجوز کافی ندارید.
- **تعریف کوتاه:** تعیین سطح دسترسی کاربران به فایل‌ها.

✅ **پایان فصل ۴**

---

<a id="chapter5"></a>
# فصل ۵ | شبکه 🌐

شبکه یعنی اتصال کامپیوترها به یکدیگر برای اشتراک داده و منابع. این فصل شما را از کابل اترنت تا اینترنت و VPN همراهی می‌کند.

---

### ۵.۱) Network — شبکه
- **تعریف:** مجموعه‌ای از دو یا چند دستگاه (کامپیوتر، گوشی، پرینتر و...) که برای تبادل داده و اشتراک‌گذاری منابع به هم متصل شده‌اند — به صورت سیمی یا بی‌سیم.
- **آموزش و کاربرد:** ساده‌ترین شبکه، اتصال گوشی و لپ‌تاپ شما به مودم خانه است. شبکه سه هدف اصلی دارد: **اشتراک داده**، **اشتراک سخت‌افزار** (مثل پرینتر مشترک) و **ارتباط** (پیام و تماس).
- **تعریف کوتاه:** مجموعه دستگاه‌های متصل برای تبادل داده.

### ۵.۲) LAN — شبکه‌ی محلی
- **تعریف:** شبکه‌ای با محدوده‌ی جغرافیایی کوچک — یک خانه، دفتر یا ساختمان — که معمولاً متعلق به یک سازمان یا فرد است.
- **آموزش و کاربرد:** وقتی دو کامپیوتر در خانه از طریق یک مودم/روتر فایل ردوبدل می‌کنند، یک LAN ساخته‌اید. مزیت LAN سرعت بالا (تا ۱ گیگابیت و بیشتر با کابل) و امنیت نسبی است. بازی‌های LAN و انتقال فایل بدون اینترنت از کاربردهای معروف‌اند.
- **تعریف کوتاه:** شبکه‌ی کوچک و محلی (خانه یا اداره).

### ۵.۳) WAN — شبکه‌ی گسترده
- **تعریف:** شبکه‌ای که محدوده‌ی جغرافیایی بزرگی — شهرها، کشورها و قاره‌ها — را پوشش می‌دهد و معمولاً از اتصال چند LAN به یکدیگر ساخته می‌شود.
- **آموزش و کاربرد:** بزرگ‌ترین WAN جهان همان **اینترنت** است. شرکت‌های چندشعبه‌ای شعب خود را از طریق WAN (با لینک‌های اختصاصی یا VPN) به دفتر مرکزی وصل می‌کنند. در مودم خانه، پورت آبی معمولاً «WAN» است: یعنی ورودی اینترنت از شرکت سرویس‌دهنده.
- **تعریف کوتاه:** شبکه‌ی بزرگ در مقیاس شهری و بین‌المللی.

### ۵.۴) Internet — اینترنت
- **تعریف:** شبکه‌ی جهانیِ متشکل از میلیون‌ها شبکه‌ی به‌هم‌پیوسته که با زبان مشترک (مجموعه پروتکل‌های TCP/IP) با هم صحبت می‌کنند.
- **آموزش و کاربرد:** اینترنت «زیرساخت» است و خدماتی مثل وب (WWW)، ایمیل و پیام‌رسان‌ها **روی** آن سوار می‌شوند — پس وب و اینترنت یک چیز نیستند! شرکت سرویس‌دهنده‌ی اینترنت شما **ISP** نام دارد (مثل مخابرات یا های‌وب).
- **تعریف کوتاه:** شبکه‌ی جهانی به‌هم‌پیوسته‌ی همه‌ی شبکه‌ها.

### ۵.۵) Router — روتر / مسیریاب
- **تعریف:** دستگاهی که چند شبکه را به هم متصل می‌کند و بهترین مسیر را برای رسیدن بسته‌های داده از مبدأ به مقصد انتخاب می‌کند.
- **آموزش و کاربرد:** مودم-روتر خانگی شما دو کار را هم‌زمان می‌کند: شبکه‌ی خانه (LAN) را می‌سازد و آن را به اینترنت (WAN) وصل می‌کند. برای ورود به تنظیمات روتر معمولاً آدرس `192.168.1.1` را در مرورگر می‌زنید. کلمات عبور پیش‌فرض روتر را حتماً تغییر دهید!
- **تعریف کوتاه:** مسیریاب بین شبکه‌ها و دروازه‌ی اتصال به اینترنت.

### ۵.۶) Switch — سوییچ
- **تعریف:** دستگاهی که چند دستگاه را درون یک شبکه‌ی محلی به هم وصل می‌کند و داده را **هوشمندانه** فقط به پورت دستگاه مقصد می‌فرستد (با استفاده از MAC Address).
- **آموزش و کاربرد:** سوییچ مثل مأمور راهنمایی‌ورانندگی داده‌هاست؛ برخلاف هاب، ترافیک اضافی ایجاد نمی‌کند. در ادارات برای اتصال ده‌ها کامپیوتر به شبکه از سوییچ استفاده می‌شود. سوییچ‌های «مدیریتی (Managed)» امکان ساخت شبکه‌های مجازی (VLAN) را هم می‌دهند.
- **تعریف کوتاه:** توزیع‌کننده‌ی هوشمند داده در داخل شبکه‌ی محلی.

### ۵.۷) Hub — هاب
- **تعریف:** دستگاه قدیمی اتصال دستگاه‌ها در شبکه که هر داده‌ای را از هر پورتی دریافت می‌کرد، کورکورانه به **همه‌ی** پورت‌ها می‌فرستاد.
- **آموزش و کاربرد:** هاب تقریباً منسوخ شده و جایش را سوییچ گرفته، چون: ۱) امنیت کمتر — همه داده‌ی همدیگر را می‌بینند، ۲) تداخل و کندی — با چند دستگاه فعال، شبکه شلوغ می‌شود. اگر در سوالی از مقایسه‌ی هاب و سوییچ پرسیدند: هاب «فریاد می‌زند»، سوییچ «نامه می‌رساند».
- **تعریف کوتاه:** توزیع‌کننده‌ی ساده و منسوخ داده به همه.

### ۵.۸) Modem — مودم
- **تعریف:** دستگاهی که سیگنال خط ارتباطی شرکت اینترنت (تلفن، کابل یا فیبر) را به سیگنال قابل فهم برای شبکه‌ی خانگی تبدیل می‌کند و برعکس (مدولاتور-دمدولاتور).
- **آموزش و کاربرد:** فرق کلیدی: **مودم** دروازه‌ی ورود اینترنت به خانه است (ارتباط با ISP) و **روتر** آن را بین دستگاه‌ها تقسیم می‌کند؛ امروزه این دو معمولاً در یک دستگاه ترکیب شده‌اند. اگر اینترنت قطع شد، اولین کار «ری‌استارت مودم» معروف است — چون اتصال منطقی با ISP از نو برقرار می‌شود.
- **تعریف کوتاه:** مترجم سیگنال بین خانه و شرکت اینترنت.

### ۵.۹) Wi-Fi — وای‌فای
- **تعریف:** فناوری شبکه‌ی بی‌سیم محلی بر پایه‌ی امواج رادیویی (استاندارد IEEE 802.11) که دستگاه‌ها را بدون کابل به شبکه وصل می‌کند.
- **آموزش و کاربرد:** نسل‌های Wi-Fi: Wi-Fi 5 (802.11ac)، Wi-Fi 6/6E و Wi-Fi 7 که هر نسل سریع‌تر و پایدارتر است. باند ۲٫۴ گیگاهرتز برد بیشتر و باند ۵ گیگاهرتز سرعت بیشتری دارد. نکته‌ی امنیتی: رمز وای‌فای را از نوع **WPA2 یا WPA3** انتخاب کنید و با رمز قوی محافظت کنید.
- **تعریف کوتاه:** شبکه‌ی بی‌سیم خانگی و اداری.

### ۵.۱۰) Ethernet — اترنت
- **تعریف:** استاندارد رایج شبکه‌های سیمی که با کابل‌های شبکه (مثل Cat5e و Cat6) و سوکت RJ45 پیاده‌سازی می‌شود.
- **آموزش و کاربرد:** اتصال سیمی اترنت معمولاً از وای‌فای **سریع‌تر، پایدارتر و کم‌تأخیرتر** است — برای بازی آنلاین، تماس ویدیویی مهم و انتقال فایل‌های سنگین بهترین انتخاب. درگاه‌های رایج: ۱۰۰ مگابیت (Fast Ethernet) و ۱ گیگابیت (Gigabit Ethernet).
- **تعریف کوتاه:** شبکه‌ی سیمی استاندارد با کابل LAN.

### ۵.۱۱) IP Address — آدرس آی‌پی
- **تعریف:** شناسه‌ی عددی یکتا برای هر دستگاه در شبکه تا بسته‌های داده بتوانند صاحب‌شان را پیدا کنند؛ دو نسخه دارد: **IPv4** (مثل 192.168.1.10) و نسل جدید **IPv6** برای جبران کمبود آدرس.
- **آموزش و کاربرد:** دو نوع رایج: **IP خصوصی** درون شبکه‌ی خانه (مثل 192.168.x.x) و **IP عمومی** که در اینترنت می‌بینند و ISP به شما می‌دهد. برای دیدن IP خود در ویندوز `ipconfig` را بزنید و برای دیدن IP عمومی در گوگل بنویسید «my ip». آدرس‌هایی مثل 192.168.1.1 معمولاً مال خودِ روتر است.
- **تعریف کوتاه:** شناسه‌ی عددی هر دستگاه در شبکه.

### ۵.۱۲) MAC Address — آدرس مک
- **تعریف:** شناسه‌ی فیزیکی و از پیش ثبت‌شده‌ی کارت شبکه‌ی هر دستگاه (۴۸ بیتی) که توسط سازنده روی تراشه حک شده و در شبکه‌ی محلی برای شناسایی دستگاه استفاده می‌شود.
- **آموزش و کاربرد:** نمونه: `3C:52:82:9F:1A:4B`. برخلاف IP که می‌تواند عوض شود، MAC ثابت سخت‌افزاری است — به همین دلیل روترها قابلیت «فیلتر مک» دارند (فقط دستگاه‌های مجاز وصل شوند) و سوییچ با جدول مک تصمیم می‌گیرد بسته را به کدام پورت بفرستد. در ویندوز با `getmac` یا در تنظیمات وای‌فای دیده می‌شود.
- **تعریف کوتاه:** شناسه‌ی فیزیکی و ثابتِ کارت شبکه.

### ۵.۱۳) DNS — دی‌ان‌اس
- **تعریف:** سامانه‌ی نام دامنه؛ مترجمی که نام‌های خوانا مثل google.com را به آدرس IP ماشین‌ها تبدیل می‌کند، چون انسان‌ها نام را و ماشین‌ها عدد را بهتر به خاطر می‌سپارند.
- **آموزش و کاربرد:** DNS «دفترچه تلفن اینترنت» است. سرورهای DNS معروف: گوگل `8.8.8.8` و کلودفلر `1.1.1.1` — تغییر DNS در تنظیمات گاهی سرعت باز شدن سایت‌ها را بهتر یا برخی محدودیت‌های سطح DNS را دور می‌زند. اگر «سایت نمی‌آید ولی اینترنت وصل است»، مشکل از DNS است.
- **تعریف کوتاه:** مترجم نام سایت به آدرس IP.

### ۵.۱۴) DHCP — دی‌اچ‌سی‌پی
- **تعریف:** پروتکلی که به صورت خودکار به هر دستگاه تازه‌واردشده به شبکه، آدرس IP و تنظیمات شبکه (درگاه، DNS) را اجاره می‌دهد.
- **آموزش و کاربرد:** به لطف DHCP لازم نیست برای هر گوشی یا لپ‌تاپ دستی IP تعریف کنید — روتر خانه مسئول DHCP است. این آدرس‌ها «اجاره‌ای» (Lease) هستند و بعد از مدتی تمدید یا عوض می‌شوند. برای دستگاه‌های ثابت مثل پرینتر و سرور معمولاً IP ثابت (Static) ترجیح داده می‌شود.
- **تعریف کوتاه:** مسئول تخصیص خودکار آدرس IP به دستگاه‌ها.

### ۵.۱۵) Gateway — دروازه / گیت‌وی
- **تعریف:** نقطه‌ی خروجی شبکه‌ی محلی به شبکه‌های دیگر؛ دستگاهی (معمولاً روتر) که ترافیکِ «بیرون از محله» را دریافت و به مقصد هدایت می‌کند.
- **آموزش و کاربرد:** در خانه، **Default Gateway** شما همان آدرس روتر است (مثل 192.168.1.1). هر بسته‌ای که مقصدش بیرون شبکه‌ی شماست، ابتدا به گیت‌وی سپرده می‌شود. برای دیدن گیت‌وی خود خروجی `ipconfig` را ببینید.
- **تعریف کوتاه:** دروازه‌ی خروج از شبکه‌ی محلی به بیرون.

### ۵.۱۶) VPN — وی‌پی‌ان / شبکه‌ی خصوصی مجازی
- **تعریف:** فناوری ساخت یک «تونل رمزنگاری‌شده» روی اینترنت عمومی که ترافیک شما را محرمانه می‌کند و آدرس IP واقعیتان را پشت سرور VPN پنهان می‌کند.
- **آموزش و کاربرد:** کاربردها: امنیت در وای‌فای عمومی (ترافیکتان دزدیده نمی‌شود)، اتصال امن کارمندان به شبکه‌ی اداره از راه دور، و تغییر موقعیت مکانیِ ظاهری IP. نکته‌ی فنی: پروتکل‌های معروف OpenVPN و WireGuard هستند. VPN ناشناسی مطلق نمی‌دهد؛ شرکت VPN جانب اعتماد جدید شماست.
- **تعریف کوتاه:** تونل امن و رمزنگاری‌شده روی اینترنت.

### ۵.۱۷) Proxy — پروکسی / پراکسی
- **تعریف:** سرور واسطی که درخواست‌های شما را به اینترنت می‌برد و پاسخ را برمی‌گرداند؛ یعنی سایت‌ها به‌جای شما، پروکسی را می‌بینند.
- **آموزش و کاربرد:** تفاوت با VPN: پروکسی معمولاً رمزنگاری کامل ندارد و فقط ترافیک یک برنامه (مثل مرورگر) را هدایت می‌کند، ولی VPN کل سیستم را پوشش می‌دهد و رمزنگاری‌اش کامل‌تر است. کاربرد پروکسی در سازمان‌ها: فیلترکردن سایت‌ها، کش‌کردن سرعت و کنترل دسترسی.
- **تعریف کوتاه:** واسطه بین کاربر و اینترنت.

### ۵.۱۸) Bandwidth — پهنای باند
- **تعریف:** حداکثر ظرفیت انتقال داده‌ی یک اتصال در واحد زمان، که معمولاً با مگابیت بر ثانیه (Mbps) یا گیگابیت بر ثانیه (Gbps) بیان می‌شود.
- **آموزش و کاربرد:** پهنای باند را مثل «عرض بزرگراه» تصور کنید، نه سرعت ماشین: بزرگراه ۸ خطه ماشین‌های بیشتری را هم‌زمان جابه‌جا می‌کند。 سرویس «۱۰۰ مگابیتی» یعنی حداکثر ۱۰۰ مگابیت در ثانیه ≈ ۱۲٫۵ مگابایت دانلود در ثانیه. تماشای 4K حدود ۲۵ مگابیت پهنای باند می‌خواهد.
- **تعریف کوتاه:** ظرفیت کانال انتقال داده (چند مگابیت در ثانیه).

### ۵.۱۹) Latency — تأخیر / پینگ
- **تعریف:** مدت‌زمان رفت‌وآمد یک بسته از دستگاه شما به مقصد و برگشت، بر حسب میلی‌ثانیه (ms).
- **آموزش و کاربرد:** با دستور `ping` تأخیر را می‌سنجید. برای کاربران معمولی زیر ۵۰ میلی‌ثانیه عالی است؛ گیمرها به زیر ۲۰ حساس‌اند، چون تأخیر یعنی «لگ». نکته: سرعت بالا (Bandwidth) تضمین‌کننده‌ی تأخیر پایین نیست؛ اینترنت ماهواره‌ای پهنای باند خوب اما تأخیر بالایی دارد.
- **تعریف کوتاه:** تأخیر رفت‌وبرگشت داده (ms).

### ۵.۲۰) Packet — بسته / پاکت
- **تعریف:** واحد کوچک و استاندارد داده در شبکه؛ هر داده‌ی بزرگ (عکس، ایمیل، ویدیو) به هزاران بسته‌ی کوچک تبدیل می‌شود که هرکدام آدرس مبدأ و مقصد دارند و پس از عبور مسیرهای مختلف، در مقصد دوباره کنار هم چیده می‌شوند.
- **آموزش و کاربرد:** مزیت بسته‌بندی: اگر چند بسته گم شوند، فقط همان‌ها دوباره فرستاده می‌شوند نه کل فایل. اصطلاح **Packet Loss** (گم‌شدن بسته) مشهورترین علت لگ بازی‌های آنلاین و کیفیت بد تماس‌هاست. ابزار Wireshark بسته‌های شبکه را برای تحلیل نشان می‌دهد.
- **تعریف کوتاه:** تکه‌های کوچک داده که در شبکه سفر می‌کنند.

✅ **پایان فصل ۵**

<a id="chapter6"></a>
# فصل ۶ | وب و اینترنت 🔎

وب (World Wide Web) معروف‌ترین خدمت روی اینترنت است. در این فصل می‌آموزید سایت‌ها چطور ساخته می‌شوند، مرورگر چه می‌کند و HTTPS چرا مهم است.

---

### ۶.۱) Website — وب‌سایت
- **تعریف:** مجموعه‌ای از صفحات وب مرتبط به هم که زیر یک نام دامنه‌ی مشخص در اینترنت قرار گرفته‌اند و روی یک سرور نگهداری می‌شوند.
- **آموزش و کاربرد:** یک سایت = یک «ساختمان» با آدرس مشخص (دامنه) که اتاق‌های آن صفحات وب‌اند. سایت‌ها انواع مختلفی دارند: خبری، فروشگاهی، آموزشی و... . امروزه با ابزارهایی مثل WordPress بدون برنامه‌نویسی هم می‌توان سایت ساخت.
- **تعریف کوتاه:** مجموعه صفحات اینترنتی زیر یک دامنه.

### ۶.۲) Web Page — صفحه‌ی وب
- **تعریف:** یک سند منفرد از وب که با مرورگر نمایش داده می‌شود و می‌تواند متن، تصویر، ویدیو و لینک داشته باشد؛ هر صفحه آدرس یکتای خودش (URL) را دارد.
- **آموزش و کاربرد:** صفحه‌ی اصلی سایت **Homepage** نام دارد و بقیه‌ی صفحات با «لینک (Hyperlink)» به هم وصل‌اند. با کلید **F12** مرورگر می‌توانید کدهای تشکیل‌دهنده‌ی هر صفحه (HTML/CSS/JS) را ببینید — بهترین راه یادگیری!
- **تعریف کوتاه:** یک صفحه‌ی منفرد از یک سایت.

### ۶.۳) Browser — مرورگر
- **تعریف:** نرم‌افزاری برای درخواست، دریافت و نمایش صفحات وب؛ مثل Chrome، Firefox، Edge و Safari.
- **آموزش و کاربرد:** کار مرورگر: URL را می‌گیرد → از DNS آدرس سرور را پیدا می‌کند → فایل‌های HTML/CSS/JS تصویرها را می‌گیرد → صفحه را رندر و نمایش می‌دهد. میانبرهای مفید: **Ctrl+T** (تب جدید)، **Ctrl+Shift+N** (حالت ناشناس)، **Ctrl+D** (نشانک). مرورگر خودش «موتور جستجو» نیست؛ گوگل داخل مرورگر اجرا می‌شود.
- **تعریف کوتاه:** پنجره‌ی دیدن و گشتن در وب.

### ۶.۴) Search Engine — موتور جستجو
- **تعریف:** سامانه‌ای که با ربات‌های خزنده (Crawler) صفحات وب را جمع و فهرست (Index) می‌کند و با الگوریتم‌های رتبه‌بندی، مرتبط‌ترین نتایج را برای عبارت جستجوی شما نشان می‌دهد.
- **آموزش و کاربرد:** معروف‌ترین‌ها: Google، Bing و DuckDuckGo (تمرکز بر حریم خصوصی). ترفند جستجو: عبارت دقیق را داخل دوباله بگذارید ("عبارت")، کلمه‌ی ناخواسته را با علامت‌ی منها حذف کنید (-tehran) و جستجو در یک سایت خاص با `site:example.com`.
- **تعریف کوتاه:** جستجوگر و فهرست‌کننده‌ی اینترنت.

### ۶.۵) URL — نشانی اینترنتی
- **تعریف:** آدرس کامل و یکتای هر منبع در وب (صفحه، فایل، تصویر) که ساختار مشخصی دارد: `پروتکل :// دامنه / مسیر`
- **آموزش و کاربرد:** اجزای `https://www.shop.com/products?id=25` را بشناسید: `https://` پروتکل، `shop.com` دامنه، `/products` مسیر و `?id=25` پارامتر. قبل از کلیک روی هر لینک، آن را نگاه کنید — حمله‌های فیشینگ با URLهای مشابه (مثل gooogle.com با سه o) فریب می‌دهند.
- **تعریف کوتاه:** آدرس دقیق یک صفحه یا فایل در وب.

### ۶.۶) Domain — دامنه
- **تعریف:** نام یکتا و خوانای هر سایت در اینترنت (مثل google.com) که در واقع جایگزین انسانیِ آدرس IP سرور است.
- **آموزش و کاربرد:** اجزای دامنه: نام (google) + پسوند (.com). پسوندها (TLD): عمومی مثل .com و .org، کشوری مثل .ir و .de. دامنه‌ها «اجاره» می‌شوند و باید سالانه تمدید شوند. زیردامنه (Subdomain) بخشی قبل از دامنه است: mail.google.com
- **تعریف کوتاه:** نام یکتای سایت در اینترنت.

### ۶.۷) Hosting — هاست / میزبانی وب
- **تعریف:** فضا و منابعی روی سرور که فایل‌ها و داده‌های سایت شما را نگه می‌دارد تا همیشه در اینترنت در دسترس باشد.
- **آموزش و کاربرد:** سه عنصر لازم برای داشتن سایت: **دامنه** (آدرس) + **هاست** (زمین و ساختمان) + **طراحی سایت**. انواع هاست: اشتراکی (ارزان، مناسب شروع)، VPS (سرور مجازی اختصاصی‌تر) و اختصاصی/ابری (حرفه‌ای). شرکت‌های هاستینگ با «آپ‌تایم (Uptime)» می‌سنجند که سایت شما چند درصد زمان در دسترس است.
- **تعریف کوتاه:** جای نگهداری فایل‌های سایت روی سرور.

### ۶.۸) HTTP — اچ‌تی‌تی‌پی
- **تعریف:** پروتکل انتقال ابرمتن؛ قوانین گفت‌وگو بین مرورگر و سرور وب برای درخواست و دریافت صفحات. نسخه‌ی اولیه‌ی آن داده را **بدون رمزنگاری** می‌فرستد.
- **آموزش و کاربرد:** مدل کار: مرورگر «درخواست (Request)» می‌فرستد و سرور «پاسخ (Response)» می‌دهد. کدهای وضعیت معروف را بشناسید: **200** = موفق، **404** = صفحه پیدا نشد، **500** = خطای سرور. چون HTTP رمزنگاری ندارد، رمز عبور را هرگز در سایت‌های http:// وارد نکنید.
- **تعریف کوتاه:** پروتکل پایه‌ی انتقال صفحات وب (بدون رمزنگاری).

### ۶.۹) HTTPS — اچ‌تی‌تی‌پی‌اس
- **تعریف:** نسخه‌ی امن HTTP که ارتباط بین مرورگر و سرور را با فناوری SSL/TLS **رمزنگاری** می‌کند تا شنود و دست‌کاری داده ناممکن شود.
- **آموزش و کاربرد:** نشانه‌اش قفل 🔒 کنار آدرس مرورگر است. امروزه تقریباً همه‌ی سایت‌های معتبر HTTPS هستند و گوگل سایت‌های غیرامن را جریمه می‌کند. نکته‌ی منفی: HTTPS فقط «ارتباط» را امن می‌کند، معتبر‌بودن «محتوای» سایت را تضمین نمی‌کند؛ یک سایت فیشینگ هم می‌تواند قفل داشته باشد!
- **تعریف کوتاه:** نسخه‌ی امن و رمزنگاری‌شده‌ی HTTP.

### ۶.۱۰) Cookie — کوکی
- **تعریف:** فایل‌های متنی کوچکی که سایت‌ها در مرورگر شما ذخیره می‌کنند تا شما را بشناسند: وضعیت ورود، سبد خرید، تنظیمات زبان و... 
- **آموزش و کاربرد:** انواع: کوکی **طرف اول** (از خودِ سایت) و کوکی **طرف سوم** (ردیابی تبلیغاتی بین چند سایت). بنر معروف «Accept Cookies» محصول قانون حریم خصوصی اروپا (GDPR) است. پاک کردن کوکی‌ها باعث خروج از حساب‌ها می‌شود، اما ردیابی‌ها را هم پاک می‌کند.
- **تعریف کوتاه:** حافظه‌ی کوچک سایت‌ها در مرورگر شما برای شناختنتان.

### ۶.۱۱) Cache — کش مرورگر
- **تعریف:** ذخیره‌ی موقت بخش‌هایی از سایت (عکس‌ها، استایل‌ها، اسکریپت‌ها) روی دستگاه شما تا در بازدید دوباره، صفحه سریع‌تر و با مصرف اینترنت کمتر باز شود.
- **آموزش و کاربرد:** تفاوت با کوکی: کش «فایل‌های سایت» را و کوکی «اطلاعات شما» را نگه می‌دارد. وقتی سایت تازه به‌روز شده ولی نسخه‌ی قدیمی را می‌بینید، مشکل کش است؛ با **Ctrl+F5** (به‌روزرسانی اجباری بدون کش) حلش کنید. پاک کردن کش از تنظیمات مرورگر بخشی از اینترنت را آزادتر مصرف می‌کند.
- **تعریف کوتاه:** حافظه‌ی موقت مرورگر برای سرعت بیشتر.

### ۶.۱۲) Session — نشست / جلسه
- **تعریف:** دوره‌ی پیوسته‌ی تعامل شما با یک سایت یا برنامه، از لحظه‌ی ورود تا خروج یا اتمام مهلت؛ سرور از طریق یک شناسه‌ی نشست (Session ID) شما را در همان بازه می‌شناسد.
- **آموزش و کاربرد:** وقتی در سایت بانکی وارد می‌شوید و بین صفحات جابه‌جا می‌شوید، لازم نیست هر بار رمز بزنید چون «نشست» فعال است. برای امنیت، نشست‌ها بعد از چند دقیقه بی‌فعالی منقضی می‌شوند — به همین دلیل در سیستم‌های حساس همیشه دکمه‌ی Logout را بزنید، نه فقط بستن تب.
- **تعریف کوتاه:** دوره‌ی یک اتصال فعال کاربر با سایت.

### ۶.۱۳) SSL / TLS — اس‌اس‌ال
- **تعریف:** فناوری رمزنگاری استاندارد ارتباطات وب که تضمین می‌کند داده بین مرورگر و سرور محرمانه و دست‌نخورده می‌ماند. SSL نسخه‌ی قدیمی است و امروزه جانشین امن‌تر آن، **TLS** استفاده می‌شود.
- **آموزش و کاربرد:** سایت‌ها برای فعال شدن HTTPS به «گواهی SSL» نیاز دارند که مرجع معتبر (CA) صادر می‌کند و هویت سایت را تأیید می‌کند. گواهی رایگان معروف: Let's Encrypt. اگر مرورگر خطای «گواهی نامعتبر» نشان داد، جدی بگیرید — ممکن است ارتباطتان شنود شود.
- **تعریف کوتاه:** فناوری رمزنگاری و احراز هویت ارتباط امن وب.

### ۶.۱۴) HTML — اچ‌تی‌ام‌ال
- **تعریف:** زبان نشانه‌گذاری استاندارد ساختار و محتوای صفحات وب (متن، تیترها، تصاویر، لینک‌ها)؛ هر صفحه‌ی وب از تگ‌های HTML ساخته می‌شود.
- **آموزش و کاربرد:** HTML «اسکلت» صفحه است، نه زبان برنامه‌نویسی. چند تگ معروف: `<h1>` تیتر، `<p>` پاراگراف، `<a>` لینک، `<img>` تصویر. برای شروع یادگیری طراحی وب، HTML اولین قدم است و ظرف چند روز قابل یادگیری است. کد هر صفحه را با F12 ببینید.
- **تعریف کوتاه:** زبان ساختار و اسکلت صفحات وب.

### ۶.۱۵) CSS — سی‌اس‌اس
- **تعریف:** زبان استایل‌دهی صفحات وب که ظاهر HTML را می‌سازد: رنگ‌ها، فونت‌ها، اندازه‌ها، چیدمان (Layout) و انیمیشن‌ها.
- **آموزش و کاربرد:** اگر HTML اسکلت باشد، CSS «لباس و آرایش» صفحه است. مفاهیم کلیدی آن: انتخابگر (Selector)، Box Model (حاشیه و فاصله)، و **Responsive Design** (واکنش‌گرایی — یعنی سایت در گوشی و کامپیوتر خوب به نظر برسد).
- **تعریف کوتاه:** زبان ظاهر و طراحی دیداری صفحات وب.

### ۶.۱۶) JavaScript — جاوااسکریپت
- **تعریف:** زبان برنامه‌نویسی وب که به صفحات «رفتار و تعامل» می‌بخشد: اعتبارسنجی فرم، منوهای متحرک، به‌روزرسانی صفحه بدون رفرش و هزاران کار دیگر — درست داخل مرورگر.
- **آموزش و کاربرد:** مثلث طلایی وب: **HTML** (اسکلت) + **CSS** (ظاهر) + **JavaScript** (مغز و رفتار). امروزه جاوااسکریپت با Node.js روی سرور هم اجرا می‌شود و یکی از پرتقاضاترین زبان‌های بازار کار جهان است. توجه: جاوااسکریپت با زبان «جاوا» هیچ ارتباط محتوایی ندارد — فقط اسمشان شبیه است!
- **تعریف کوتاه:** زبان برنامه‌نویسیِ تعامل و پویایی صفحات وب.

✅ **پایان فصل ۶**

---

<a id="chapter7"></a>
# فصل ۷ | برنامه‌نویسی 💻

برنامه‌نویسی یعنی صحبت کردن با کامپیوتر به زبان خودش. این فصل ابزارهای اصلی این حرفه — از الگوریتم تا گیت — را پوشش می‌دهد.

---

### ۷.۱) Algorithm — الگوریتم
- **تعریف:** راه‌حل گام‌به‌گام، دقیق و متناهی برای حل یک مسئله؛ مجموعه دستورهایی که اگر به ترتیب اجرا شوند، به جواب می‌رسند.
- **آموزش و کاربرد:** دستور پختِ آشپزی یک الگوریتم است: مواد (ورودی) → مراحل (دستورها) → غذا (خروجی). قبل از نوشتن کد، برنامه‌نویسان حرفه‌ای اول الگوریتم را روی کاغذ طراحی می‌کنند (فلوچارت یا شبه‌کد). تمرین: الگوریتم «پیدا کردن بزرگ‌ترین عدد بین سه عدد» را روی کاغذ بنویسید.
- **تعریف کوتاه:** دستورالعمل قدم‌به‌قدم حل مسئله.

### ۷.۲) Programming Language — زبان برنامه‌نویسی
- **تعریف:** زبان رسمی و قاعده‌مندی که برنامه‌نویس با آن دستورهای ماشین را می‌نویسد؛ مثل Python، Java، C++ و JavaScript.
- **آموزش و کاربرد:** زبان‌ها دو دسته‌اند: **سطح بالا** (نزدیک به زبان انسان، مثل پایتون — یادگیری آسان) و **سطح پایین** (نزدیک به ماشین، مثل اسمبلی — کنترل بیشتر). برای شروع، **پایتون** بهترین انتخاب است: سینتکس ساده و کاربرد گسترده (هوش مصنوعی تا وب). طبق آمارهای جهانی، پایتون، جاوااسکریپت و جاوا همیشه جزو پرتقاضاترین‌ها هستند.
- **تعریف کوتاه:** زبان نوشتن دستورها برای کامپیوتر.

### ۷.۳) Compiler — کامپایلر / مترجم
- **تعریف:** برنامه‌ای که کل کد منبع را **یک‌جا** به زبان ماشین (فایل اجرایی) ترجمه می‌کند؛ بعد از آن برنامه مستقل و سریع اجرا می‌شود. زبان‌هایی مثل C و C++ کامپایلری هستند.
- **آموزش و کاربرد:** مزیت: سرعت اجرای بالا و عدم نیاز به داشتن کد در مقصد؛ خطاها را هم قبل از اجرا می‌گیرد. عیب: اصلاح هر خطای کوچک یعنی کامپایل دوباره‌ی همه‌چیز. کامپایلر را مثل مترجمی ببینید که **کل کتاب** را یک‌جا ترجمه و چاپ می‌کند.
- **تعریف کوتاه:** مترجم یک‌جای کد به زبان ماشین.

### ۷.۴) Interpreter — مفسر
- **تعریف:** برنامه‌ای که کد را **خط‌به‌خط**، هم‌زمان با اجرا، ترجمه و اجرا می‌کند؛ زبان‌هایی مثل Python و JavaScript مفسری‌اند.
- **آموزش و کاربرد:** مزیت: نوشتن و تست سریع — همان لحظه نتیجه را می‌بینید و یادگیری را آسان می‌کند. عیب: سرعت اجرا معمولاً کمتر از کامپایلری است. مفسر مثل مترجم هم‌زمان است: جمله‌به‌جمله ترجمه می‌کند. برخی زبان‌ها (مثل جاوا) ترکیبی‌اند: ابتدا به بایت‌کد کامپایل، سپس تفسیر می‌شوند.
- **تعریف کوتاه:** مترجم و اجراکننده‌ی خط‌به‌خط کد.

### ۷.۵) IDE — محیط توسعه‌ی یکپارچه
- **تعریف:** نرم‌افزار همه‌کاره‌ی برنامه‌نویسی که ویرایشگر کد، رنگی‌کردن سینتکس، تکمیل خودکار، دیباگر و اجرای برنامه را یک‌جا جمع کرده است.
- **آموزش و کاربرد:** IDEهای معروف: **VS Code** (سبک و محبوب‌ترین)، **PyCharm** (پایتون)، **IntelliJ** (جاوا) و **Visual Studio** (دات‌نت). امکانات کلیدی که یادگیریشان سرعتتان را دوبرابر می‌کند: تکمیل خودکار، پیدایش خطاهای زیر خط سرخ، و اجرا با یک کلیک.
- **تعریف کوتاه:** کارگاه کامل برنامه‌نویس در یک نرم‌افزار.

### ۷.۶) SDK — کیت توسعه‌ی نرم‌افزار
- **تعریف:** بسته‌ی کاملی از ابزارها، کتابخانه‌ها، مستندات و مثال‌ها که شرکت سازنده‌ی یک پلتفرم ارائه می‌دهد تا برنامه‌نویسان برای آن پلتفرم نرم‌افزار بسازند.
- **آموزش و کاربرد:** مثال‌ها: **Android SDK** برای ساخت اپلیکیشن اندروید، SDK پرداخت برای اتصال فروشگاه به درگاه بانکی. تفاوت با API: API «قرارداد ارتباط» است و SDK «جعبه‌ابزار کامل» که معمولاً API را هم در خود دارد.
- **تعریف کوتاه:** جعبه‌ابزار آماده برای ساخت برنامه روی یک پلتفرم.

### ۷.۷) API — رابط برنامه‌نویسی کاربردی
- **تعریف:** مجموعه قواعدی که به نرم‌افزارها اجازه می‌دهد با یکدیگر «صحبت» کنند و از امکانات همدیگر استفاده کنند، بدون این‌که بدانند داخلش چه می‌گذرد.
- **آموزش و کاربرد:** مثال از زندگی: API مثل «گارسون رستوران» است — شما سفارش (درخواست) می‌دهید، آشپزخانه (سرور) آماده می‌کند و غذا (پاسخ) را می‌آورد. وقتی در سفری هتل کتابتان با گوگل‌مپ موقعیت نشان می‌دهد، از API گوگل استفاده شده. APIهای وب معمولاً بقیه (REST) و با فرمت JSON داده ردوبدل می‌کنند.
- **تعریف کوتاه:** درگاه استاندارد ارتباط دو نرم‌افزار با هم.

### ۷.۸) Framework — چارچوب / فریم‌ورک
- **تعریف:** اسکلت و ساختار از پیش ساخته‌شده‌ای برای توسعه‌ی نرم‌افزار که قواعد، ابزارها و بخش‌های آماده را فراهم می‌کند تا برنامه‌نویس از صفر شروع نکند.
- **آموزش و کاربرد:** فریم‌ورک‌ها: **Django و Flask** (وب با پایتون)، **React** (رابط کاربری)، **Laravel** (PHP) و **.NET** (مایکروسافت). تفاوت کلیدی با کتابخانه: در استفاده از کتابخانه «شما» کد را صدا می‌زنید، ولی در فریم‌ورک فریم‌ورک است که کد شما را صدا می‌زند (وارونگی کنترل).
- **تعریف کوتاه:** چارچوب آماده که ساخت برنامه را سریع و منظم می‌کند.

### ۷.۹) Library — کتابخانه
- **تعریف:** مجموعه‌ای از کدها و توابع آماده و قابل استفاده‌ی مجدد که برنامه‌نویس در پروژه‌اش صدا می‌زند تا چرخ را دوباره اختراع نکند.
- **آموزش و کاربرد:** مثال‌های پرکاربرد: **NumPy و Pandas** (محاسبات و داده در پایتون) و **jQuery** (جاوااسکریپت). هر زمان کاری تکراری و رایج لازم داشتید، اول جستجو کنید «کتابخانه‌ی آماده دارد یا نه» — برنامه‌نویسان حرفه‌ای یعنی استفاده‌ی هوشمند از کدهای قبلی.
- **تعریف کوتاه:** کدهای آماده در اختیار برنامه‌نویس.

### ۷.۱۰) Git — گیت
- **تعریف:** سیستم کنترل نسخه (Version Control) که تمام تاریخچه‌ی تغییرات کد را ثبت می‌کند تا بتوانید به هر مرحله برگردید، تغییرات را مقایسه و تیمی کار کنید. ساخته‌ی لینوس توروالدز است.
- **آموزش و کاربرد:** دستورهای پایه‌ی گیت: `git init` (شروع)، `git add` (آماده‌سازی تغییرات)، `git commit` (ثبت)، `git status` (وضعیت) و `git log` (تاریخچه). بدون گیت، کارتان با فایل‌هایی مثل «final-v2-REALLY-final.zip» تمام می‌شود!
- **تعریف کوتاه:** دفتر ثبت تاریخچه و تغییرات کد.

### ۷.۱۱) GitHub — گیت‌هاب
- **تعریف:** محبوب‌ترین پلتفرم آنلاین میزبانی مخازن گیت که امکان همکاری تیمی، اشتراک‌گذاری کد، گزارش مشکل و بررسی کد (Code Review) را فراهم می‌کند.
- **آموزش و کاربرد:** تفاوت مهم: **گیت ابزار است، گیت‌هاب سایت/سرویس است** (مثل تفاوت دوربین با اینستاگرام). پروفایل گیت‌هاب رزومه‌ی واقعی برنامه‌نویس محسوب می‌شود — کارفرماها آن را می‌بینند. با «Fork» پروژه‌ی دیگران را کپی و با «Pull Request» تغییرتان را پیشنهاد می‌دهید.
- **تعریف کوتاه:** سایت اشتراک و همکاری روی کدهای گیت.

### ۷.۱۲) Repository (Repo) — مخزن / ریپو
- **تعریف:** محل ذخیره‌سازی کامل یک پروژه در گیت: هم فایل‌ها و هم تمام تاریخچه‌ی تغییراتشان — یا روی کامپیوتر (Local) یا روی سرور (Remote).
- **آموزش و کاربرد:** هر ریپو معمولاً فایل **README.md** دارد: معرفی رسمی پروژه که اولین چیزی است که دیگران می‌خوانند. با دستور `git clone` مخزنی از گیت‌هاب را روی کامپیوتر خودتان کپی می‌کنید. ریپوها می‌توانند عمومی یا خصوصی باشند.
- **تعریف کوتاه:** انبار پروژه همراه با تاریخچه‌ی تغییرات.

### ۷.۱۳) Branch — شاخه / برنچ
- **تعریف:** خط توسعه‌ی موازی در مخزن گیت؛ کپی‌ای از کد که می‌توانید روی آن آزادانه تغییر بدهید و بعداً به شاخه‌ی اصلی برگردانید.
- **آموزش و کاربرد:** شاخه‌ی اصلی معمولاً `main` است. الگوی حرفه‌ای کار تیمی: برای هر قابلیت (Feature) برنچ جدیدی بسازید، روی آن کار کنید و بعد **Merge** (ادغام) کنید — بدون این‌که کد اصلی خراب شود. دستورها: `git branch` (لیست) و `git checkout -b feature-x` (ساخت برنچ).
- **تعریف کوتاه:** شاخه‌ی جدا برای توسعه، دور از کد اصلی.

### ۷.۱۴) Commit — کامیت / ثبت
- **تعریف:** ثبت یک نقطه از تاریخچه‌ی پروژه در گیت؛ اسنپ‌شاتی از تغییرات همراه با یک پیام توضیحی و شناسه‌ی یکتا (Hash).
- **آموزش و کاربرد:** قاعده‌ی طلایی کامیت: «زیاد و کوچک کامیت کنید» — مثل ذخیره‌ی بازی در چک‌پوینت‌ها؛ اگر چیزی خراب شد به نقطه‌ی سالم برمی‌گردید. پیام کامیت باید واضح باشد: «افزودن فرم ورود» بهتر از «fix» است. دستور: `git commit -m "پیام"`.
- **تعریف کوتاه:** ذخیره‌ی یک نقطه از تاریخچه‌ی تغییرات کد.

### ۷.۱۵) Debug — اشکال‌زدایی / دیباگ
- **تعریف:** فرایند یافتن، تحلیل و رفع خطاها (باگ‌ها) در برنامه؛ با ابزارهایی به نام **Debugger** انجام می‌شود.
- **آموزش و کاربرد:** تکنیک‌های اصلی دیباگ: گذاشتن **نقطه‌ی توقف (Breakpoint)** تا برنامه خط‌به‌خط اجرا شود، بررسی مقدار متغیرها در حین اجرا، و خواندن دقیق **پیام خطا** (معمولاً شماره‌ی خط و علت را می‌گوید). طنز معروف: «خارجی که مشکل سرویس‌شدن آ نداشتم؛ مشکل این بود که اشتباه می‌فکر کردم.» کپی پیام خطا + جستجو در Google/StackOverflow روش اول همه‌ی برنامه‌نویسان است!
- **تعریف کوتاه:** پیدا کردن و درست کردن خطاهای برنامه.

✅ **پایان فصل ۷**

---

<a id="chapter8"></a>
# فصل ۸ | پایگاه داده 🗄️

پایگاه داده جایی است که اطلاعات برنامه‌ها (کاربران، محصولات، سفارش‌ها) سازمان‌یافته نگهداری می‌شوند. قلب تقریباً هر نرم‌افزار امروزی است.

---

### ۸.۱) Database — پایگاه داده / دیتابیس
- **تعریف:** مجموعه‌ی سازمان‌یافته‌ای از داده‌های مرتبط که به شکلی ذخیره می‌شوند تا دسترسی، جستجو و به‌روزرسانی‌شان سریع و منظم باشد.
- **آموزش و کاربرد:** فهرست مخاطبین گوشی شما یک پایگاه داده‌ی کوچک است؛ فروشگاه اینترنتی پایگاه داده‌ای از محصولات، کاربران و سفارش‌ها دارد. انواع اصلی: **رابطه‌ای** (جدولی — رایج‌ترین) و **غیررابطه‌ای (NoSQL)** برای داده‌های انعطاف‌پذیر و مقیاس بزرگ.
- **تعریف کوتاه:** انبار سازمان‌یافته‌ی داده‌ها.

### ۸.۲) DBMS — سامانه‌ی مدیریت پایگاه داده
- **تعریف:** نرم‌افزاری که امکان ساخت، نگهداری، جستجو و مدیریت پایگاه داده را فراهم می‌کند؛ واسط بین کاربر/برنامه و خودِ داده‌ها.
- **آموزش و کاربرد:** DBMSهای معروف رابطه‌ای: **MySQL** (متن‌باز و پرکاربرد وب)، **PostgreSQL** (قدرتمند و حرفه‌ای)، **SQL Server** (مایکروسافت) و **SQLite** (سبک، داخل اپ‌ها). نمونه‌ی NoSQL: MongoDB. این نرم‌افزارها امنیت، هم‌زمانی کاربران و بکاپ را هم مدیریت می‌کنند.
- **تعریف کوتاه:** نرم‌افزار مدیریت دیتابیس.

### ۸.۳) Table — جدول
- **تعریف:** ساختار اصلی ذخیره‌ی داده در پایگاه داده‌ی رابطه‌ای؛ متشکل از سطرها و ستون‌ها که هر اطلاعاتِ یک «موجودیت» را نگه می‌دارد (مثل جدول مشتریان).
- **آموزش و کاربرد:** جدول را مثل یک صفحه‌ی Excel تصور کنید: جدول «دانش‌آموزان» ستون‌هایی مثل نام، نام خانوادگی و معدل دارد و هر دانش‌آموز یک سطر است. طراحی خوب جداول (نرمال‌سازی) از تکرار و افزونگی داده جلوگیری می‌کند.
- **تعریف کوتاه:** جدول سطروستونی داده‌ها.

### ۸.۴) Record — رکورد / سطر
- **تعریف:** یک سطر کامل از جدول؛ مجموعه‌ی اطلاعات یک موجودیت واحد — مثلاً اطلاعات کاملِ یک مشتری خاص.
- **آموزش و کاربرد:** در جدول «کاربران»، اطلاعات «علی رضایی، تهران، 0912...» تشکیل یک رکورد را می‌دهد. وقتی در کوئری می‌گوییم «۱۰ رکورد برگردان» یعنی ۱۰ سطر نتیجه. معادل رایج دیگرش «Row» است.
- **تعریف کوتاه:** یک سطر داده در جدول.

### ۸.۵) Field — فیلد / ستون
- **تعریف:** یک ستون از جدول؛ مشخصه‌ای مشخص که برای همه‌ی رکوردها تکرار می‌شود و نوع داده‌ی مشخصی دارد (متن، عدد، تاریخ و...).
- **آموزش و کاربرد:** در جدول کاربران، «ایمیل»، «نام کاربری» و «تاریخ تولد» هر کدام یک فیلد هستند. هر فیلد **نوع داده (Data Type)** دارد — مثلاً فیلد سن INT (عدد صحیح) و فیلد نام TEXT است. تعریف صحیح نوع داده از خطاهای بعدی جلوگیری می‌کند.
- **تعریف کوتاه:** یک ستون/مشخصه‌ی جدول.

### ۸.۶) Primary Key — کلید اصلی
- **تعریف:** فیلدی (یا ترکیب چند فیلد) که هر رکورد را به صورت **یکتا** شناسایی می‌کند؛ نمی‌تواند تکراری یا خالی (NULL) باشد و هر جدول معمولاً یکی دارد.
- **آموزش و کاربرد:** کد ملی یا شماره‌ی دانشجویی کلید اصلی خوبی‌اند چون تکراری نمی‌شوند، ولی «نام» کلید بدی است چون تکرار دارد. معمولاً فیلدی عددی و خودافزا (Auto Increment) مثل ID تعریف می‌کنند. کلید اصلی ضامن یکتایی و شاخص‌بندی سریع جدول است.
- **تعریف کوتاه:** شناسه‌ی منحصربه‌فرد هر سطر جدول.

### ۸.۷) Foreign Key — کلید خارجی
- **تعریف:** فیلدی در یک جدول که به **کلید اصلیِ جدول دیگر** اشاره می‌کند و پیوند و ارتباط بین جداول را برقرار می‌سازد.
- **آموزش و کاربرد:** مثال کلاسیک: جدول «سفارش‌ها» فیلد «کد مشتری» دارد که کلید خارجی است و به جدول «مشتریان» وصل می‌شود — پس می‌فهمید هر سفارش مال کیست، بدون تکرار اطلاعات مشتری در هر سفارش. کلید خارجی «جامعیت ارجاعی» را هم تضمین می‌کند: نمی‌توانید سفارش برای مشتری ناقص/حذف‌شده ثبت کنید.
- **تعریف کوتاه:** پیونددهنده‌ی جداول به هم.

### ۸.۸) SQL — اس‌کیوال
- **تعریف:** زبان استاندارد دنیا برای تعریف، پرس‌وجو و مدیریت داده در پایگاه‌های داده‌ی رابطه‌ای.
- **آموزش و کاربرد:** چهار دستور اصلی (عملیات CRUD): `SELECT` (خواندن)، `INSERT` (افزودن)، `UPDATE` (ویرایش) و `DELETE` (حذف). مثال: `SELECT name, city FROM customers WHERE city = 'Tehran';` یعنی نام و شهر مشتریانی که تهرانی‌اند. SQL زبان اعلانی است: «چه می‌خواهید» را می‌گویید نه «چطور» — دانستنش برای هر برنامه‌نویس و تحلیل‌گر داده ضروری است.
- **تعریف کوتاه:** زبان استاندارد کار با دیتابیس‌های رابطه‌ای.

### ۸.۹) Query — کوئری / پرس‌وجو
- **تعریف:** درخواستی که به پایگاه داده فرستاده می‌شود تا داده‌ای را بخواند، تغییر دهد یا حذف کند — معمولاً به زبان SQL نوشته می‌شود.
- **آموزش و کاربرد:** هر سوالی از دیتابیس یک کوئری است: «گران‌ترین محصولات کدامند؟»، «فروش این ماه چقدر بود؟». کوئری‌های کند رایج‌ترین علت کندی سایت‌های بزرگ‌اند و بهینه‌سازی‌شان مهارت مهم شغلی است. خروجی کوئری SELECT همیشه خودش یک «جدول موقت» است.
- **تعریف کوتاه:** درخواست (سوال یا تغییر) ارسالی به دیتابیس.

### ۸.۱۰) Index — ایندکس / شاخص
- **تعریف:** ساختار داده‌ی کمکی که پایگاه داده برای یافتن سریع رکوردها نگه می‌دارد — درست مثل فهرست موضوعیِ آخر کتاب که به جای ورق‌زدن کل کتاب، مستقیم صفحه را می‌دهد.
- **آموزش و کاربرد:** بدون ایندکس، دیتابیس برای هر جستجو مجبور است **کل جدول** را بخواند (Full Table Scan) — در جدول میلیون‌رکوردی یعنی ثانیه‌ها تأخیر. با ایندکس روی فیلدهای پرجستجو (مثل نام کاربری و ایمیل)، جستجو به میلی‌ثانیه می‌رسد. هزینه‌اش: فضای بیشتر و کندیِ نوشتن. کلید اصلی خودبه‌خود ایندکس دارد.
- **تعریف کوتاه:** فهرست تسریع جستجو در دیتابیس.

### ۸.۱۱) View — ویو / نما
- **تعریف:** جدول مجازی که از نتیجه‌ی یک کوئری ذخیره‌شده ساخته می‌شود؛ خودش داده نگه نمی‌دارد بلکه هر بار که صدا زده شود، کوئری‌اش را روی جداول واقعی اجرا می‌کند.
- **آموزش و کاربرد:** کاربردها: ۱) **ساده‌سازی** — کوئری پیچیده‌ی چندجدولی را یک‌بار به صورت ویو ذخیره کنید و همیشه مثل یک جدول ساده بخوانید، ۲) **امنیت** — به کاربر فقط ویویی بدهید که فیلدهای حساس ندارد (مثلاً بدون رمز). چون داده‌نمایان، همیشه تازه‌اند.
- **تعریف کوتاه:** جدول مجازی ساخته‌شده از نتیجه‌ی یک کوئری.

### ۸.۱۲) Transaction — تراکنش
- **تعریف:** مجموعه‌ای از عملیات دیتابیسی که مثل «یک عمل واحد» رفتار می‌شود: یا **همه** با موفقیت انجام می‌شوند یا **هیچ‌کدام** — وضعیت میانی وجود ندارد.
- **آموزش و کاربرد:** مثال کلاسیک «انتقال پول»: کم‌کردن از حساب A + افزودن به حساب B؛ اگر وسط کار سیستم کرش کند و پول کم شده ولی اضافه نشده باشد فاجعه است! تراکنش با **COMMIT** (تأیید نهایی) یا **ROLLBACK** (برگشت کامل) این را تضمین می‌کند. ویژگی‌های تراکنش امن: **ACID** (اتمی، سازگار، ایزوله، ماندگار).
- **تعریف کوتاه:** عملیات «همه یا هیچ» در دیتابیس.

✅ **پایان فصل ۸**

<a id="chapter9"></a>
# فصل ۹ | امنیت سایبری 🛡️

داده‌های ما ارزشمندتر از همیشه‌اند. در این فصل یاد می‌گیرید حمله‌ها چگونه‌اند و چطور از خودتان محافظت کنید.

---

### ۹.۱) Cybersecurity — امنیت سایبری
- **تعریف:** مجموعه‌ی فناوری‌ها، فرایندها و رفتارهایی برای حفاظت از سیستم‌ها، شبکه‌ها و داده‌ها در برابر حمله، سرقت، خرابی و دسترسی غیرمجاز.
- **آموزش و کاربرد:** سه اصل طلایی امنیت اطلاعات (**مثلث CIA**): **محرمانگی** (فقط مجازها ببینند)، **یکپارچگی** (داده دست‌کاری نشود) و **دسترس‌پذیری** (در زمان نیاز در دسترس باشد). مهم‌ترین حلقه‌ی ضعیف اکثر سیستم‌ها «عامل انسانی» است؛ نه فناوری.
- **تعریف کوتاه:** علم و هنر محافظت از دنیای دیجیتال.

### ۹.۲) Encryption — رمزنگاری
- **تعریف:** تبدیل داده‌ی خوانا (متن ساده) به شکلی ناخوانا (Cipher) با استفاده از یک «کلید»، به‌طوری که فقط دارنده‌ی کلید بتواند آن را بخواند.
- **آموزش و کاربرد:** دو نوع اصلی: **متقارن** (یک کلید برای قفل و بازکردن — سریع، مثل AES) و **نامتقارن** (کلید عمومی و خصوصی — پایه‌ی HTTPS و امضای دیجیتال، مثل RSA). رمزنگاری جیب شما هم پر است: واتساپ و تلگرام (چت‌های مخفی) از رمزنگاری سرتاسری (E2EE) استفاده می‌کنند. گوشی‌تان را رمزنگاری (Encrypt) روشن کنید تا در صورت سرقت، داده‌ها امن بمانند.
- **تعریف کوتاه:** قفل‌کردن اطلاعات با کلید رمز.

### ۹.۳) Decryption — رمزگشایی
- **تعریف:** عملیات معکوس رمزنگاری؛ تبدیل داده‌ی رمزشده به شکل اصلی و خوانا با استفاده از کلید صحیح.
- **آموزش و کاربرد:** وقتى پیام رمزشده‌ی شما به گوشی دوستتان می‌رسد، دستگاه او با کلید مناسب آن را رمزگشایی و نمایش می‌دهد. نکته‌ی امنیتی: اگر کلید (یا رمز عبورِ بازکننده‌ی کلید) را گم کنید، رمزگشایی غیرممکن است — به همین دلیل کلید بازیابی کیف پول ارز دیجیتال را باید جای امنی نگه دارید.
- **تعریف کوتاه:** بازکردن قفل اطلاعات رمزشده.

### ۹.۴) Password — رمز عبور / گذرواژه
- **تعریف:** رشته‌ای محرمانه از کاراکترها که برای احراز هویت کاربر و بازکردن حساب یا دستگاه استفاده می‌شود.
- **آموزش و کاربرد:** قواعد رمز قوی: حداقل **۱۲ کاراکتر**، ترکیب بزرگ/کوچک/عدد/نماد، بدون اطلاعات شخصی (تاریخ تولد!)، و **متفاوت برای هر حساب**. از مدیر رمز عبور (Password Manager) مثل Bitwarden استفاده کنید تا یک رمز قویِ واحد کافی باشد. رایج‌ترین رمزهای دنیا هنوز 123456 و password هستند — جزو آن‌ها نباشید!
- **تعریف کوتاه:** کلید ورود شما به حساب‌ها.

### ۹.۵) Hash — هش
- **تعریف:** خروجی یک تابع ریاضیِ یک‌طرفه که از هر داده‌ای یک «اثر انگشت دیجیتال» با طول ثابت می‌سازد؛ یک‌طرفه یعنی از هش **نمی‌توان** به داده‌ی اصلی برگشت.
- **آموزش و کاربرد:** مثال: هش SHA-256 کلمه‌ی «سلام» یک رشته‌ی ۶۴ کاراکتری ثابت است که همیشه برای همان کلمه تکرار می‌شود. کاربردها: ۱) سایت‌های امن **رمز شما را هش** ذخیره می‌کنند، نه خود رمز را — تا در نفوذ هم لو نرود، ۲) بررسی سلامت فایل دانلودشده (کنترل‌سام). الگوریتم‌های MD5 و SHA-1 کهنه و شکسته‌شده‌اند؛ استاندارد امروزی SHA-256 است.
- **تعریف کوتاه:** اثر انگشت دیجیتالِ یک‌طرفه‌ی داده.

### ۹.۶) Firewall — دیوار آتش / فایروال
- **تعریف:** سامانه‌ی امنیتی (سخت‌افزاری یا نرم‌افزاری) که ترافیک ورودی و خروجی شبکه را بر اساس قواعد مشخص بازرسی و اجازه‌ی عبور یا مسدودسازی می‌دهد.
- **آموزش و کاربرد:** فایروال مثل نگهبان درب ساختمان است: برگه‌ی مجوز (Rule) هر بسته را چک می‌کند — از کجا، به کجا، روی کدام پورت. ویندوز فایروال داخلی دارد (**Windows Defender Firewall**) که بهتر است همیشه روشن باشد. در سازمان‌ها، فایروال سخت‌افزاری جلوی کل شبکه می‌ایستد.
- **تعریف کوتاه:** نگهبان و فیلتر ترافیک شبکه.

### ۹.۷) Antivirus — آنتی‌ویروس
- **تعریف:** نرم‌افزاری که بدافزارها را با امضای شناخته‌شده (Signature) و تحلیل رفتاری شناسایی، قرنطینه یا حذف می‌کند.
- **آموزش و کاربرد:** ویندوز ۱۰/۱۱ **Microsoft Defender** رایگان و قابل اعتماد دارد و برای اکثر کاربران کافی است — مهم‌تر از خرید آنتی‌ویروس این است که سیستم و آنتی‌ویروس‌تان همیشه **به‌روز** باشد. آنتی‌ویروس قابلیت «اسکن زمان‌بندی‌شده» و «محافظت بلادرنگ (Real-time)» دارد.
- **تعریف کوتاه:** محافظ سیستم در برابر بدافزارها.

### ۹.۸) Malware — بدافزار
- **تعریف:** اصطلاح کلی برای هر نرم‌افزاری که با سوءنیت طراحی شده تا به سیستم آسیب برساند، اطلاعات بدزدد یا دسترسی غیرمجاز بگیرد؛ چترِ همه‌ی تهدیدها: ویروس، کرم، تروجان، جاسوس‌افزار و باج‌افزار.
- **آموزش و کاربرد:** راه‌های ورود رایج بدافزار: پیوست ایمیل مشکوک، دانلود کرک و فایل از منابع ناشناس، لینک‌های پیامکی (اسمشیینگ) و درایوهای USB آلوده. بهترین دفاع ترکیبی است: سیستم به‌روز، آنتی‌ویروس فعال، رفتار احتیاط‌آمیز و بکاپ منظم.
- **تعریف کوتاه:** هر نرم‌افزار مخرب و سوءخیم.

### ۹.۹) Virus — ویروس
- **تعریف:** بدافزاری که خود را به یک فایل یا برنامه‌ی سالم «می‌چسباند» و فقط وقتی فعال و تکثیر می‌شود که کاربر آن برنامه‌ی میزبان را اجرا کند؛ بعد به فایل‌های دیگر سرایت می‌کند.
- **آموزش و کاربرد:** تفاوت کلیدی ویروس با کرم: ویروس برای تکثیر به **میزبان و اقدام کاربر** نیاز دارد (مثل ویروس آنفلوانزا که با تماس پخش می‌شود)، ولی کرم خودش مستقل حرکت می‌کند. علائم آلودگی: کندی ناگهانی، فایل‌های ناشناس و رفتار عجیب برنامه‌ها.
- **تعریف کوتاه:** بدافزار تکثیرشونده‌ی وابسته به میزبان و اجرای کاربر.

### ۹.۱۰) Trojan — تروجان / اسب تروا
- **تعریف:** بدافزاری که خود را به شکل یک برنامه‌ی سالم و مفید (بازی، کرک، ابزار رایگان) نشان می‌دهد تا کاربر داوطلبانه آن را نصب کند و درب را از داخل باز کند.
- **آموزش و کاربرد:** اسمش از اسب تروای افسانه‌ای آمده: هدیه‌ای که دشمن درونش پنهان است. برخلاف ویروس، تروجان خودش تکثیر نمی‌شود ولی درب پشتی (Backdoor) باز می‌کند تا مهاجم کنترل سیستم را در دست بگیرد یا اطلاعات برباید. قاعده‌ی طلایی: کرک‌ها و «نسخه‌ی رایگانِ برنامه‌های پولی» از رایج‌ترین حامل‌های تروجان‌اند.
- **تعریف کوتاه:** بدافزاری که با ظاهر یک برنامه‌ی سالم وارد سیستم می‌شود.

### ۹.۱۱) Worm — کرم کامپیوتری
- **تعریف:** بدافزار مستقلی که بدون نیاز به هیچ میزبان یا دخالت کاربر، خودش را به صورت خودکار از طریق شبکه (و از راه آسیب‌پذیری‌های سیستم‌ها) تکثیر و پخش می‌کند.
- **آموزش و کاربرد:** کرم معروف WannaCry سال ۲۰۱۷ در چند ساعت صدها هزار سیستم در ۱۵۰ کشور را آلوده کرد. بهترین دفاع در برابر کرم‌ها: نصب سریع **به‌روزرسانی‌های امنیتی** (چون از حفره‌های نرم‌افزاری سوءاستفاده می‌کنند)، فایروال فعال و غیرفعال‌کردن سرویس‌های اضافی.
- **تعریف کوتاه:** بدافزاری که خودکار و بدون دخالت کاربر در شبکه تکثیر می‌شود.

### ۹.۱۲) Spyware — جاسوس‌افزار
- **تعریف:** بدافزاری که مخفیانه روی سیستم نصب می‌شود و اطلاعات کاربر — از کلیدهای فشرده‌شده (Keylogger) و رمزها تا عادت‌های مرور وب — را جمع‌آوری و برای مهاجم می‌فرستد.
- **آموزش و کاربرد:** علائم احتمالی: تبلیغات پاپ‌اپ غیرعادی، تغییر ناگهانی صفحه‌ی اول مرورگر، کندی و مصرف غیرمعمول اینترنت. دفاع: دانلود فقط از منابع رسمی، چک‌کردن دسترسی‌های عجیب اپ‌ها (چرا یک فونت به میکروفون دسترسی می‌خواهد؟) و اسکن دوره‌ای با ابزارهای ضدجاسوسی.
- **تعریف کوتاه:** بدافزاری برای جاسوسی و سرقت اطلاعات کاربر.

### ۹.۱۳) Ransomware — باج‌افزار
- **تعریف:** بدافزاری که فایل‌های قربانی را **رمزنگاری** می‌کند و برای تحویل کلید رمزگشایی، «باج» (معمولاً ارز دیجیتال) طلب می‌کند.
- **آموزش و کاربرد:** قاعده‌ی اول: **باج ندهید** — نه تضمینی برای بازگشت فایل است و نه جلوی حمله‌ی دوباره را می‌گیرد، بلکه چرخه‌ی جرم را تغذیه می‌کند. تنها سپر واقعی: **بکاپ منظم و آفلاین** (باج‌افزار بکاپِ متصل را هم رمز می‌کند!). اگر آلوده شدید، سریع سیستم را از شبکه جدا کنید تا سرایت نکند.
- **تعریف کوتاه:** بدافزاری که فایل‌ها را قفل کرده و باج می‌خواهد.

### ۹.۱۴) Phishing — فیشینگ / حمله‌ی جعل
- **تعریف:** فریب فنی-اجتماعی که مهاجم با پیام، ایمیل یا سایتی جعلی اما بسیار شبیه به سازمان معتبر (بانک، مخابرات، اداره‌ی پست)، قربانی را وادار به افشای رمز، اطلاعات کارت یا نصب بدافزار می‌کند.
- **آموزش و کاربرد:** نشانه‌های فیشینگ: انگلیسی/فارسی شلخته، فوریت و ارعاب («کارت شما مسدود شد!»)، لینک کوتاه یا مشابه (paypa1.com به جای paypal.com) و درخواست اطلاعات حساس. قاعده‌ی طلایی: **هیچ بانکی هیچ‌وقت رمز کارت یا کد OTP را نمی‌پرسد**. قبل از کلیک، آدرس سایت را خودتان تایپ کنید. نسخه‌ی پیامکی آن «اسمشیینگ» و تلفنی‌اش «ویشینگ» نام دارد.
- **تعریف کوتاه:** دام جعلی برای سرقت اطلاعات با فریب کاربر.

### ۹.۱۵) Authentication — احراز هویت
- **تعریف:** فرایند تأیید این‌که «شما واقعاً همان کسی هستید که ادعا می‌کنید» — معمولاً با رمز عبور، اثر انگشت یا کد یک‌بارمصرف.
- **آموزش و کاربرد:** سه عامل احراز هویت: **چیزی که می‌دانید** (رمز)، **چیزی که دارید** (گوشی، کارت بانکی) و **چیزی که هستید** (بیومتریک: اثر انگشت، چهره). احراز هویت با Authorization (مجوز) فرق دارد: الاولی «کی هستی؟» می‌پرسد، دومی «چه مجاز هستی؟». ورود به اینترنت‌بانک با رمز = احراز هویت.
- **تعریف کوتاه:** اثبات هویت کاربر.

### ۹.۱۶) Authorization — مجوزدهی / اعطای دسترسی
- **تعریف:** فرایند تعیین این‌که یک کاربرِ تاییدهویت‌شده «به چه منابعی و تا چه حد» حق دسترسی دارد — خواندن، ویرایش، حذف یا مدیریت.
- **آموزش و کاربرد:** ترتیب درست: اول Authentication بعد Authorization — مثل ورود به برج اداری: اول کارت تردد می‌زنید (هویت)، بعد آسانسور فقط به طبقات مجاز می‌بردتان (مجوز). در سیستم‌ها با نقش‌ها (Role) پیاده می‌شود: مدیر، ویراستار، بازدیدکننده. خطای HTTP 403 (Forbidden) یعنی «حضور شناخته‌شده، اما غیرمجاز».
- **تعریف کوتاه:** تعیین سطح دسترسی بعد از احراز هویت.

### ۹.۱۷) MFA (Multi-Factor Authentication) — احراز هویت چندعاملی
- **تعریف:** روش امنیتی که برای ورود، ترکیبی از **دو یا چند عامل** احراز هویت می‌خواهد تا اگر یکی لو رفت، دیگری جلوی نفوذ را بگیرد؛ حالت خاص و رایج آن «دوعاملی (2FA)» است.
- **آموزش و کاربرد:** مثال: رمز عبور (چیزی که می‌دانید) + کد پیامکی یا اپلیکیشن تأییدکننده مثل Google Authenticator (چیزی که دارید). ⚠️ حتماً MFA را روی ایمیل، اینترنت‌بانک و شبکه‌های اجتماعی فعال کنید — مهم‌ترین اقدام امنیتیِ تک‌نفره است. اپلیکیشن‌های Authenticator امن‌تر از SMS‌اند (چون پیامک قابل سرقت خط است).
- **تعریف کوتاه:** ورود با چند لایه‌ی امنیتی، نه فقط رمز.

✅ **پایان فصل ۹**

---

<a id="chapter10"></a>
# فصل ۱۰ | رایانش ابری ☁️

رایانش ابری یعنی به‌جای خرید و نگهداری سخت‌افزار، قدرت پردازش و ذخیره‌سازی را از طریق اینترنت «اجاره» کنید — مثل برق که از پریز می‌گیرید و نیروگاه نمی‌سازید.

---

### ۱۰.۱) Cloud Computing — رایانش ابری
- **تعریف:** ارائه‌ی منابع محاسباتی (سرور، فضای ذخیره، پایگاه داده، نرم‌افزار) از طریق اینترنت به صورت سرویسِ تقاضامحور با پرداخت بر اساس مصرف (Pay-as-you-go).
- **آموزش و کاربرد:** شما همین حالا کاربر ابر هستید: Google Drive (ذخیره‌سازی ابری)، Gmail و Netflix (پخش فیلم‌اش روی ابر است). بزرگ‌ترین ارائه‌دهندگان: **AWS** آمازون، **Azure** مایکروسافت و **Google Cloud**. مزایا: بدون سرمایه‌ی اولیه، مقیاس‌پذیری فوری و دسترسی از هرجا. سه مدل سرویس‌اش (IaaS/PaaS/SaaS) همین‌جا می‌آیند.
- **تعریف کوتاه:** اجاره‌ی کامپیوتر و زیرساخت از طریق اینترنت.

### ۱۰.۲) SaaS — نرم‌افزار به‌عنوان سرویس
- **تعریف:** مدلی که نرم‌افزار کامل و آماده از طریق مرورگر در اختیارتان قرار می‌گیرد؛ هیچ چیز نصب نمی‌کنید و همه‌چیز — از سرور تا به‌روزرسانی — بر عهده‌ی ارائه‌دهنده است.
- **آموزش و کاربرد:** مثال‌های ملموس: **Gmail**، **Google Docs**، **Netflix**، Office 365 و Trello. مزیت: ورود با هر دستگاهی کار می‌کند و معمولاً اشتراک ماهانه است. شما فقط «کاربر» هستید؛ نگران هیچ بخش فنی نیستید.
- **تعریف کوتاه:** استفاده از نرم‌افزار کامل در مرورگر با اشتراک.

### ۱۰.۳) PaaS — پلتفرم به‌عنوان سرویس
- **تعریف:** مدلی که به برنامه‌نویسان یک «محیط آماده» (سیستم‌عامل، دیتابیس، ابزارهای اجرا) می‌دهد تا فقط روی کد خودشان تمرکز کنند و درگیر مدیریت سرور نشوند.
- **آموزش و کاربرد:** مثال‌ها: **Heroku** و Google App Engine. رابطه‌ی سه مدل را با تشبیه «پیتزا» حفظ کنید: **IaaS** = مواد اولیه و فر را می‌دهند (خودتان می‌پزید)، **PaaS** = خمیر و فر حاضر است (فقط موادش را شما می‌چینید)، **SaaS** = پیتزای آماده می‌خورید! PaaS نقطه‌ی تعادل بین آزادی و راحتی است.
- **تعریف کوتاه:** پلتفرم آماده برای ساخت و مستقرکردن برنامه.

### ۱۰.۴) IaaS — زیرساخت به‌عنوان سرویس
- **تعریف:** مدلی که زیرساخت پایه‌ی محاسباتی — سرور مجازی، فضای ذخیره و شبکه — را اجاره می‌دهد و مدیریت نصب سیستم‌عامل و نرم‌افزار با خودتان است؛ بیشترین کنترل و بیشترین مسئولیت.
- **آموزش و کاربرد:** مثال‌های معروف: **AWS EC2**، Azure Virtual Machines. برای شرکت‌ها حرفه‌ای است: به‌جای خرید و نگهداری سخت‌افزار گران‌قیمت، ظرف چند دقیقه یک سرور مجازی با مشخصات دلخواه می‌گیرید و ساعتی هزینه می‌پردازید. کاربران: معمولاً مدیران زیرساخت و مهندسان DevOps.
- **تعریف کوتاه:** اجاره‌ی سرور مجازی و زیرساخت محاسباتی.

### ۱۰.۵) Server — سرور
- **تعریف:** کامپیوتر قدرتمندی که دائم روشن است و «سرویس» ارائه می‌دهد: میزبانی سایت، نگه‌داری پایگاه داده، ارسال ایمیل یا ذخیره‌ی فایل — در خدمت درخواست‌های کلاینت‌ها.
- **آموزش و کاربرد:** انواع سرور بر اساس نقش: **وب‌سرور** (سایت‌ها)، **میل‌سرور** (ایمیل)، فایل‌سرور و دیتابیس‌سرور. سرورها معمولاً در دیتاسنترهایی با برق و اینترنت پشتیبان و خنک‌کننده‌ی قوی نگهداری می‌شوند. هر سایتی که باز می‌کنید از یک سرور گفته می‌آید.
- **تعریف کوتاه:** کامپیوتر همیشه‌روشنِ ارائه‌دهنده‌ی سرویس.

### ۱۰.۶) Client — کلاینت / کارخواه
- **تعریف:** دستگاه یا برنامه‌ای که از سرور درخواست سرویس می‌کند و پاسخ را دریافت و نمایش می‌دهد — در مدل دوطرفه‌ی «کلاینت-سرور».
- **آموزش و کاربرد:** مرورگر شما وقتی سایت باز می‌کند، یک کلاینت وب است؛ اپلیکیشن موبایل یک کلاینت برای سرورهای شرکتش. معماری کلاینت-سرور را با رستوران حفظ کنید: شما (کلاینت) سفارش می‌دهید و آشپزخانه (سرور) سرو می‌کند. نقطه‌ی مقابلش مدل «همتا-به-همتا (P2P)» است که واسطه ندارد.
- **تعریف کوتاه:** درخواست‌کننده و استفاده‌کننده‌ی سرویس از سرور.

### ۱۰.۷) Host — میزبان
- **تعریف:** هر دستگاه متصل به شبکه که سرویسی را ارائه یا میزبانی می‌کند (سایت، بازی، فایل)؛ در وب معمولاً به سرور میزبان سایت گفته می‌شود.
- **آموزش و کاربرد:** وقتی می‌گوییم «سایت روی هاست فلان‌شرکت است» یعنی فایل‌هایش روی سرور آن شرکت میزبانی می‌شود. در شبکه‌ی خانگی پیام‌های `ping` به یک «هاست» فرستاده می‌شود. فایل معروف `hosts` در سیستم‌عامل هم برای نگاشت دستی نام→آدرس به کار می‌رود. نام هاست (Hostname) شناسنامه‌ی دستگاه در شبکه است.
- **تعریف کوتاه:** دستگاهی که سرویس یا سایت را میزبانی می‌کند.

### ۱۰.۸) Virtual Machine (VM) — ماشین مجازی
- **تعریف:** شبیه‌سازی یک کامپیوتر کامل (پردازنده، رم، دیسک و سیستم‌عامل مستقل) که با نرم‌افزاری به نام هایپروایزر روی یک سرور فیزیکی مشترک اجرا می‌شود.
- **آموزش و کاربرد:** با نرم‌افزارهای **VMware** یا **VirtualBox** می‌توانید داخل ویندوزتان یک «ویندوز یا لینوکس مجازی» اجرا کنید — عالی برای تست نرم‌افزار، یادگیری لینوکس و اجرای برنامه‌ی مشکوک در محیط ایزوله (Sandbox). همین فناوری پایه‌ی کل صنعت ابری است: درخواستِ IaaS شما در واقع می‌شود یک VM.
- **تعریف کوتاه:** کامپیوتر مجازیِ مستقل درون یک کامپیوتر واقعی.

### ۱۰.۹) Container — کانتینر
- **تعریف:** بسته‌ی استاندارد و سبک شامل برنامه به‌همراه همه‌ی وابستگی‌هایش (کتابخانه‌ها و تنظیمات) که در هر محیطی — لپ‌تاپ برنامه‌نویس یا سرور ابری — دقیقاً یک‌سان اجرا می‌شود.
- **آموزش و کاربرد:** کانتینر مشکل تاریخی «روی سیستم من کار می‌کرد!» را حل کرد. تفاوت با ماشین مجازی: VM کل سیستم‌عامل را مجازی‌سازی می‌کند (سنگین، چند گیگ) ولی کانتینرها هسته‌ی سیستم‌عامل میزبان را شریک می‌شوند (سبک، چند مگ، راه‌اندازی در چند ثانیه). جریان مدرن توسعه‌ی نرم‌افزار روی همین استوار است.
- **تعریف کوتاه:** بسته‌ی سبک و قابل‌حمل برنامه با همه‌ی نیازمندی‌ها.

### ۱۰.۱۰) Docker — داکر
- **تعریف:** محبوب‌ترین پلتفرم ساخت، اجرا و مدیریت کانتینرها که فناوری کانتینر را برای برنامه‌نویسان ساده و استاندارد کرد.
- **آموزش و کاربرد:** دستورالعمل ساخت کانتینر در فایلی به نام **Dockerfile** نوشته می‌شود و کانتینر آماده‌شده «Image» نام دارد. با `docker run` برنامه را در چند ثانیه بالا می‌آورید. مخزن اشتراک ایمیج‌ها: **Docker Hub**. داکر امروزه مهارت استاندارد هر برنامه‌نویس و مهندس DevOps است.
- **تعریف کوتاه:** ابزار استاندارد کار با کانتینرها.

### ۱۰.۱۱) Kubernetes (K8s) — کوبرنتیز
- **تعریف:** سامانه‌ی متن‌باز ارکستراسیون کانتینرها؛ یعنی مدیریت خودکار صدها یا هزاران کانتینر روی خوشه‌ای از سرورها: مستقرسازی (Deployment)، مقیاس‌دهی و بازیابی خودکار.
- **آموزش و کاربرد:** اگر داکر «یک کانتینر» را مدیریت کند، کوبرنتیز رهبرِ ارکسترِ هزاران کانتینر است: اگر یک کانتینر مُرد خودش نمونه‌ی جدید می‌سازد، اگر ترافیک زیاد شد نمونه‌های بیشتر می‌سازد (Auto-scaling) و بار را پخش می‌کند. ساخت گوگل و الان استاندارد جهانی — به آن مختصراً **K8s** می‌گویند. سرویس‌های مدیریت‌شده‌اش: EKS آمازون و GKE گوگل.
- **تعریف کوتاه:** مدیر و هماهنگ‌کننده‌ی خودکار کانتینرها در مقیاس بزرگ.

✅ **پایان فصل ۱۰**

<a id="chapter11"></a>
# فصل ۱۱ | هوش مصنوعی 🤖

هوش مصنوعی دیگر علمی-تخیلی نیست؛ از جستجوی گوگل تا دستیارهای گفتاری و تصویرسازها، همه‌جا الفبای تازه‌ای شکل گرفته که این فصل به شما یاد می‌دهد.

---

### ۱۱.۱) AI (Artificial Intelligence) — هوش مصنوعی
- **تعریف:** شاخه‌ای از علوم کامپیوتر که به ماشین‌ها توانایی انجام کارهای نیازمند «هوش انسانی» را می‌دهد: یادگیری، استدلال، درک زبان، تشخیص تصویر و تصمیم‌گیری.
- **آموزش و کاربرد:** AI دیروز یعنی شطرنج‌باز کامپیوتری؛ امروز یعنی تشخیص چهره‌ی گوشی، پیشنهادهای یوتیوب و ماشین‌های خودران. AI یک «چتر» است: یادگیری ماشین و یادگیری عمیق زیرمجموعه‌های آن‌اند (رابطه‌اش را در پیوست مقایسه کرده‌ایم). نگرش کلیدی: هوش مصنوعی جادو نیست؛ برنامه‌هایی هستند که از داده الگو یاد می‌گیرند.
- **تعریف کوتاه:** شبیه‌سازی توانایی‌های هوشمندانه در ماشین.

### ۱۱.۲) Machine Learning (ML) — یادگیری ماشین
- **تعریف:** زیرشاخه‌ی هوش مصنوعی که به‌جای نوشتن قاعده‌ی صریح برای هر حالت، به ماشین امکان می‌دهد از روی **داده‌ها الگو بیاموزد** و روی داده‌ی ندیده پیش‌بینی کند.
- **آموزش و کاربرد:** تفاوت با برنامه‌نویسی سنتی: در روش کلاسیک «داده + قوانین → جواب» است، اما در ML «داده + جواب → ماشین قوانین را کشف می‌کند». مثال: فیلتر اسپم ایمیل از هزاران ایمیل برچسب‌خورده (اسپم/عادی) یاد گرفته. انواع یادگیری: **با نظارت** (داده‌ی برچسب‌دار)، **بدون نظارت** (کشف ساختار پنهان) و **تقویتی** (امتحان و خطا با جایزه).
- **تعریف کوتاه:** یادگیری ماشین از تجربه و داده، نه دستور مستقیم.

### ۱۱.۳) Deep Learning — یادگیری عمیق
- **تعریف:** شاخه‌ی قدرتمند یادگیری ماشین که با **شبکه‌های عصبی چندلایه (عمیق)** کار می‌کند و می‌تواند الگوهای بسیار پیچیده — تصویر، صدا و زبان — را بیاموزد.
- **آموزش و کاربرد:** پشت تمام موفقیت‌های چشمگیر دهه‌ی اخیر — تشخیص چهره، مترجم خودکار، مدل‌های زبانی — یادگیری عمیق است. «عمیق» یعنی لایه‌های پردازشی متوالی: لایه‌ی اول خطوط ساده، لایه‌های بعدی چشم و بینی و لایه‌های آخر کل چهره را تشخیص می‌دهد. نیازمندی: داده‌یانبوه و GPU قدرتمند و محاسبات سنگین.
- **تعریف کوتاه:** یادگیری الگوهای پیچیده با شبکه‌های عصبی چندلایه.

### ۱۱.۴) Neural Network — شبکه‌ی عصبی
- **تعریف:** مدل محاسباتی الهام‌گرفته از ساختار مغز انسان؛ مجموعه‌ای از «نورون‌های» مصنوعی که در لایه‌ها (ورودی، پنهان، خروجی) چیده شده‌اند و با وزن‌های قابل تنظیم به هم متصل‌اند.
- **آموزش و کاربرد:** کار هر نورون ساده است: ورودی‌ها × وزن‌ها، جمع و عبور از یک تابع؛ اما کنار هم، توانایی تشخیص دست‌خط، ترجمه‌ی زبان و... را می‌سازند. آموزش شبکه یعنی دقیق‌کردن همین «وزن‌ها» با میلیون‌ها مثال. مدل‌های پیشرفته‌ی امروزی (Transformer) هم خانواده‌ی شبکه‌های عصبی‌اند.
- **تعریف کوتاه:** مدل محاسباتی شبیه‌سازی‌شده از مغز.

### ۱۱.۵) LLM (Large Language Model) — مدل زبانی بزرگ
- **تعریف:** مدل عظیم یادگیری عمیق که روی حجم بسیار عظیمی از متن (کتاب، سایت، مقاله) آموزش دیده و می‌تواند زبان انسانی را بفهمد و تولید کند: پاسخ، ترجمه، خلاصه و کدنویسی.
- **آموزش و کاربرد:** نمونه‌ها: خانواده‌ی **GPT** (پشتیبان ChatGPT)، **Claude**، **Gemini** و مدل‌های متن‌بازی مثل Llama. توانایی شگفت‌انگیزشان از «پیش‌بینی کلمه‌ی بعدی» تک‌تک به پایه رسیده، ولی به همان دلیل گاهی **توهم (Hallucination)** می‌کنند — یعنی با اعتمادبه‌نفس اطلاعات غلط تولید می‌کنند؛ پس نکات مهم را همیشه راستی‌آزمایی کنید.
- **تعریف کوتاه:** مدل عظیم درک و تولید زبان انسانی.

### ۱۱.۶) Prompt — پرامپت / دستور ورودی
- **تعریف:** متنِ درخواست، سؤال یا دستور شما که به مدل هوش مصنوعی داده می‌شود تا پاسخ تولید کند؛ کیفیت پاسخ به‌شدت به کیفیت پرامپت بستگی دارد.
- **آموزش و کاربرد:** مهارت «مهندسی پرامپت (Prompt Engineering)» یعنی درخواست‌نویسی حرفه‌ای. فرمول عملی: **نقش + وظیفه + جزئیات + قالب خروجی**. پرامپت ضعیف: «مقاله بنویس»؛ پرامپت قوی: «به‌عنوان یک معلم فیزیک، در ۲۰۰ کلمه برای نوجوانان توضیح بده گرانش چیست و یک مثال روزمره بزن.»
- **تعریف کوتاه:** دستوری که به هوش مصنوعی می‌دهیم تا جواب بسازد.

### ۱۱.۷) Token — توکن
- **تعریف:** واحد پردازش متن در مدل‌های زبانی؛ بخشی از کلمه، کل کلمه یا حتی علامت نگارشی که متن قبل از پردازش به آن‌ها خرد (Tokenize) می‌شود.
- **آموزش و کاربرد:** در فارسی/انگلیسی، به‌طور میانگین هر ۱۰۰ کلمه ≈ ۱۰۰ تا ۱۵۰ توکن است. کاربرد عملی: ۱) محدودیت «حافظه‌ی» گفت‌وگو (Context Window) با توکن سنجیده می‌شود — گفت‌وگوی طولانی یعنی مدل شروعِ گفت‌وگو را فراموش می‌کند، ۲) هزینه‌ی استفاده از APIها معمولاً به‌ازای هر توکن محاسبه می‌شود.
- **تعریف کوتاه:** واحدهای خردشده‌ی متن برای پردازش مدل.

### ۱۱.۸) Dataset — مجموعه داده / دیتاست
- **تعریف:** مجموعه‌ی سازمان‌یافته‌ای از داده‌ها (متن، تصویر، عدد) که برای آموزش، ارزیابی یا آزمون مدل‌های هوش مصنوعی به کار می‌رود.
- **آموزش و کاربرد:** قانون طلایی: کیفیت مدل از کیفیت داده‌اش فراتر نمی‌رود — «زباله وارد شود، زباله خارج می‌شود» (GIGO). دیتاست معمولاً سه بخش می‌شود: **آموزش (Train)** برای یادگیری، **اعتبارسنجی (Validation)** برای تنظیم و **آزمون (Test)** برای سنجش نهایی. دیتاست معروف تصویری: MNIST (دست‌خط اعداد).
- **تعریف کوتاه:** داده‌هایی که مدل از رویشان یاد می‌گیرد.

### ۱۱.۹) Model — مدل
- **تعریف:** نتیجه و محصول نهایی فرایند آموزش؛ ساختار ریاضی «آموخته‌شده‌ای» که الگوهای داده را در وزن‌هایش ذخیره کرده و حالا می‌تواند روی ورودی جدید پیش‌بینی کند.
- **آموزش و کاربرد:** رابطه‌ی مفاهیم: **الگوریتم** دستور پخت است، **دیتاست** مواد اولیه و **مدل** غذای آماده‌شده! وقتی می‌گوییم «مدل تشخیص چهره» یعنی برنامه‌ای که با میلیون‌ها عکس آموزش دیده و حالا آموخته‌اش را اجرا می‌کند. مدل‌ها را می‌توان ذخیره، به اشتراک گذاشت (مثل پلتفرم Hugging Face) و به‌کار گرفت.
- **تعریف کوتاه:** برنامه‌ی آموزش‌دیده‌ی آماده برای کار.

### ۱۱.۱۰) Training — آموزش / ترینینگ
- **تعریف:** فرایند یادگیری مدل از روی دیتاست: مدل پیش‌بینی می‌کند، میزان خطایش سنجیده می‌شود و وزن‌هاش کمی‌کمی اصلاح می‌شوند تا خطا کمینه شود (با الگوریتم گرادیان کاهشی و پس‌انتشار).
- **آموزش و کاربرد:** آموزش مدل‌های بزرگ می‌تواند میلیون‌ها ساعت GPU و هزینه‌ی هنگفت ببرد — به همین دلیل اغلب از مدل‌های «از پیش آموزش‌دیده (Pre-trained)» استفاده و فقط روی داده‌ی خودمان «ریزتنظیم (Fine-tuning)» می‌کنیم. خبرهای «آموزش مدل جدید X میلیون دلار آب خورد» همین فرایند است.
- **تعریف کوتاه:** فرایند یاددادن الگوها به مدل با داده.

### ۱۱.۱۱) Inference — استنتاج / اینفرنس
- **تعریف:** مرحله‌ی به‌کارگیری مدلِ آموزش‌دیده برای تولید پاسخ روی داده‌ی جدید و ندیده؛ یعنی «زمان استفاده‌ی واقعی» از مدل.
- **آموزش و کاربرد:** تقسیم‌بندی مهم: **Training** = دوران مدرسه (گران و طولانی) و **Inference** = دوران کار (ارزان‌تر و سریع، ولی بی‌نهایت بار تکرار می‌شود). هر پاسخی که از ChatGPT می‌گیرید یک Inference است. برای Inference سرعت و هزینه معیار است — به همین دلیل مدل‌ها را فشرده (Quantize) می‌کنند تا روی دستگاه‌های کوچک هم اجرا شوند.
- **تعریف کوتاه:** به‌کارگیری مدل آموزش‌دیده برای پاسخ‌به‌کاربر.

✅ **پایان فصل ۱۱**

---

<a id="chapter12"></a>
# فصل ۱۲ | گرافیک و چندرسانه‌ای 🎨

دنیای تصویر دیجیتال قواعد خاص خودش را دارد: پیکسل، رزولوشن و تفاوت وکتور با راستر. این فصل برای هرکسی که با عکس، فیلم یا طراحی سروکار دارد لازم است.

---

### ۱۲.۱) Resolution — رزولوشن / وضوح
- **تعریف:** تعداد کل پیکسل‌های تشکیل‌دهنده‌ی یک تصویر یا نمایشگر، که به صورت «عرض × ارتفاع» بیان می‌شود؛ معیار اصلی جزئیات و وضوح تصویر.
- **آموزش و کاربرد:** استانداردهای رایج: **HD** = 1280×720، **Full HD** = 1920×1080، **2K/QHD** = 2560×1440 و **4K/UHD** = 3840×2160. هرچه رزولوشن بالاتر، جزئیات بیشتر اما فایل سنگین‌تر و نیاز به پردازش بیشتر. در مانیتور، رزولوشن بالا روی صفحه‌ی کوچک یعنی پیکسل‌های فشرده‌تر و تصویر شارپ‌تر.
- **تعریف کوتاه:** تعداد پیکسل‌های تصویر یا صفحه (مثل 1080×1920).

### ۱۲.۲) Pixel — پیکسل
- **تعریف:** کوچک‌ترین واحد و «ذره‌ی سازنده‌ی» هر تصویر دیجیتال و نمایشگر؛ نقطه‌ای کوچک با یک رنگ مشخص که کنار میلیون‌ها پیکسل دیگر، تصویر را می‌سازد.
- **آموزش و کاربرد:** کلمه از Picture + Element می‌آید. ترفند تجربی: تصویر را روی صفحه زیاد بزرگنمایی (Zoom) کنید — مربع‌های رنگی‌ای که می‌بینید همان پیکسل‌ها هستند. «مگاپیکسل» دوربین یعنی تعداد میلیون‌های پیکسل هر عکس. تعداد پیکسل بیشتر همیشه کیفیت بهتر نیست؛ اندازه‌ی هر پیکسل (نورگیری) هم مهم است.
- **تعریف کوتاه:** نقطه‌ی سازنده‌ی تصویر دیجیتال.

### ۱۲.۳) DPI — دی‌پی‌آی
- **تعریف:** مخفف Dots Per Inch؛ تعداد نقاط (جوهر یا پیکسل) در هر اینچ — معیار تراکم و کیفیت در **چاپ** (و حساسیت در ماوس گیمینگ).
- **آموزش و کاربرد:** برای چاپ عکس و تبلیغات، فایل **۳۰۰ DPI** استاندارد کیفیت چاپ است؛ برای نمایش روی مانیتور DPI معنایی ندارد و مهم «پیکسل» است (معیار صفحه‌ها PPI نام دارد). در ماوس، DPI بالاتر یعنی حساسیت بیشتر: با حرکت کوچک دست، نشانگر مسیر بیشتری طی می‌کند. دقت کنید: DPI بالا با فایل کم‌پیکسل باز هم چاپ «تار» است.
- **تعریف کوتاه:** تراکم نقاط در اینچ؛ معیار کیفیت چاپ.

### ۱۲.۴) Vector — برداری / وکتور
- **تعریف:** نوع گرافیکی که تصویر را با فرمول‌های ریاضی (خط، منحنی و شکل) توصیف می‌کند، نه با پیکسل؛ به همین دلیل در **هر اندازه‌ای** — از کارت ویزیت تا بیلبورد — بدون کوچک‌ترین افت کیفیت بزرگنمایی می‌شود.
- **آموزش و کاربرد:** فرمت‌های رایج: **SVG** (وب) و AI/EPS (ایلوستریتور). کاربرد اصلی: طراحی **لوگو**، آیکن و تایپوگرافی — چیزهایی که باید در ابعاد مختلف تمیز بمانند. نرم‌افزار استاندارد: Adobe Illustrator. محدودیت: برای عکس واقعی مناسب نیست (سایه روشن‌های پیوسته پیکسلی‌اند).
- **تعریف کوتاه:** گرافیک مقیاس‌پذیرِ بی‌افت با فرمول ریاضی.

### ۱۲.۵) Raster — شطرنجی / راستر
- **تعریف:** نوع گرافیکی که تصویر از شبکه‌ای از پیکسل‌ها ساخته می‌شود؛ تمام عکس‌های واقعی دنیای دیجیتال راسترند و با بزرگنمایی زیاد «پیکسلی و دانه‌دانه» می‌شوند.
- **آموزش و کاربرد:** فرمت‌های رایج: **JPG** (فشرده، برای عکس)، **PNG** (باکیفیت + شفافیت) و GIF. نرم‌افزار استاندارد ویرایش راستر: Adobe Photoshop. قاعده‌ی عملی: عکس = راستر، لوگو = وکتور. اگر لوگو را به‌صورت JPG تحویل بگیرید، در اندازه‌ی بزرگ خراب می‌شود — همیشه فایل وکتورش را هم بخواهید.
- **تعریف کوتاه:** تصویر پیکسلی (مانند عکس).

### ۱۲.۶) Codec — کدک
- **تعریف:** فناوری فشرده‌سازی و بازکردن (کدگذاری/کدگشایی) صدا و تصویر؛ بدون کدک، یک دقیقه فیلم خام چندین گیگابایت حجم می‌گرفت.
- **آموزش و کاربرد:** کدک‌های ویدیویی معروف: **H.264** (استاندارد جهانی فعلی)، **H.265/HEVC** (فشردگی دوبرابر برای 4K) و AV1. کدک‌های صوتی: **MP3** و AAC. تفاوت مهم: **کدک ≠ فرمت فایل** — MP4 فقط «ظرف (Container)» است که داخلش ویدیوی H.264 + صدای AAC می‌تواند باشد. اگر فیلمی صدا دارد و تصویر نه، کدکش روی پلیرتان نصب نیست.
- **تعریف کوتاه:** ابزار فشرده‌سازی و پخش صدا و تصویر.

### ۱۲.۷) Frame Rate (FPS) — نرخ فریم
- **تعریف:** تعداد فریم‌های (تصاویر) نمایش‌داده‌شده در هر ثانیه‌ی ویدیو یا بازی؛ هرچه بالاتر، حرکت روان‌تر.
- **آموزش و کاربرد:** استانداردهای مرجع: سینما **۲۴ فریم** (حس فیلم)، تلویزیون **۲۵/۳۰**، و گیمینگ **۶۰+** (مسابقاتی‌ها ۱۴۴ یا ۲۴۰ فریم با مانیتور مطابق!). نکته: «فریم خروجی کارت گرافیک» و «نرخ تازه‌سازی مانیتور (Hz)» باید هماهنگ باشند — کارت ۱۲۰ فریمی روی مانیتور ۶۰ هرتزی از ظرفیتش استفاده‌نشده می‌ماند.
- **تعریف کوتاه:** تعداد فریم‌های ویدیو در ثانیه (fps).

### ۱۲.۸) Rendering — رندرینگ / پردازش خروجی
- **تعریف:** فرایند محاسبه و تولید «تصویر یا خروجی نهایی» از داده‌های خام: تبدیل مدل سه‌بعدی به عکس واقع‌گرایانه، مونتاژ لایه‌ها به فیلم آماده، یا نمایش کد HTML به صفحه‌ی زیبا.
- **آموزش و کاربرد:** سه کاربرد رایج: ۱) **معماری و سینما** — ساخت فریم‌های واقع‌گرایانه از مدل‌های سه‌بعدی (ساعت‌ها زمان می‌برد)، ۲) **تدوین ویدیو** — خروجی‌گرفتن نهایی پروژه‌ی پریمیر، ۳) **مرورگر** — رندرکردن صفحات وب. رندر کاری سنگین است؛ به همین دلیل GPU تمام‌نشدنی‌ترین دوستِ رندرکارهاست.
- **تعریف کوتاه:** تولید خروجی تصویری نهایی از داده‌ها.

### ۱۲.۹) GPU Acceleration — شتاب‌دهی گرافیکی
- **تعریف:** استفاده از توان پردازنده‌ی گرافیکی (به‌جای فقط CPU) برای انجام سریع‌تر کارهای سنگین و موازی: رندر، تدوین، بازی، محاسبات هوش مصنوعی و حتی مرور وب.
- **آموزش و کاربرد:** GPU با هزاران هسته‌ی کوچک، برای کارهای «هزاران محاسبه‌ی هم‌شکل هم‌زمان» (پردازش پیکسل!) ده‌ها برابر CPU سرعت دارد. کاربرد عملی: فعال‌بودن «Hardware Acceleration» در نرم‌افزارهای تدوین (خروجی‌گیری چند برابر سریع‌تر) و چارچوب‌های هوش مصنوعی مثل CUDA انویدیا — دلیل طلایی‌بودن GPU در دنیای AI همین است.
- **تعریف کوتاه:** تقویت سرعت پردازش با استفاده از کارت گرافیک.

✅ **پایان فصل ۱۲**

<a id="chapter13"></a>
# فصل ۱۳ | اصطلاحات عمومی نرم‌افزار 📦

واژه‌هایی که هر روز در دنیای نرم‌افزار می‌شنوید: لایسنس، آپدیت، نسخه‌ی بتا و... . دانستنشان شما را از یک کاربر معمولی به کاربری آگاه تبدیل می‌کند.

---

### ۱۳.۱) Open Source — متن‌باز / اوپن‌سورس
- **تعریف:** نرم‌افزاری که «کد منبع» آن برای همگان آزادانه قابل مشاهده، مطالعه، تغییر و بازنشر است — بر پایه‌ی فرهنگ همکاری و شفافیت.
- **آموزش و کاربرد:** نمونه‌های موفق: **لینوکس**، فایرفاکس، VLC و وردپرس (که بخش بزرگی از وب روی آن می‌چرخد). مزیت‌ها: رایگان بودن معمولی، شفافیت امنیتی (هزاران چشم کد را بازبینی می‌کنند) و نبود وابستگی به یک شرکت. نکته: متن‌باز بودن با «رایگان بودن» یکی نیست — لایسنسش (مثل GPL و MIT) شرایط را تعیین می‌کند.
- **تعریف کوتاه:** نرم‌افزاری که کدش برای همه باز است.

### ۱۳.۲) Closed Source — بسته‌متن / اختصاصی
- **تعریف:** نرم‌افزاری که کد منبعش محرمانه است و مالک آن فقط فایل اجرایی را می‌دهد؛ کاربر حق دیدن، تغییر یا بازنشر کد را ندارد.
- **آموزش و کاربرد:** نمونه‌ها: ویندوز، فتوشاپ و اکثر بازی‌ها. مزیت: پشتیبانی رسمی و رابط کاربری یکدست (کاربر پول می‌دهد تا نگران نباشد). نقد: نمی‌توانید مطمئن شوید «داخلش چه می‌گذرد» و اگر شرکت پشتیبانی را قطع کند، شما می‌مانید و یک برنامه‌ی مرده. مدل درآمدی‌اش فروش لایسنس است.
- **تعریف کوتاه:** نرم‌افزاری که کدش محرمانه و اختصاصی است.

### ۱۳.۳) Freeware — رایگان‌افزار
- **تعریف:** نرم‌افزاری که استفاده‌اش **رایگان** است ولی کد منبعش باز نیست و مالک حقوقش را حفظ کرده است.
- **آموزش و کاربرد:** مثال‌ها: Adobe Acrobat Reader (نسخه‌ی خواندن) و Google Chrome. فرق با Open Source: در فری‌ویر فقط «مصرف» رایگان است، نه آزادی تغییر کد. برخی فری‌ویرها نسخه‌ی جذبِ مشتری برای نسخه‌ی پولی‌اند (مدل Freemium: رایگان + ویژگی‌های ویژه با پول).
- **تعریف کوتاه:** نرم‌افزار رایگان، اما نه متن‌باز.

### ۱۳.۴) Shareware — اشتراک‌افزار / سهم‌افزار
- **تعریف:** نرم‌افزاری که برای «امتحان» به‌صورت محدود (زمانی یا امکاناتی) رایگان داده می‌شود و برای استفاده‌ی کامل باید خریداری شود.
- **آموزش و کاربرد:** معروف‌ترین مثال: **WinRAR** با دوره‌ی آزمایشی ۴۰ روزه‌ی افسانه‌ای! اشکال رایج محدودیت: ۳۰ روز مهلت، واترمارک روی خروجی، یا غیرفعال‌بودن امکانات حرفه‌ای. این مدل «اول چشیدن، بعد خریدن» به کاربر اعتماد می‌دهد.
- **تعریف کوتاه:** نرم‌افزار آزمایشی که نسخه‌ی کاملش پولی است.

### ۱۳.۵) License — لایسنس / مجوز
- **تعریف:** قرارداد حقوقی که شرایط مجاز استفاده، کپی، تغییر و توزیع یک نرم‌افزار (یا داده/اثر) را مشخص می‌کند.
- **آموزش و کاربرد:** لایسنس‌های متن‌باز معروف: **MIT** (بسیار آزاد)، **GPL** (تغییراتتان هم باید آزاد شود) و Apache. آن متن طولانی «EULA» که هنگام نصب برنامه Accept می‌کنید، همان قرارداد لایسنس است. لایسنس‌ها انواعی مثل تک‌کاربره، خانوادگی و سازمانی دارند — خرید یک لایسنس و نصب روی ۱۰ سیستم معمولاً نقض قرارداد است.
- **تعریف کوتاه:** سند حقوقیِ شرایط استفاده از نرم‌افزار.

### ۱۳.۶) Update — به‌روزرسانی / آپدیت
- **تعریف:** انتشار جزئی برای **همان نسخه** نرم‌افزار با هدف رفع باگ، وصله‌های امنیتی و بهبودهای کوچک — بدون تغییر نسلی.
- **آموزش و کاربرد:** آپدیت‌ها معمولاً رایگان و گاهی خودکارند. مهم‌ترین توصیه‌ی امنیتی جهان: **آپدیت‌ها را به تأخیر نیندازید!** بخش بزرگی از حمله‌های موفق از راه حفره‌هایی بوده که «وصله‌اش منتشر شده اما قربانی نصب نکرده». آپدیت ویندوز، مرورگر و گوشی‌تان جدی‌ترین سپر رایگان است.
- **تعریف کوتاه:** بهسازی و رفع نقص نسخه‌ی فعلی.

### ۱۳.۷) Upgrade — ارتقا / آپگرید
- **تعریف:** جایگزینی با یک **نسخه‌ی جدید و بزرگ‌تر** (نسل بعد نرم‌افزار) یا قطعه‌ی سخت‌افزاری قوی‌تر — برخلاف آپدیت که اصلاح جزئی است.
- **آموزش و کاربرد:** مثال نرم‌افزاری: ارتقا از ویندوز ۱۰ به ویندوز ۱۱ — معمولاً با تغییرات اساسی و گاهی هزینه‌دار. مثال سخت‌افزاری: ارتقای رم از ۸ به ۱۶ گیگ. روش حفظ تفاوت: **Update** = تعمیر و سرویس ماشین فعلی، **Upgrade** = تعویض به مدل بالاتر.
- **تعریف کوتاه:** ارتقا به نسخه یا سخت‌افزار بالاتر.

### ۱۳.۸) Patch — پچ / وصله
- **تعریف:** فایل اصلاحی‌ی کوچک و هدفمند که برای رفع یک مشخص مشکل — معمولاً **حفره‌ی امنیتی یا باگ جدی** — فوراً به نرم‌افزار اضافه می‌شود.
- **آموزش و کاربرد:** روز معروف «سه‌شنبه‌ی پچ (Patch Tuesday)» روز انتشار منظم وصله‌های مایکروسافت است. در بازی‌های ویدیویی «Day One Patch» وصله‌ی حجیم روز اول برای رفع باگ‌های عجله‌ی عرضه است. در دنیای امنیت، فاصله‌ی بین منتشرشدن پچ و نصب‌شدنش، «پنجره‌ی خطر» است — هرچه کوتاه‌تر بهتر.
- **تعریف کوتاه:** وصله‌ی کوچک برای رفع فوری یک مشکل.

### ۱۳.۹) Crack — کرک
- **تعریف:** ابزار یا روش **غیرقانونی** برای دور زدن قفل لایسنس نرم‌افزارهای پولی تا بدون پرداخت استفاده شوند.
- **آموزش و کاربرد:** علاوه بر جنبه‌ی حقوقی، خطر واقعی کرک **امنیتی** است: بخش قابل‌توجهی از کرک‌ها حامل تروجان، استخراج‌کننده‌ی ارز دیجیتال و باج‌افزارند — «چیزی را که مجانی گرفته‌ای، با داده‌هایت پولش را داده‌ای». جایگزین‌های درست: نسخه‌های رایگان رسمی، نرم‌افزارهای متن‌باز معادل (مثل GIMP به‌جای فتوشاپ) و تخفیف‌های دانشجویی.
- **تعریف کوتاه:** دور زدن غیرقانونی قفل نرم‌افزار (با ریسک بدافزار).

### ۱۳.۱۰) Bug — باگ / خطا
- **تعریف:** خطا یا نقص در کد برنامه که باعث رفتار اشتباه، نتیجه‌ی غلط یا کرش می‌شود.
- **آموزش و کاربرد:** افسانه‌ی نام‌گذاری: سال ۱۹۴۷ یک «مگس واقعی» در کامپیوتر Mark II گیر کرد و در دفتر ثبت «First actual case of bug being found» نوشته شد — اگرچه کلمه قبل‌تر هم رایج بود. انواع باگ: منطقی (خروجی غلط)، رابط کاربری (دکمه کار نمی‌کند) و امنیتی (خطرناک‌ترین نوع). کاربران با «گزارش باگ (Bug Report)» توصیف دقیق مشکل + مراحل تکرار به توسعه‌دهنده کمک می‌کنند.
- **تعریف کوتاه:** خطا و اشکال در برنامه.

### ۱۳.۱۱) Feature — فیچر / قابلیت
- **تعریف:** یک قابلیت یا ویژگی مشخص و قابل استفاده در نرم‌افزار که برای کاربر ارزش ایجاد می‌کند.
- **آموزش و کاربرد:** «ویژگی‌های کلیدی» در صفحه‌ی هر محصول همین فیچرها هستند (مثل «حالت تاریک» یا «سینک ابری»). اصطلاح معروف در تیم‌های محصول: **Feature Creep** — وسوسه‌ی افزودن قابلیت‌های بیش‌ازحد که نرم‌افزار را شلوغ و کند می‌کند. قابلیت خوب: چیزی که مشکل واقعی کاربر را حل کند، نه هر ایده‌ی قشنگ.
- **تعریف کوتاه:** یک قابلیت یا ویژگی نرم‌افزار.

### ۱۳.۱۲) Beta — بتا
- **تعریف:** نسخه‌ی **آزمایشی** نرم‌افزار که قبل از عرضه‌ی نهایی به گروهی از کاربران واقعی داده می‌شود تا خطاها و بازخوردهایشان را بگیرند؛ مرحله‌ای بعد از آلفا (تست داخلی).
- **آموزش و کاربرد:** مراحل عرضه: **Alpha** (تست داخل شرکت، پر باگ) ← **Beta** (تست عمومی، نزدیک به نهایی ولی ممکن است باگ داشته باشد) ← **نسخه‌ی پایدار**. شرکت در «بتا‌تست» عمومی با بازی‌ها و اپ‌ها رایج است. نصب نسخه‌ی بتا روی گوشی اصلی‌تان توصیه نمی‌شود — ممکن است برق، پایداری یا داده‌ها قربانی شود.
- **تعریف کوتاه:** نسخه‌ی آزمایشی عمومی قبل از انتشار نهایی.

### ۱۳.۱۳) Stable — پایدار / استیبل
- **تعریف:** نسخه‌ی نهایی، تست‌شده و قابل اعتماد نرم‌افزار که برای استفاده‌ی عمومی و روزمره آماده است — نقطه‌ی مقابل نسخه‌های آزمایشی.
- **آموزش و کاربرد:** در حرفه می‌گویند «Production» — یعنی روی نسخه‌ی استیبل سرمایه و کار واقعی ببندید. نکته‌ی دانلود: همیشه «Latest Stable Release» را بگیرید، نه Nightly یا Dev را، مگر برای تستِ آگاهانه. منحنی تکامل هر نسخه: بتاهای متوالی ← Release Candidate (نامزد نهایی) ← Stable.
- **تعریف کوتاه:** نسخه‌ی نهایی و باثبات برای استفاده‌ی روزمره.

### ۱۳.۱۴) Version — ورژن / نسخه
- **تعریف:** شماره یا نام شناسایی‌کننده‌ی هر انتشار از نرم‌افزار که نشان می‌دهد دقیقاً با کدام مرحله از تکامل آن سروکار دارید.
- **آموزش و کاربرد:** رمزگشایی شماره‌گذاری استاندارد (Semantic Versioning) مثل `2.4.1`: عدد اول = **نسخه‌ی بزرگ** (تغییر سازگاری‌شکن)، عدد دوم = **قابلیت‌های جدید** (سازگار با قبل)، عدد سوم = **رفع باگ**. وقتی پشتیبانی می‌پرسد «چه نسخه‌ای نصب است؟» نسخه را از بخش About یا تنظیمات برنامه پیدا می‌کنید — دانستنش اولین قدم هر عیب‌یابی است.
- **تعریف کوتاه:** شماره‌ی شناسایی هر انتشار نرم‌افزار.

✅ **پایان فصل ۱۳**

---

<a id="appendix"></a>
# پیوست الف | جداول مقایسه‌ای ⚖️

هوشمندانه‌ترین راه یادگیری، مقایسه است. مفاهیم مشابهِ فسلی را این‌جا کنار هم بگذارید تا هیچ‌وقت قاطی نشوند.

### جدول ۱ — RAM در برابر ROM در برابر هارد
| ویژگی | RAM | ROM | هارد (HDD/SSD) |
|---|---|---|---|
| ماندگاری داده | ❌ موقت (با خاموشی پاک می‌شود) | ✅ دائمی | ✅ دائمی |
| سرعت | بسیار زیاد | زیاد | کمتر از رم |
| نقش | میز کار فعلی | دستورهای راه‌اندازی | انبار بلندمدت |
| تغییرپذیری | دائم در حال نوشتن/خواندن | معمولاً فقط خواندنی | خواندن/نوشتن آزاد |

### جدول ۲ — HDD در برابر SSD
| ویژگی | HDD | SSD |
|---|---|---|
| قطعه‌ی متحرک | دارد (صفحه و سوزن) | ندارد (تراشه‌ی فلش) |
| سرعت | ~۱۰۰–۲۰۰ مگابایت/ثانیه | ۵۰۰ مگابایت تا چند هزار (NVMe) |
| قیمت به‌ازای گیگابایت | ارزان | گران‌تر |
| مقاومت در برابر ضربه | ضعیف | عالی |
| بهترین کاربرد | آرشیو انبوه | سیستم‌عامل و برنامه‌ها |

### جدول ۳ — BIOS در برابر UEFI
| ویژگی | BIOS | UEFI |
|---|---|---|
| نسل | قدیمی (دهه ۱۹۸۰) | مدرن |
| رابط کاربری | متنی، فقط کیبورد | گرافیکی، با ماوس |
| سرعت بوت | کندتر | سریع‌تر |
| پشتیبانی دیسک >۲ ترابایت | ❌ | ✅ |
| امنیت (Secure Boot) | ❌ | ✅ |

### جدول ۴ — NTFS در برابر FAT32 در برابر exFAT
| ویژگی | NTFS | FAT32 | exFAT |
|---|---|---|---|
| سقف حجم هر فایل | نامحدودِ عملی | ۴ گیگابایت | نامحدودِ عملی |
| سازگاری با دستگاه‌ها | ویندوز (مک: فقط خواندنی) | تقریباً همه‌چیز | ویندوز + مک + دوربین |
| امنیت و مجوز | ✅ دارد | ❌ ندارد | ❌ ندارد |
| بهترین کاربرد | درایو ویندوز | فلش‌های بوت و دستگاه قدیمی | فلش/هارد بین ویندوز و مک |

### جدول ۵ — LAN در برابر WAN
| ویژگی | LAN | WAN |
|---|---|---|
| محدوده | خانه، اداره، ساختمان | شهر، کشور، جهان |
| مالکیت | شخص/سازمان | سرویس‌دهندگان مخابراتی |
| سرعت | بسیار بالا (گیگابیتی) | متغیر و کمتر |
| مثال | شبکه‌ی خانه‌ی شما | اینترنت |

### جدول ۶ — Hub در برابر Switch در برابر Router
| ویژگی | Hub | Switch | Router |
|---|---|---|---|
| هوشمندی | هیچ‌کدام | ارسال فقط به مقصد | مسیریابی بین شبکه‌ها |
| محدوده‌ی کار | داخل یک LAN | داخل یک LAN | اتصال LAN به WAN/اینترنت |
| وضعیت امروز | منسوخ | استاندارد شبکه‌های سیمی | قلب شبکه‌ی خانگی |

### جدول ۷ — IPv4 در برابر IPv6
| ویژگی | IPv4 | IPv6 |
|---|---|---|
| طول آدرس | ۳۲ بیت | ۱۲۸ بیت |
| شکل | 192.168.1.1 | 2001:0db8:85a3::8a2e:0370:7334 |
| تعداد آدرس | ~۴٫۳ میلیارد (رو به اتمام) | تقریباً بی‌نهایت |
| وضعیت | هنوز غالب | نسل آینده، در حال گسترش |

### جدول ۸ — HTTP در برابر HTTPS
| ویژگی | HTTP | HTTPS |
|---|---|---|
| رمزنگاری | ❌ ندارد | ✅ دارد (SSL/TLS) |
| امنیت داده در مسیر | ناامن (قابل شنود) | امن |
| پورت رایج | 80 | 443 |
| نشانه | http:// | https:// و قفل 🔒 |

### جدول ۹ — Compiler در برابر Interpreter
| ویژگی | Compiler | Interpreter |
|---|---|---|
| نحوه‌ی ترجمه | کل کد، یک‌جا | خط‌به‌خط، هم‌زمان با اجرا |
| خروجی | فایل اجرایی مستقل | اجرای مستقیم |
| سرعت اجرا | بیشتر | کمتر |
| مثال زبان‌ها | C، C++، Go | Python، JavaScript |

### جدول ۱۰ — Library در برابر Framework در برابر API در برابر SDK
| مفهوم | چیست | تشبیه آشپزی |
|---|---|---|
| Library | کدهای آماده، دعوت‌شده وقتی لازم شد | ادویه‌های قفسه |
| Framework | ساختار و اسکلتی که کد شما را خواسته | آشپزخانه‌ی آماده با قواعد آشپزخانه |
| API | قرارداد و درگاه ارتباط دو نرم‌افزار | منوی رستوران |
| SDK | همه‌چیز یک‌جا: ابزار + کتابخانه + مستندات | جعبه‌ی کامل مواد و دستور پخت |

### جدول ۱۱ — تفاوت انواع بدافزار
| بدافزار | ویژگی شاخص | راه ورود |
|---|---|---|
| Virus | چسبیده به میزبان، با اجرای کاربر پخش می‌شود | فایل‌های آلوده |
| Worm | تکثیر خودکار، بدون دخالت کاربر | حفره‌های شبکه |
| Trojan | ظاهر مفید و فریبنده (مثل کرک) | نصب داوطلبانه |
| Spyware | جاسوسی مخفیانه و سرقت اطلاعات | همراه نرم‌افزارها |
| Ransomware | قفل فایل‌ها و درخواست باج | فیشینگ و دانلود مشکوک |
| Phishing | فریب انسان با سایت/پیام جعلی | پیامک و ایمیل |

### جدول ۱۲ — Authentication در برابر Authorization
| | Authentication (احراز هویت) | Authorization (مجوزدهی) |
|---|---|---|
| سؤال | «تو کی هستی؟» | «به چه چیزی دسترسی داری؟» |
| ترتیب | همیشه اول | بعد از احراز هویت |
| مثال | رمز عبور + اثر انگشت | دسترسی مدیر در برابر کاربر عادی |
| خطای رایج | 401 Unauthorized | 403 Forbidden |

### جدول ۱۳ — SaaS در برابر PaaS در برابر IaaS (با تشبیه پیتزا 🍕)
| مدل | چه چیزی گرفته می‌شود | تشبیه | مثال |
|---|---|---|---|
| IaaS | زیرساخت خام (سرور مجازی، فضا) | مواد اولیه + فر — خودت می‌پزی | AWS EC2 |
| PaaS | محیط آماده‌ی توسعه و اجرا | خمیر و فر حاضر | Heroku |
| SaaS | نرم‌افزار کامل آماده‌ی استفاده | پیتزای آمادهٔ سرو | Gmail، Netflix |

### جدول ۱۴ — ماشین مجازی در برابر کانتینر
| ویژگی | Virtual Machine | Container |
|---|---|---|
| چه چیزی مجازی می‌شود | کل سخت‌افزار + سیستم‌عامل کامل | فقط محیط اجرای برنامه |
| حجم و سرعت | سنگین، بوت در دقیقه | سبک، راه‌اندازی در ثانیه |
| جداسازی | ایزولاسیون کامل‌تر | سبک‌تر، مشترک در هسته‌ی سیستم‌عامل |
| ابزار | VMware، VirtualBox | Docker، Kubernetes |

### جدول ۱۵ — Update در برابر Upgrade در برابر Patch
| | Update | Upgrade | Patch |
|---|---|---|---|
| مقیاس تغییر | جزئی | بزرگ و نسلی | نقطه‌ای و اورژانسی |
| هدف | رفع باگ + امنیت + بهبود | قابلیت‌ها و نسل جدید | وصله‌ی فوری حفره |
| هزینه | معمولاً رایگان | گاهی پولی | رایگان |
| مثال | آپدیت ویندوز | ویندوز 10 به 11 | پچ امنیتی مرورگر |

### جدول ۱۶ — Vector در برابر Raster
| ویژگی | Vector (برداری) | Raster (شطرنجی) |
|---|---|---|
| ساخت تصویر | فرمول ریاضی | پیکسل‌های رنگی |
| بزرگنمایی | بی‌نهایت، بدون افت | پیکسلی می‌شود |
| کاربرد | لوگو، آیکن، تایپوگرافی | عکس واقعی |
| فرمت‌ها | SVG، AI، EPS | JPG، PNG، GIF |

### جدول ۱۷ — انواع مدل‌های قیمت/انتشار نرم‌افزار
| مدل | رایگان؟ | کد باز؟ | شرط اصلی |
|---|---|---|---|
| Open Source | اغلب ✅ | ✅ | رعایت لایسنس (MIT/GPL و...) |
| Closed Source | ❌ | ❌ | خرید لایسنس |
| Freeware | ✅ کامل | ❌ | فقط استفاده |
| Shareware | ✅ محدود/موقت | ❌ | خرید برای نسخه‌ی کامل |

### جدول ۱۸ — رابطه‌ی AI، ML و DL
```
┌─────────────────────────────────────┐
│ AI (هوش مصنوعی) — بزرگ‌ترین چتر                                     │
│   ┌─────────────────────────────┐              │
│   │ ML (یادگیری ماشین)                                            │              │
│   │   ┌─────────────────────┐              │               │
│   │   │ DL (یادگیری عمیق)                        │              │               │
│   │   │ شبکه‌های عصبی عمیق                │              │               │
│   │   └─────────────────────┘              │               │
│   └─────────────────────────────┘               │
└─────────────────────────────────────┘
```
هر DL یک ML است و هر ML یک AI، ولی برعکسش درست نیست!

### جدول ۱۹ — Process در برابر Thread
| ویژگی | Process (فرایند) | Thread (رشته) |
|---|---|---|
| تعریف | برنامه‌ی در حال اجرا | واحد اجرایی درون فرایند |
| حافظه | مستقل و اختصاصی | مشترک با رشته‌های همان فرایند |
| سربار | سنگین‌تر | سبک‌تر و سریع‌تر |
| مثال | یک نمونه‌ی کروم | تب‌های کنار هم آن نمونه |

### جدول ۲۰ — Cookie در برابر Cache در برابر Session
| | Cookie | Cache | Session |
|---|---|---|---|
| چه چیزی نگه می‌دارد | اطلاعات شما (ورود، تنظیمات) | فایل‌های سایت (عکس، استایل) | شناسه‌ی نشست فعال شما |
| کجا ذخیره می‌شود | مرورگر شما | مرورگر شما | سمت سرور |
| پاک‌شدنش چه می‌کند | خروج از حساب‌ها | بارگذاری کندترِ دفعه‌ی اول | پایان ورود فعلی |

### جدول ۲۱ — File در برابر Folder در برابر Shortcut
| | چیست | حذفش چه می‌کند |
|---|---|---|
| File | خودِ داده | داده می‌رود |
| Folder | محفظه‌ی فایل‌ها | فایل‌های درونش هم می‌روند |
| Shortcut | فقط یک لینک/نشانگر | اصل فایل سالم می‌ماند |

---

<a id="quiz"></a>
# پیوست ب | خودآزمایی پایانی 📝

بدون نگاه‌کردن به متن، به این ۱۵ سؤال پاسخ دهید — سپس پاسخ‌نامه را باز کنید.

1. فرق RAM با هارد در دو جمله چیست؟
2. SSD از نظر ساختاری با HDD چه تفاوتی دارد و چرا سریع‌تر است؟
3. «Kernel» چه نقشی در سیستم‌عامل دارد؟
4. برای این‌که فایل ۶ گیگابایتی را هم روی ویندوز ببینید هم مک، فلش را با کدام سیستم‌فایل فرمت می‌کنید؟
5. DNS و DHCP هر کدام چه می‌کنند؟
6. فرق HTTP با HTTPS در یک کلمه چیست؟
7. Library و Framework را با یک جمله از هم جدا کنید.
8. «کلید خارجی» در پایگاه داده چه کاربردی دارد؟
9. تفاوت ویروس با کرم کامپیوتری چیست؟
10. چرا گفته می‌شود باج به باج‌افزار نباید پرداخت شود؟
11. MFA دقیقاً یعنی چه و یک نمونه‌ی ترکیبش را نام ببرید.
12. SaaS را با تشبیه پیتزا توضیح دهید و یک مثال بزنید.
13. در هوش مصنوعی فرق Training و Inference چیست؟
14. چرا برای طراحی لوگو Vector و برای عکس Raster استفاده می‌شود؟
15. Update، Upgrade و Patch را هر کدام با یک مثال کوتاه توضیح دهید.

<details>
<summary>✅ نمایش پاسخ‌نامه (کلیک کنید)</summary>

1. RAM حافظه‌ی **موقت** کار جاری است و با خاموشی پاک می‌شود؛ هارد حافظه‌ی **دائمی** ذخیره‌ی فایل‌هاست.
2. SSD قطعه‌ی متحرک ندارد و با تراشه‌ی فلش کار می‌کند؛ دسترسی الکترونیکی است نه مکانیکی، پس چند برابر سریع‌تر است.
3. هسته‌ی سیستم‌عامل: مدیریت مستقیم CPU، حافظه و دستگاه‌ها و واسط برنامه‌ها با سخت‌افزار.
4. **exFAT** — چون محدودیت ۴ گیگابایتی FAT32 را ندارد و هر دو سیستم آن را می‌شناسند.
5. DNS نام سایت را به IP ترجمه می‌کند؛ DHCP به صورت خودکار IP و تنظیمات شبکه را به دستگاه‌ها اجاره می‌دهد.
6. **رمزنگاری**.
7. در کتابخانه «شما» کد را صدا می‌زنید؛ در فریم‌ورک «فریم‌ورک» کد شما را صدا می‌زند (وارونگی کنترل).
8. پیوند دو جدول با اشاره به کلید اصلی جدول دیگر — مثل اتصال سفارش‌ها به مشتریان.
9. ویروس به فایل میزبان و اجرای کاربر نیاز دارد، ولی کرم **به‌تنهایی و خودکار** در شبکه تکثیر می‌شود.
10. چون بازگشت فایل تضمینی نیست، حمله‌ی دوباره ممکن است تکرار شود و چرخه‌ی جرم سودآور می‌شود؛ راه‌حل واقعی بکاپ آفلاین است.
11. احراز هویت چندعاملی: ترکیب دو یا چند عامل؛ مثلاً رمز عبور (می‌دانید) + کد اپلیکیشن Authenticator (دارید).
12. پیتزای آماده‌ی سرو: نرم‌افزار کامل بدون درگیری فنی؛ مثل Gmail یا Netflix.
13. Training = آموزش مدل با دیتاست (سنگین و یک‌بار)؛ Inference = استفاده‌ی مدل آموخته برای پاسخ به ورودی جدید (روزمره).
14. چون لوگو باید در هر اندازه‌ای بدون افت کیفیت بماند (فرمول‌های وکتور) و عکس واقعی ذاتاً مجموعه‌ای از پیکسل‌های رنگی است (راستر).
15. Update: آپدیت امنیتی ویندوز؛ Upgrade: جابه‌جایی از ویندوز ۱۰ به ۱۱ یا رم ۸ به ۱۶؛ Patch: وصله‌ی فوری حفره‌ی امنیتی مرورگر.

</details>

---

# 🎓 سخن پایانی

تبریک! حالا زبان مادری دنیای دیجیتال را می‌دانید. این تقریباً ۲۰۰ اصطلاح، الفبای کار با کامپیوتر، شبکه، برنامه‌نویسی، امنیت و هوش مصنوعی است.

**گام‌های بعدی پیشنهادی:**
- 🔁 هر هفته یک فصل را مرور کنید — تکرار، مادرِ مهارت است.
- 🃏 از «تعریف کوتاه»ها فلش‌کارت بسازید (اپلیکیشن Anki عالی است).
- 🛠️ عملی استفاده کنید: Task Manager را باز کنید، یک Query در یک دیتابیس آنلاین رایگان تست کنید و در مودم خانه به دنبال DHCP بگردید.
- 📖 این جزوه را به دوستانتان هم بدهید — آموزش‌دادن، عمیق‌ترین روش یادگیری است.

*دانشنامه‌ی اصطلاحات کامپیوتر و IT — نسخه‌ی ۱٫۰*
