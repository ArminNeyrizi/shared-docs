این یک **چیت‌شیت (Cheat Sheet) سریع و کاربردی برای زبان Asciidoc** است. این فرمت به دلیل سادگی و قابلیت تبدیل آسان به PDF، HTML و دیگر فرمت‌ها، برای مستندات فنی بسیار محبوب است.

---

### ۱. ساختار پایه (Document Structure)

```asciidoc
= عنوان سند
نویسنده، نام نویسنده
نسخه ۱.۰
تاریخ: ۲۰۲۶-۰۴-۲۰

== مقدمه
متن اصلی سند در اینجا قرار می‌گیرد.

=== زیربخش
متن مربوط به زیربخش.
```

*   **عنوان اصلی:** با `=` در خط اول شروع می‌شود.
*   **سطح‌بندی:** هر `=` اضافه، سطح پایین‌تری را نشان می‌دهد (`==` برای h2، `===` برای h3 و غیره).

---

### ۲. فرمت‌دهی متن (Text Formatting)

| عملیات | سینتکس | خروجی |
| :--- | :--- | :--- |
| **بولد (پررنگ)** | `**متن**` یا `__متن__` | **متن** |
| *ایتالیک (کج)* | `*متن*` یا `_متن_` | *متن* |
| ~~خط‌خورده~~ | `++~~متن~~++` یا `__--متن--__` | ~~متن~~ |
| `کد داخل خط` | `` `متن` `` | `متن` |
| ابرنوشته | `a^2^` | a² |
| زیرنوشته | `a~2~` | a₂ |

---

### ۳. لیست‌ها (Lists)

**لیست مرتب‌نشده (Bullet Points):**
```asciidoc
* آیتم اول
** آیتم فرعی
* آیتم دوم
```

**لیست مرتب‌شده (Numbered):**
```asciidoc
. آیتم اول
. آیتم دوم
. آیتم سوم
```

**لیست وظایف (Todo List):**
```asciidoc
* [ ] کار انجام نشده
* [x] کار انجام شده
```

---

### ۴. بلوک‌های کد (Code Blocks)

**کد با سینتکس‌هایyntax highlighting:**
```asciidoc
[source, python]
----
def hello():
    print("Hello World")
----
```

**کد بدون هایلایت (Plain Text):**
```asciidoc
[, text]
----
این یک متن ساده است و هایلایت نمی‌شود.
----
```

---

### ۵. جداول (Tables)

```asciidoc
[cols="2,1,1"]
|===
| ستون ۱ | ستون ۲ | ستون ۳
| ردیف ۱ | داده A | داده B
| ردیف ۲ | داده C | داده D
|===
```

*   `cols`: عرض ستون‌ها را مشخص می‌کند.
*   `|`: جداکننده سلول‌هاست.
*   `|===`: شروع و پایان جدول.

---

### ۶. نقل‌قول (Quotes/Admonitions)

برای هشدارها، نکات و یادآوری‌ها:

```asciidoc
NOTE: این یک نکته مهم است.

TIP: این یک نکته کاربردی است.

IMPORTANT: این بخش بسیار حیاتی است.

WARNING: این یک هشدار است.

CAUTION: این یک احتیاط است.
```

---

### ۷. لینک و تصویر (Links & Images)

**لینک:**
```asciidoc
https://example.com[لینک به سایت مثال]
mailto:user@example.com[ارسال ایمیل]
```

**تصویر:**
```asciidoc
image::logo.png[لوگو شرکت, width=200, align=center]
```

**تصویر به عنوان لینک:**
```asciidoc
link:https://example.com[image:example.png[تصویر لینک شده]]
```

---

### ۸. بلوک‌های اختصاصی (Special Blocks)

**توضیح کد (Code Block with Title):**
```asciidoc
.Example: توضیح کد بالا
[source, javascript]
----
console.log("Hello");
----
```

**دیاگرام (با استفاده از Mermaid - اگر پشتیبانی شود):**
```asciidoc
[mermaid]
----
graph TD
    A[شروع] --> B{آیا درست است؟}
    B -->|بله| C[ادامه]
    B -->|خیر| D[تلاش مجدد]
----
```

---

### ۹. ویژگی‌های پیشرفته (Advanced Features)

**نقل‌قول طولانی (Quote Block):**
```asciidoc
[quote, نویسنده, منبع]
____
این یک نقل قول طولانی است که در چند خط قرار می‌گیرد.
____
```

**تفکیک‌کننده (Horizontal Rule):**
```asciidoc
***
```

**تغییر جهت متن (LTR/RTL):**
```asciidoc
.ltr:
متن چپ به راست
```

---

### ۱۰. متادیتا (Meta Data)

در بالای فایل برای تنظیمات سند:

```asciidoc
:doctype: book
:toc: left
:toclevels: 3
:sectnums:
:revdate: 2026-04-20
:revnumber: 1.0
```

*   `toc`: جدول محتویات را فعال می‌کند.
*   `sectnums`: شماره‌گذاری بخش‌ها را فعال می‌کند.

---

### نکات کلیدی برای شروع سریع:
1.  از ویرایشگرهایی مثل **VS Code** با افزونه *AsciiDoc* استفاده کنید.
2.  برای تبدیل به PDF، از ابزار `asciidoctor-pdf` استفاده کنید.
3.  برای تبدیل به HTML، از `asciidoctor` استفاده کنید.

اگر سوال خاصی درباره سینتکس پیچیده‌تر دارید، بپرسید!