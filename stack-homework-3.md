## هدف پروژه
ساخت یک وب‌اپلیکیشن تک‌صفحه‌ای (SPA) که تجربه‌ی کاربری (UX) مشابه محیط ادیتور **Obsidian** با افزونه‌ی **LaTeX Suite** را شبیه‌سازی کند. هدف اصلی، درک عمیق از نحوه مدیریت رویدادهای کیبورد، پردازش رشته‌ها (String Manipulation) و رندر کردن زنده‌ی کد لاتک به HTML است.

##  تکنولوژی‌های مجاز
*   **فریم‌ورک:** آزاد 
*   **رندرینگ لاتک:** پیشنهاد می‌شود از **KaTeX** یا **MathJax** استفاده کنید (KaTeX سریع‌تر است).
*   **استایل‌دهی:** Tailwind CSS, Bootstrap یا CSS خالص (بسته به سلیقه).
*   **منبع الهام:** کدهای منبع پروژه [obsidian-latex-suite](https://github.com/artisticat1/obsidian-latex-suite) در گیت‌هاب.

##  ویژگی‌های مورد نیاز (Functional Requirements)

### ۱. محیط ادیتور دو بخشی (Split View)
صفحه باید به دو بخش تقسیم شود:
*   **سمت چپ (Input):** یک ادیتور متن ساده (می‌تواند `textarea` باشد یا یک `contenteditable div`).
*   **سمت راست (Output):** بخشی که خروجی لاتک رندر شده را نمایش می‌دهد.

### ۲. پشتیبانی از بلوک‌های ریاضی (Math Blocks)
دانشجو باید بتواند کدهای لاتک را در بلوک‌های زیر تایپ کند و خروجی را به صورت زنده ببیند:
*   **بلوک خطی (Inline):** استفاده از `$ ... $` (مانند: $x^2$).
*   **بلوک جداگانه (Block):** استفاده از `$$ ... $$` مانند: $$ E=mc^2 $$
### ۳. پیاده‌سازی رفتارهای کلیدی افزونه (Core Logic Simulation)
این بخش مهم‌ترین قسمت پروژه است. دانشجو باید منطق زیر را از کدهای TypeScript پروژه اصلی استخراج و پیاده‌سازی کند:

*   **تشخیص خودکار بلوک‌ها (Auto-Detection):**
    *   وقتی کاربر تایپ می‌کند، سیستم باید تشخیص دهد که آیا کاربر قصد شروع یک بلوک ریاضی را دارد یا خیر.
    *   *مثال:* اگر کاربر تایپ کند `$$` و اینتر بزند، باید یک بلوک جدید ایجاد شود.
*   **پشتیبانی از تگ‌های مخفف (Abbreviations):**
    *   مشابه افزونه اصلی، اگر کاربر کلمات مخفف لاتک را تایپ کند، باید به کد کامل تبدیل شوند.
    *   *مثال:* تایپ `\b` باید به `\begin{...}` تبدیل شود یا تایپ `\a` به `\alpha`.
    *   *نکته:* این نیاز به یک دیکشنری (Object/Map) در جاوااسکریپت دارد.
    *  نکته: لیست کامل Snippet ها داخل مستند رسمی گیت هاب موجود است.
*   **پیش‌نمایش زنده (Live Preview):**
    *   با هر تغییر در متن ورودی، خروجی سمت راست باید به‌روز شود.

### ۴. مدیریت خطاها (Error Handling)
*   اگر کاربر کد لاتک ناقص یا اشتباه تایپ کند (مثلاً `$` باز شده و بسته نشده)، نباید کل صفحه کرش کند.
*   بهتر است در بخش خروجی، یک پیام خطای واضح (با رنگ قرمز) نمایش داده شود که نشان دهد کدام بخش از کد مشکل دارد.

### ۵. ذخیره‌سازی موقت (Persistence)
*   متن تایپ شده کاربر باید در `localStorage` مرورگر ذخیره شود تا با رفرش کردن صفحه، اطلاعات از بین نرود.

---

## الزامات طراحی و رابط کاربری (UI/UX)
*   **تم تاریک/روشن:** دکمه‌ای برای تغییر تم (Dark/Light Mode) اضافه کنید.
*   **فونت مناسب:** از فونت‌های مناسب برای نمایش کد (مانند `Fira Code` یا `Monaco`) در بخش ورودی استفاده کنید.
*   **ریسپانسیو:** در موبایل، ادیتور و خروجی باید به صورت عمودی (زیر هم) نمایش داده شوند و در دسکتاپ به صورت افقی (کنار هم).

---

## معیارهای ارزیابی (Rubric)

این پروژه بر اساس ۴ معیار اصلی نمره‌دهی می‌شود:

### ۱. کیفیت کد (Code Quality) - ۴۰٪
*   **تمیزی کد (Clean Code):** استفاده از نام‌گذاری معنی‌دار برای متغیرها و توابع.
*   **ماژولار بودن:** منطق پردازش لاتک نباید داخل کامپوننت اصلی پخش شده باشد. باید به صورت توابع جداگانه (Utility Functions) یا کلاس‌های جداگانه نوشته شود.
*   **عدم تکرار (DRY):** از تکرار کد در بخش‌های مختلف پرهیز شود.
*   **کامنت‌گذاری:** کدهای پیچیده (مخصوصاً بخش‌هایی که از TypeScript اصلی گرفته شده‌اند) باید کامنت‌گذاری شوند تا منطق توضیح داده شود.

### ۲. عملکرد و منطق (Logic & Functionality) - ۳۰٪
*   دقت در شبیه‌سازی رفتارهای افزونه (تبدیل تگ‌ها، تشخیص بلوک‌ها).
*   سرعت رندرینگ (اگر از کتابخانه سنگین استفاده شده، باید بهینه باشد).
*   مدیریت صحیح خطاهای لاتک.

### ۳. رابط کاربری (UI/UX) - ۲۰٪
*   زیبایی بصری، چیدمان مناسب، و تجربه‌ی کاربری روان.
*   واکنش‌گرایی (Responsive) صحیح در دستگاه‌های مختلف.

### ۴. مستندات (Documentation) - ۱۰٪
*   یک فایل `README.md` شامل:
    *   نحوه نصب و اجرای پروژه.
    *   توضیحی درباره اینکه چگونه منطق TypeScript پروژه اصلی به JavaScript تبدیل شده است.
    *   اسکرین‌شات‌هایی از خروجی نهایی.

---

## راهنمایی برای دانشجوها (Hints)

1.  **تحلیل کد منبع:** قبل از شروع کدنویسی، به پوشه `src` در گیت‌هاب پروژه `obsidian-latex-suite` بروید. فایل‌های `utils.ts` یا `abbreviations.ts` را بررسی کنید. منطق تبدیل تگ‌ها معمولاً در این فایل‌هاست.
2.  **تبدیل TS به JS:** شما نیاز نیست TypeScript بلد باشید، اما می‌توانید کدهای TS را در مرورگر بخوانید. منطق آن‌ها معمولاً ساده است (استفاده از Regex برای پیدا کردن الگوها).
3.  **استفاده از Regex:** برای تشخیص `$` و `$$` و تبدیل آن‌ها، از عبارات باقاعده (Regular Expressions) قدرتمند استفاده کنید.
4.  **کتابخانه KaTeX:** مستندات KaTeX را مطالعه کنید تا بدانید چگونه متن لاتک را به HTML تبدیل کنید (`katex.renderToString`).

---

## مراحل پیشنهادی برای انجام پروژه (Milestones)

1.  **هفته اول (ساختار پایه):**
    *   راه‌اندازی پروژه (با React/Vue/Vanilla).
    *   ایجاد دو ستون (ورودی و خروجی).
    *   اتصال `textarea` به متن خروجی (نمایش متن خام لاتک در خروجی).

2.  **هفته دوم (رندرینگ لاتک):**
    *   نصب و پیکربندی `KaTeX`.
    *   نوشتن تابعی که متن ورودی را اسکن کند و بلوک‌های `$...$` و `$$...$$` را پیدا کرده و با `KaTeX` رندر کند.
    *   نمایش خروجی در ستون سمت راست.

3.  **هفته سوم (پیاده‌سازی هوشمند افزونه):**
    *   اضافه کردن دیکشنری تگ‌های مخفف (مثل `\b` به `\begin`).
    *   پیاده‌سازی منطق "تبدیل خودکار" هنگام تایپ (مثلاً وقتی کاربر `$$` را تایپ می‌کند و اینتر می‌زند).

4.  **هفته چهارم (بهبود و دیباگ):**
    *   اضافه کردن ذخیره‌سازی در `localStorage`.
    *   مدیریت خطاها (Try-Catch blocks).
    *   تمیزکاری کد (Refactoring) و افزودن مستندات.

---