## **هدف**

این سند مرجع اصلی توسعه Joinly است.

تمام توسعه‌دهندگان و Agentهای هوش مصنوعی موظف‌اند پیش از ایجاد هر قابلیت جدید این سند را مطالعه کنند.

در صورت تضاد بین پیاده‌سازی و این سند، این سند مرجع نهایی تصمیم‌گیری است.

---

# **فلسفه توسعه**

Joinly یک محصول بلندمدت است.

هدف، توسعه سریع یک MVP نیست؛ هدف ساخت یک پلتفرم قابل توسعه برای سال‌های آینده است.

تمام تصمیمات فنی باید بر اساس معیارهای زیر گرفته شوند:

- سادگی
- خوانایی
- توسعه‌پذیری
- قابلیت نگهداری
- مقیاس‌پذیری

---

# **معماری سیستم**

معماری سیستم بر پایه Client-Server طراحی می‌شود.

Frontend و Backend کاملاً از هم مستقل هستند.

معماری کلی:

```text
Browser

↓

Next.js Application

↓

API Layer

↓

Business Logic

↓

Database
```

هیچ صفحه‌ای نباید مستقیماً منطق کسب‌وکار را پیاده‌سازی کند.

---

# **فناوری‌های اصلی**

Frontend

- Next.js
- React
- TypeScript
- Tailwind CSS
- shadcn/ui

Backend

- Next.js API Routes (نسخه MVP)
- در آینده قابلیت مهاجرت به سرویس مستقل

Database

- PostgreSQL

ORM

- Prisma ORM

Authentication

- Supabase Auth

Storage

- Supabase Storage

Deployment

- Vercel

---

# **اصول معماری**

Joinly بر پایه معماری ماژولار توسعه می‌شود.

هر ماژول باید تا حد امکان مستقل باشد.

نمونه ماژول‌ها:

- Authentication
- Academy
- Course
- Enrollment
- Dashboard
- Project Management
- Time Tracking
- LMS

هیچ ماژولی نباید وابستگی غیرضروری به ماژول دیگر ایجاد کند.

---

# **ساختار پروژه**

```text
src/

app/

components/

modules/

lib/

hooks/

services/

types/

utils/

config/
```

هر قابلیت جدید ابتدا در پوشه modules توسعه داده می‌شود.

---

# **ساختار ماژول**

نمونه:

```text
modules/

course/

components/

actions/

services/

repository/

types/

validation/

constants/
```

هر ماژول مسئول تمام منطق مربوط به خودش است.

---

# **اصول کدنویسی**

کد باید:

- خوانا باشد.
- ساده باشد.
- قابل تست باشد.
- بدون تکرار باشد.

از پیچیدگی غیرضروری اجتناب شود.

---

# **TypeScript**

استفاده از TypeScript الزامی است.

استفاده از `any` ممنوع است، مگر در مواردی که دلیل مشخصی وجود داشته باشد.

---

# **کامپوننت‌ها**

کامپوننت‌ها باید:

- کوچک
- قابل استفاده مجدد
- مستقل

باشند.

کامپوننت‌های بزرگ باید شکسته شوند.

---

# **مدیریت State**

اولویت:

1. React State
2. Server Components
3. Context

تا زمانی که واقعاً نیاز نباشد از State Managerهای سنگین استفاده نشود.

---

# **فرم‌ها**

تمام فرم‌ها باید:

- اعتبارسنجی سمت کلاینت
- اعتبارسنجی سمت سرور

داشته باشند.

---

# **اعتبارسنجی**

اعتبارسنجی باید با Zod انجام شود.

هیچ داده‌ای نباید بدون اعتبارسنجی وارد دیتابیس شود.

---

# **API**

تمام APIها باید:

RESTful

باشند.

ساختار پاسخ:

```json
{
  "success": true,
  "data": {},
  "message": ""
}
```

در صورت خطا:

```json
{
  "success": false,
  "error": ""
}
```

---

# **دیتابیس**

اصول:

- Normalized
- Foreign Keys
- Indexهای مناسب
- Soft Delete در صورت نیاز

از ذخیره اطلاعات تکراری خودداری شود.

---

# **امنیت**

تمام Endpointها باید:

- احراز هویت
- مجوز دسترسی
- اعتبارسنجی

داشته باشند.

هیچ Endpointی نباید به کلاینت اعتماد کند.

---

# **لاگ‌گیری**

تمام عملیات مهم باید ثبت شوند.

مانند:

- ورود
- ثبت‌نام
- انتخاب واحد
- حذف انتخاب واحد
- ایجاد پروژه
- ثبت ساعت

---

# **مدیریت خطا**

Errorها باید:

- قابل فهم
- قابل ثبت
- قابل پیگیری

باشند.

Stack Trace نباید به کاربر نمایش داده شود.

---

# **طراحی رابط کاربری**

رابط کاربری باید از:

- shadcn/ui

استفاده کند.

در صورت نیاز، ابتدا کامپوننت‌های موجود توسعه داده شوند.

---

# **مستندسازی**

هر قابلیت جدید باید شامل موارد زیر باشد:

- توضیح عملکرد
- قوانین کسب‌وکار
- وابستگی‌ها

---

# **تست**

در آینده هر قابلیت باید شامل:

- Unit Test
- Integration Test

باشد.

---

# **قوانین توسعه با هوش مصنوعی**

Agentها مجاز نیستند:

- ساختار پروژه را بدون دلیل تغییر دهند.
- کتابخانه جدید اضافه کنند مگر با ضرورت فنی.
- فناوری‌های اصلی پروژه را جایگزین کنند.
- فایل‌های نامرتبط را تغییر دهند.
- منطق کسب‌وکار را بدون هماهنگی تغییر دهند.
- کد تکراری تولید کنند.
- وابستگی‌های بلااستفاده اضافه کنند.

---

# **اولویت تصمیم‌گیری**

در صورت وجود چند راه‌حل، اولویت به ترتیب زیر است:

1. ساده‌ترین راه‌حل صحیح
2. راه‌حل قابل نگهداری
3. راه‌حل مقیاس‌پذیر
4. راه‌حل با کمترین وابستگی
5. راه‌حل با بهترین تجربه توسعه‌دهنده (Developer Experience)

---
